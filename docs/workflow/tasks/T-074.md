# T-074

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `toolSubagent`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner toolSubagent |
| 依赖 | T-034 |
| TDD Red 用例 | • `toolSubagent` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E toolSubagent spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/run-subagent-tool.test.ts（或新建）<br>• apps/web/e2e/nodes/toolSubagent.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/run-subagent-tool.ts<br>• packages/node-runner/src/executors/run-subagent-tool.test.ts<br>• apps/web/e2e/nodes/toolSubagent.spec.ts<br>• docs/test/node-audit-rows/toolSubagent.md<br>• apps/web/src/features/editor/node-params/toolSubagent.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e toolSubagent |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/run-subagent-tool.test.ts`、`apps/web/e2e/nodes/toolSubagent.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test run-subagent-tool.test.ts`（文件创建前 module not found）；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e toolSubagent --retries=0`（audit row 创建前）
- 失败摘要（预期内）：单测文件不存在；E2E audit row `ENOENT: docs/test/node-audit-rows/toolSubagent.md`；E1049 validate 用例初版因 createWorkflow 即校验失败需改为先建合法 wf 再 validate 非法 definition

### Green

- 实现文件：`run-subagent-tool.ts`（既有实现审查通过，无需改动）；新增 `run-subagent-tool.test.ts`、`toolSubagent.spec.ts`、`toolSubagent.md`
- 运行命令：`pnpm --filter @rxwf/node-runner test run-subagent-tool.test.ts`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9925 RXWF_E2E_WEB_PORT=9365 pnpm --filter @rxwf/web test:e2e toolSubagent --retries=0 --workers=1`
- 通过摘要：单测 11 passed；E2E lite 4 passed（含 audit row、validate E1049、面板；`@plus` 在 lite 轨 skip）

### Refactor

- 变更说明：无执行器实现重构；E2E `@any` 块增加 `serial` 并将 validate 用例置于 panel 用例之前，避免 page 上下文 teardown 后后续 request-only 用例偶发 auth 401/ENOENT；未新增独立 `toolSubagent.tsx`（`node-param-schemas.ts` 通用面板已覆盖）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test run-subagent-tool.test.ts` | 11 passed | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9925 RXWF_E2E_WEB_PORT=9365 pnpm --filter @rxwf/web test:e2e toolSubagent --retries=0 --workers=1` | 4 passed | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus RXWF_E2E_API_PORT=9924 RXWF_E2E_WEB_PORT=9364 pnpm --filter @rxwf/web test:e2e toolSubagent --retries=0` | global-setup 失败：`docker-compose.plus.yml` api 服务无 image/build | 否（本机环境） |
| 验收标准核对 | `docs/test/node-audit-rows/toolSubagent.md` 四维 ok + executor satellite + status ok；单测 E2003/E1048/E1050/E3001；E2E spec 含 `@plus` 执行用例 | 代码就绪；plus 需 CI Docker + 可选本机 Ollama | 部分 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/run-subagent-tool.test.ts`
- `apps/web/e2e/nodes/toolSubagent.spec.ts`
- `docs/test/node-audit-rows/toolSubagent.md`

## 实现说明

审查确认 `toolSubagent` 卫星执行路径（`runSubagentTool` → 合成 `aiAgent` ReAct）、保存期 `E1049` 校验与 `node-param-schemas` 面板均已就绪；补充单测、审查行与 E2E 覆盖 matrix E2E-N-toolSubagent。

## 未解决问题

- 本机 plus compose 无 Docker image，plus 轨 E2E 需在 CI 验证；`@plus` Ollama 冒烟依赖本机 Ollama 可达
