# T-062

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `crewHierarchical`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner crewHierarchical |
| 依赖 | T-034 |
| TDD Red 用例 | • `crewHierarchical` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E crewHierarchical spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/crew-hierarchical.test.ts（或新建）<br>• apps/web/e2e/nodes/crewHierarchical.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/crew-hierarchical.ts<br>• packages/node-runner/src/executors/crew-hierarchical.test.ts<br>• apps/web/e2e/nodes/crewHierarchical.spec.ts<br>• docs/test/node-audit-rows/crewHierarchical.md<br>• apps/web/src/features/editor/node-params/crewHierarchical.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e crewHierarchical |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/crew-hierarchical.test.ts`（新增 registry/audit-row/E1031/E1032）、`apps/web/e2e/nodes/crewHierarchical.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test crew-hierarchical.test.ts`（新增 audit row 用例前 `ENOENT crewHierarchical.md`）；`RXWF_E2E_TRACK=lite ... playwright test crewHierarchical`（spec/audit row 创建前无匹配或 audit row 断言失败）
- 失败摘要（预期内）：单测 `crewHierarchical M-3 audit row` 因 `crewHierarchical.md` 不存在失败；E2E audit row 字段读取失败；registry `throws E2003 when not registered` 为设计性 Red（未注册 registry）

### Green

- 实现文件：审查确认 `crew-hierarchical.ts` 已注册；补充 `crew-hierarchical.test.ts`、`crewHierarchical.spec.ts`、`crewHierarchical.md`
- 运行命令：`pnpm --filter @rxwf/node-runner test crew-hierarchical.test.ts`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9971 RXWF_E2E_WEB_PORT=9411 pnpm --filter @rxwf/web exec playwright test crewHierarchical --retries=0`
- 通过摘要：9/9 单测；3/3 lite E2E（audit row + 面板「执行后端」/「Knowledge 注入模式」/「最大委派轮次」）

### Refactor

- 变更说明：E2E `createWorkflow` 使用 `uniqueWorkflowName` 规避 E1040 重名；`allowParallelDelegation` 改为 boolean；无执行器逻辑变更

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test crew-hierarchical.test.ts` | 9 passed | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9971 RXWF_E2E_WEB_PORT=9411 pnpm --filter @rxwf/web exec playwright test crewHierarchical --retries=0` | 3 passed | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus RXWF_E2E_API_PORT=9977 RXWF_E2E_WEB_PORT=9417 pnpm --filter @rxwf/web exec playwright test crewHierarchical --retries=0` | global-setup 失败：`docker-compose.plus.yml` api 服务无 image/build | 否（本机 compose 配置） |
| 验收标准核对 | `docs/test/node-audit-rows/crewHierarchical.md` 四维 ok + status ok；E1031/E1032/E1033/E2003 文档化；单测 registry/委派/并行/E1031/E1032 | 代码与 audit row 就绪；@plus validate/debug-node 用例待 CI compose 修复后执行 | 部分 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/crew-hierarchical.test.ts`
- `apps/web/e2e/nodes/crewHierarchical.spec.ts`
- `docs/test/node-audit-rows/crewHierarchical.md`
- `docs/workflow/tasks/T-062.md`

## 实现说明

审查结论：面板（`node-param-schemas` 含执行后端/最大委派轮次/允许并行委派 + `NodeEditorParamsPane` 条件字段）、校验（`validate.ts` E1031/E1032/W1012）、Plus 执行器（`registerPlusExecutors` + `executeCrewNode` → `runCrewHierarchicalNative`）、错误码均已就绪。补充 registry/E1031/E1032/E2003 单测、E2E spec（@any 面板 + @plus validate E1031/E1032 + Ollama 可选冒烟）与 audit row；未新增 `crewHierarchical.tsx`（通用面板已覆盖）。

## 未解决问题

- 本机 `deploy/docker-compose.plus.yml` 报 `service "api" has neither an image nor a build context`，plus 轨 E2E global-setup 未能启动；CI 修复 compose 后应跑通 `@plus`（E1031/E1032 validate + 可选 Ollama native 层级委派）
- 帮助文档 `docs/help/zh/nodes/crewHierarchical.md` 由 M-6 T-142 负责
