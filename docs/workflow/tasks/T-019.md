# T-019

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 1 |
| 描述 | credential-types 类型注册表与字段 schema 落地 |
| 追溯 | FR-05 / AC-015 / packages/credential |
| 依赖 | - |
| TDD Red 用例 | • 未注册 type 保存应校验失败<br>• oauth2 字段缺失应失败 |
| 建议测试文件 | • packages/credential/src/types/registry.test.ts |
| 文件所有权 | • packages/credential/src/types/registry.ts<br>• packages/credential/src/types/generic/*.ts |
| 验收标准 | credential-types spec AC 单测 green |
| 基本验证提示 | pnpm --filter @rxwf/credential test |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/credential/src/types/registry.test.ts`
- 运行命令：`pnpm --filter @rxwf/credential test -- registry`
- 失败摘要（预期内）：
  - `validateCredentialData rejects unknown type` — 无 registry 实现时模块不存在 / 未知 type 不抛错
  - `validateCredentialData rejects oauth2Manual missing accessToken` — 无 oauth2Manual schema 或未校验 required 时不抛 `/accessToken/`

### Green

- 实现文件：
  - `packages/credential/src/types/field-schema.ts` — CredentialFieldSchema / CredentialTypeDefinition / CredentialTypeSummary
  - `packages/credential/src/types/registry.ts` — register/get/list/validate + listCredentialTypeSummaries
  - `packages/credential/src/types/generic/api-key.ts`
  - `packages/credential/src/types/generic/http-header-auth.ts`（含 legacy `{ header }` applyAuth）
  - `packages/credential/src/types/generic/basic-auth.ts`
  - `packages/credential/src/types/generic/oauth2-manual.ts`
  - `packages/credential/src/types/register-generic-types.ts`
- 运行命令：`pnpm --filter @rxwf/credential test`
- 通过摘要：17 tests passed（registry 8 + apply-auth 4 + credential-service 5）

### Refactor

- 变更说明：无额外重构；generic 类型定义与 spec 字段 schema 一致，registry 保持最小实现。

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/credential test` | 17 passed | 是 |
| linter/build | `pnpm --filter @rxwf/credential build` | exit 0 | 是 |
| 冒烟（若适用） | — | N/A（纯库单元测试） | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/credential/src/types/field-schema.ts`
- `packages/credential/src/types/registry.ts`
- `packages/credential/src/types/registry.test.ts`
- `packages/credential/src/types/register-generic-types.ts`
- `packages/credential/src/types/generic/api-key.ts`
- `packages/credential/src/types/generic/http-header-auth.ts`
- `packages/credential/src/types/generic/basic-auth.ts`
- `packages/credential/src/types/generic/oauth2-manual.ts`
- `packages/credential/src/apply-auth.ts`
- `packages/credential/src/apply-auth.test.ts`
- `packages/credential/src/credential-service.ts`（validate on create / resolveForAuth / test 增强）
- `packages/credential/src/credential-service.test.ts`
- `packages/credential/src/index.ts`

## 实现说明

建立 Credential Type Registry（register/get/list/validate/listCredentialTypeSummaries），注册 Phase B 四种通用凭据类型（apiKey、httpHeaderAuth、basicAuth、oauth2Manual），字段 schema 驱动校验；httpHeaderAuth 兼容旧 `{ header }` 存储格式。

## 未解决问题

- 无（API 路由、HTTP 注入、Web 动态表单归属 T-020/T-027）
