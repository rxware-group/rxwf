# T-060

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `workflow_run`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner workflow_run |
| 依赖 | T-034 |
| TDD Red 用例 | • `workflow_run` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E workflow_run spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/workflow-run.test.ts（或新建）<br>• apps/web/e2e/nodes/workflow_run.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/workflow-run.ts<br>• packages/node-runner/src/executors/workflow-run.test.ts<br>• apps/web/e2e/nodes/workflow_run.spec.ts<br>• docs/test/node-audit-rows/workflow_run.md<br>• apps/web/src/features/editor/node-params/workflow_run.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e workflow_run |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/workflow-run.test.ts`（补充 registry/E1076）、`apps/web/e2e/nodes/workflow_run.spec.ts`（新建）、`docs/test/node-audit-rows/workflow_run.md`（新建）
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/workflow-run.test.ts`（补充用例前已有 3 项）；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web exec playwright test workflow_run --retries=0`（audit row / spec 创建前）
- 失败摘要（预期内）：E2E audit row `ENOENT: docs/test/node-audit-rows/workflow_run.md`；补充单测前缺 `throws E2003 when workflow_run executor is not registered`

### Green

- 实现文件：既有 `workflow-run.ts` 执行器无需改动；补充 `workflow-run.test.ts`、`workflow_run.spec.ts`、`workflow_run.md`；E2E 对齐 `readWriteFile`（`serial` + `expect(res.ok())`）
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/workflow-run.test.ts`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9920 RXWF_E2E_WEB_PORT=9360 pnpm --filter @rxwf/web test:e2e workflow_run -- --retries=0 --workers=1`
- 通过摘要：7/7 单测；5/5 lite E2E（audit row + 面板 + E1073 失败路径 + mini 模板执行）

### Refactor

- 变更说明：无执行器逻辑变更；`workflow_run.spec.ts` 增加 `test.describe.configure({ mode: 'serial' })` 稳定 API 认证；`debug-node` 对缺模板文件测 `E1073`（保存期 `E1076` 由单测覆盖）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test -- src/executors/workflow-run.test.ts` | 7 passed | 是 |
| E2E @any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9920 RXWF_E2E_WEB_PORT=9360 pnpm --filter @rxwf/web test:e2e workflow_run -- --retries=0 --workers=1` | 5 passed | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e workflow_run -- --retries=0 --workers=1` | global-setup 失败：`docker-compose.plus.yml` api/web 无 image/build | 否（本机基础设施） |
| linter/build | `pnpm --filter @rxwf/node-runner build` | tsc 失败于既有 `rag.test.ts`（非本 task 所有权） | 部分 |
| 验收标准核对 | `docs/test/node-audit-rows/workflow_run.md` 四维 ok + status ok；单测 registry/E1075/E1076；E2E 面板+执行 | 矩阵行 covered；plus compose 待 CI 基础设施 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/workflow-run.test.ts`
- `apps/web/e2e/nodes/workflow_run.spec.ts`
- `docs/test/node-audit-rows/workflow_run.md`
- `docs/workflow/tasks/T-060.md`

## 实现说明

审查结论：面板（`WorkflowRunFields` + schema）、校验（`validateWorkflowRunNodes` → E1076）、执行器（`createWorkflowRunExecutor` 经 `registerSkillExecutors`）、错误码 E1075/E1076/E2003/E1073 均已就绪。补充 registry/E1076 单测、E2E spec（serial 模式）与 audit row；无独立 `workflow_run.tsx`（专用 UI 在 `WorkflowRunFields.tsx`，所有权外）。

## 未解决问题

- 本机 `RXWF_E2E_TRACK=plus` global-setup 因 `deploy/docker-compose.plus.yml` 的 `api`/`web` 服务缺少 image/build 无法启动；CI 修复 compose 后应复跑 plus 轨
- `error-codes.md` 中 E1073 与 Web Search 码表语义冲突（audit row 已备注，待 T-080 统一）
- `pnpm --filter @rxwf/node-runner build` 因既有 `rag.test.ts` 类型错误失败（非本 task 引入）
