# T-113

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-5 |
| Wave | 5 |
| 描述 | M-5-acceptance.md + spec-gap Binary done |
| 追溯 | FR-11 / AC-054, AC-056, AC-070 / §9 Binary |
| 依赖 | T-111 |
| TDD Red 用例 | • B-6 未通过前实现测试必须失败 |
| 建议测试文件 | • docs/test/milestones/M-5-acceptance.test.mjs |
| 文件所有权 | • docs/test/milestones/M-5-acceptance.md<br>• docs/workflow/spec-gap-audit.md |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | pnpm --filter @rxwf/shared test; pnpm --filter @rxwf/web test:e2e binary |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`docs/test/milestones/M-5-acceptance.test.mjs`
- 运行命令：`node docs/test/milestones/M-5-acceptance.test.mjs`
- 失败摘要（预期内）：
  - `M-5-acceptance.md missing — AC-054 / T-113 blocked`
  - `GAP-012 gap must be done, got "partial"; audit_status must be done, got "open"`

### Green

- 实现文件：
  - `docs/test/milestones/M-5-acceptance.md`（11 条人工用例，AC-045～056 + AC-070）
  - `docs/workflow/spec-gap-audit.md`（GAP-012 → gap/audit_status done）
  - `docs/test/milestones/M-5-acceptance.test.mjs`（frontmatter 解析 CRLF 修复）
- 运行命令：`node docs/test/milestones/M-5-acceptance.test.mjs` → **3/3 pass**
- 通过摘要：验收清单含方案确认/上传/下载/表达式/透传五主题；GAP-012 Binary 差距关闭

### Refactor

- 变更说明：`parseYamlList` 限定 frontmatter 块并支持 CRLF，与 Windows 环境一致

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `node docs/test/milestones/M-5-acceptance.test.mjs` | 3/3 pass | 是 |
| spec-gap M-5 | `node scripts/validate-spec-gap-audit.mjs --milestone M-5` | OK: 40 gap rows | 是 |
| shared 单测 | `pnpm --filter @rxwf/shared test` | 21/21 pass | 是 |
| E2E 结构 | `node --experimental-strip-types apps/web/e2e/binary-full-chain.spec.test.ts` | 5/5 pass | 是 |
| E2E 冒烟 | `pnpm --filter @rxwf/web test:e2e binary` | 11/13 pass；AC-048/050-HTTP 因 httpbin 503 失败（外部依赖，T-111 已记录） | 是（本 task 为文档+gap 门禁；代码路径非回归） |
| linter/build | N/A（纯 Markdown + 测试脚本） | 跳过 | 是 |

**结论**：`passed`

## 实际变更文件

- `docs/test/milestones/M-5-acceptance.md`
- `docs/test/milestones/M-5-acceptance.test.mjs`
- `docs/workflow/spec-gap-audit.md`
- `docs/workflow/tasks/T-113.md`

## 实现说明

按 PRD §9 M-5 最低要求编写 11 条人工验收用例（方案确认 B-6、HTTP 下载/上传、Webhook multipart、`$binary` 表达式、透传/Merge 边界、blob 阈值、E2E 复现、matrix、GAP-012 meta）；同步将 spec-gap-audit GAP-012 标为 done。

## 未解决问题

- httpbin.org 间歇 503 导致 AC-048/050-HTTP E2E 重跑失败（外部服务，同 T-111）
- M5-MAN-010 matrix 行覆盖依赖 T-112（并行 task）；清单步骤已就绪，matrix status 更新待 T-112
