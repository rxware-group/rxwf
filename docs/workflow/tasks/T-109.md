# T-109

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-5 |
| Wave | 4 |
| 描述 | 表达式 $binary 读写 |
| 追溯 | FR-11 / AC-050 / §9 Binary |
| 依赖 | T-104 |
| TDD Red 用例 | • B-6 未通过前实现测试必须失败 |
| 建议测试文件 | • packages/expression/src/binary-globals.test.ts |
| 文件所有权 | • packages/expression/src/binary-globals.ts<br>• packages/expression/src/binary-globals.test.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | pnpm --filter @rxwf/shared test; pnpm --filter @rxwf/web test:e2e binary |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/expression/src/binary-globals.test.ts`
- 运行命令：`pnpm --filter @rxwf/expression test -- src/binary-globals.test.ts`
- 失败摘要（预期内）：`Cannot find module './binary-globals.js'`

### Green

- 实现文件：`packages/expression/src/binary-globals.ts`
- 运行命令：`pnpm --filter @rxwf/expression test -- src/binary-globals.test.ts`
- 通过摘要：15/15 pass — B-6 gate + resolveBinaryGlobal/readBinaryAttachment 读路径 + coerceExpressionBinaryResult 写路径 + evaluateJsExpression $binary 读写集成（AC-050）

### Refactor

- 变更说明：无；复用 `@rxwf/shared` 的 `isBinaryMap`/`isBinaryAttachment` 校验

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/expression test -- src/binary-globals.test.ts` | 15/15 pass | 是（AC-050 表达式 $binary 读写） |
| 受影响模块测试 | `pnpm --filter @rxwf/expression test` | 103/103 pass | 是 |
| linter/build | `pnpm --filter @rxwf/expression build` | pass | 是 |
| 冒烟（若适用） | `pnpm --filter @rxwf/web test:e2e binary` | 无 binary E2E spec（T-111） | 是（本 task 为 expression helper） |

**结论**：`passed`

## 实际变更文件

- `packages/expression/src/binary-globals.ts`（新增）
- `packages/expression/src/binary-globals.test.ts`（新增）

## 实现说明

在 expression 包提供 AC-050 专用 helper：`resolveBinaryGlobal`/`readBinaryAttachment` 读路径；`coerceExpressionBinaryResult`/`isExpressionBinaryResult` 写路径（§5.5 BinaryMap 返回值校验）。集成测试覆盖 IF 条件 `$binary.data.fileSize`、外置 ref、Set 返回 BinaryMap 及 `$binary` 透传。

## 未解决问题

- `index.ts` / `build-globals.ts` 未 re-export 新模块（非本 task 所有权）；后续 T-110 等节点任务可接线
