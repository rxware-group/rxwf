# T-110

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-5 |
| Wave | 4 |
| 描述 | Set/Merge 等节点 binary 合并策略 |
| 追溯 | FR-11 / AC-047 / §9 Binary |
| 依赖 | T-106 |
| TDD Red 用例 | • B-6 未通过前实现测试必须失败 |
| 建议测试文件 | • packages/node-runner/src/executors/transform/set-binary.test.ts |
| 文件所有权 | • packages/node-runner/src/executors/transform/set-binary.ts<br>• packages/node-runner/src/executors/control-flow/merge-binary.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | pnpm --filter @rxwf/shared test; pnpm --filter @rxwf/web test:e2e binary |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/control-flow/merge-binary.test.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/control-flow/merge-binary.test.ts`
- 失败摘要（预期内）：`Cannot find module './merge-binary.js'`

### Green

- 实现文件：`packages/node-runner/src/executors/control-flow/merge-binary.ts`；`packages/node-runner/src/executors/transform/set-binary.ts`（`applySetBinaryToItem`）
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/control-flow/merge-binary.test.ts src/executors/control-flow/merge.test.ts src/executors/transform/set-binary.test.ts`
- 通过摘要：20/20 pass — CONF-04 combineByKey 首个非空；combineAll 保留 upstream binary（GAP-01）

### Refactor

- 变更说明：`merge.ts` 委托 `merge-binary.ts`（combineByKey/combineAll）；Set 合并逻辑提取至 `applySetBinaryToItem` 供单测覆盖

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test -- src/executors/control-flow/merge-binary.test.ts src/executors/control-flow/merge.test.ts src/executors/transform/set-binary.test.ts src/executors/transform/set.test.ts` | 30/30 pass | 是（CONF-04 / GAP-01） |
| linter/build | read_lints merge-binary.ts set-binary.ts merge.ts | 无新增错误 | 是 |
| 冒烟（若适用） | `pnpm --filter @rxwf/shared test` | 21/21 pass | 是 |
| 冒烟（若适用） | `pnpm --filter @rxwf/web test:e2e binary` | 无 binary E2E spec（T-111） | 是（本 task 为节点合并策略） |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/control-flow/merge-binary.ts`（新增）
- `packages/node-runner/src/executors/control-flow/merge-binary.test.ts`（新增）
- `packages/node-runner/src/executors/transform/set-binary.ts`（`applySetBinaryToItem`）
- `packages/node-runner/src/executors/transform/set-binary.test.ts`（扩展）
- `packages/node-runner/src/executors/control-flow/merge.ts`（委托 merge-binary，修复 GAP-01）
- `packages/node-runner/src/executors/control-flow/merge.test.ts`（combineAll binary 集成断言）

## 实现说明

CONF-04：在 `merge-binary.ts` 实现 combineByKey「首个非空 binary」与 combineAll「合并全分支 upstream binary」；`merge.ts` 接线。Set 侧 `applySetBinaryToItem` 封装 json+binary merge 供单测与后续 delegate。

## 未解决问题

- `set.ts` 仍内联 binary merge，尚未 delegate 至 `applySetBinaryToItem`（非阻塞，行为已覆盖）
