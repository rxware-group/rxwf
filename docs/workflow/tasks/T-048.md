# T-048

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 2 |
| 描述 | 审查并修复 nodeType `httpRequest`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-028, AC-030 / node-runner httpRequest |
| 依赖 | T-034 |
| TDD Red 用例 | • `httpRequest` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E httpRequest spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/http.test.ts（或新建）<br>• apps/web/e2e/nodes/httpRequest.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/http.ts<br>• packages/node-runner/src/executors/http.test.ts<br>• apps/web/e2e/nodes/httpRequest.spec.ts<br>• docs/test/node-audit-rows/httpRequest.md<br>• apps/web/src/features/editor/node-params/httpRequest.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；lite 轨验收通过 |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e httpRequest |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/http.test.ts`（新增 registration + audit row 用例）；`apps/web/e2e/nodes/httpRequest.spec.ts`（新建）
- 运行命令：`pnpm --filter @rxwf/node-runner test http.test`；`pnpm --filter @rxwf/web test:e2e httpRequest`
- 失败摘要（预期内）：audit row 用例 `existsSync(httpRequest.md) === false`；E2E spec 文件不存在 / 无匹配测试

### Green

- 实现文件：`docs/test/node-audit-rows/httpRequest.md`、`apps/web/e2e/nodes/httpRequest.spec.ts`；`http.test.ts` 增补审查用例（执行器已注册，无需改 `http.ts`）
- 运行命令：`pnpm --filter @rxwf/node-runner test http.test`；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e httpRequest`
- 通过摘要：21 passed（http.test）；2 passed（auth setup + E2E-N-httpRequest 面板+debug-node）

### Refactor

- 变更说明：E2E 合并面板与执行为单测以避免 Playwright 多测间 auth storage 竞态；面板断言使用 zh-CN 标签（方法/Query 参数）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test http.test` | 21 passed | 是 |
| E2E lite 轨 | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e httpRequest` | 2 passed | 是 |
| linter/build | `pnpm --filter @rxwf/node-runner build` | exit 0 | 是 |
| 验收标准核对 | audit row status=ok；E2E-N-httpRequest spec 覆盖面板+执行 | 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/http.test.ts`
- `apps/web/e2e/nodes/httpRequest.spec.ts`
- `docs/test/node-audit-rows/httpRequest.md`
- `docs/workflow/tasks/T-048.md`

## 实现说明

审查确认 `httpRequest` 执行器、面板（Method/URL/高级 HTTP 字段）、Runner 策略校验（E1005）与 best-effort 运行时错误输出均已就绪；新增 M-3 审查行、registry/audit 单测与 lite 轨 E2E（httpbin GET）。

## 未解决问题

- `node-audit-matrix.md` / `e2e-coverage-matrix.md` 合并由 T-080/T-081 负责（本 task 不写 matrix）
- 帮助文档 `docs/help/zh/nodes/httpRequest.md` 待 M-6
