# T-038

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 2 |
| 描述 | 审查并修复 nodeType `errorTrigger`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-028, AC-030 / node-runner errorTrigger |
| 依赖 | T-034 |
| TDD Red 用例 | • `errorTrigger` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E errorTrigger spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/triggers/error.test.ts（或新建）<br>• apps/web/e2e/nodes/errorTrigger.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/triggers/error.ts<br>• packages/node-runner/src/executors/triggers/error.test.ts<br>• apps/web/e2e/nodes/errorTrigger.spec.ts<br>• docs/test/node-audit-rows/errorTrigger.md<br>• apps/web/src/features/editor/node-params/errorTrigger.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；lite 轨验收通过 |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e errorTrigger |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/triggers/error.test.ts`、`apps/web/e2e/nodes/errorTrigger.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/triggers/error.test.ts`；`pnpm --filter @rxwf/web test:e2e errorTrigger`
- 失败摘要（预期内）：E2E 面板用例 `textarea` 选择器未匹配 CodeMirror（`.cm-content`）；并行轨 `createWorkflow` 固定名称冲突导致 4/9 失败

### Green

- 实现文件：`error.test.ts`（补注册/E2002 用例）；`apps/web/e2e/nodes/errorTrigger.spec.ts`（面板 + debug-node）；`docs/test/node-audit-rows/errorTrigger.md`；执行器 `error.ts` 无需改动（已注册且行为正确）
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/triggers/error.test.ts`；`pnpm --filter @rxwf/web test:e2e errorTrigger`
- 通过摘要：单测 3/3；E2E 9/9（lite/standard/plus 三轨）

### Refactor

- 变更说明：无（E2E 修正选择器与唯一工作流名即可 Green）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test -- src/executors/triggers/error.test.ts` | 3 passed | 是 |
| 任务范围测试 | `pnpm --filter @rxwf/web test:e2e errorTrigger` | 9 passed | 是 |
| 受影响模块 | `pnpm --filter @rxwf/node-runner test -- src/executors/triggers/` | 22 passed | 是 |
| 验收标准核对 | audit-row `errorTrigger.md` 含 panel/validation/executor/error_codes/status=ok；E2E 覆盖面板与执行 | 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/triggers/error.test.ts`
- `apps/web/e2e/nodes/errorTrigger.spec.ts`（新建）
- `docs/test/node-audit-rows/errorTrigger.md`（新建）
- `docs/workflow/tasks/T-038.md`

## 实现说明

审查确认 `errorTrigger` 执行器已注册、面板 schema/默认参数完备；补注册与 E2002 单测、标准轨 E2E（CodeMirror 面板 + debug-node 样例载荷输出）及 audit-row 结论。

## 未解决问题

- `docs/test/e2e-coverage-matrix.md` E2E-N-errorTrigger 行 status 仍 uncovered（T-081 收口）
- `docs/test/node-audit-matrix.md` AUDIT-N-errorTrigger 列仍 pending（T-080 合并 audit-rows）
- `docs/help/zh/nodes/errorTrigger.md` 由 T-118 负责
