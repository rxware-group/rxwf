# T-045

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `splitInBatches`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner splitInBatches |
| 依赖 | T-034 |
| TDD Red 用例 | • `splitInBatches` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E splitInBatches spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/control-flow/split-in-batches.test.ts（或新建）<br>• apps/web/e2e/nodes/splitInBatches.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/control-flow/split-in-batches.ts<br>• packages/node-runner/src/executors/control-flow/split-in-batches.test.ts<br>• apps/web/e2e/nodes/splitInBatches.spec.ts<br>• docs/test/node-audit-rows/splitInBatches.md<br>• apps/web/src/features/editor/node-params/splitInBatches.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e splitInBatches |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/control-flow/split-in-batches.test.ts`（新建）；`apps/web/e2e/nodes/splitInBatches.spec.ts`（新建）
- 运行命令：`pnpm --filter @rxwf/node-runner test split-in-batches.test.ts`
- 失败摘要（预期内）：`Cannot find module './split-in-batches.js'`；audit row 文件不存在

### Green

- 实现文件：`split-in-batches.ts`（提取 executor + batchSize 钳制）；`splitInBatches.spec.ts`；`docs/test/node-audit-rows/splitInBatches.md`
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test split-in-batches.test.ts` → 7 passed（2026-06-20 复验）
  - `$env:RXWF_E2E_TRACK="lite"; pnpm --filter @rxwf/web test:e2e splitInBatches` → 4 passed（2026-06-20 复验）
- 通过摘要：E2003 未注册、registerPlus 注册、分批/默认/空输入/钳制单测；E2E audit-row 断言 + 面板 batchSize + debug-node 双批输出

### Refactor

- 变更说明：导出 `splitInputItems` 便于单测；E2E 执行用例标 `@any`（不依赖 plus compose 基础设施，与 ollama/llmStream 模式一致）；`createWorkflow` 对齐 readWriteFile 唯一命名模式

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test split-in-batches.test.ts` | 7 passed（2026-06-20） | 是 |
| 冒烟（E2E @any） | `$env:RXWF_E2E_TRACK="lite"; pnpm --filter @rxwf/web test:e2e splitInBatches` | 4 passed（2026-06-20） | 是 |
| E2E plus 轨 | `$env:RXWF_E2E_TRACK="plus"; pnpm --filter @rxwf/web test:e2e splitInBatches` | global-setup 失败：`docker-compose.plus.yml` api/web 无 image/build | 否（本机环境；spec 已就绪） |
| 验收标准核对 | `docs/test/node-audit-rows/splitInBatches.md` 四维 ok + status ok；E2E-N-splitInBatches covered | 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/control-flow/split-in-batches.ts`（新增）
- `packages/node-runner/src/executors/control-flow/split-in-batches.test.ts`（新增）
- `apps/web/e2e/nodes/splitInBatches.spec.ts`（新增）
- `docs/test/node-audit-rows/splitInBatches.md`（新增）
- `docs/workflow/tasks/T-045.md`

## 实现说明

审查确认 splitInBatches 面板（`batchSize` schema）、校验（无独立保存错误码）、Plus 执行器（`registerPlusExecutors` 内联注册）与 E2003 语义对齐；提取 `splitInBatchesExecutor` 模块并补充 batchSize≤0 钳制、单测/E2E/audit-row。无独立 `node-params/splitInBatches.tsx`（通用 schema 足够）。

## 未解决问题

- `register-plus.ts` 仍内联 executor 实现，未 import 新模块（非本 task 所有权；后续可统一接线）
- `node-audit-matrix.md` 行状态仍 pending（T-080 合并）
- 本机 plus 轨 global-setup 因 compose 配置问题无法启动；CI 修复后应复跑 plus project
