# T-055

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 3 |
| 描述 | 审查并修复 nodeType `ollama`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-025, AC-028, AC-030 / node-runner ollama |
| 依赖 | T-034 |
| TDD Red 用例 | • `ollama` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E ollama spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/ollama.test.ts（或新建）<br>• apps/web/e2e/nodes/ollama.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/ollama.ts<br>• packages/node-runner/src/executors/ollama.test.ts<br>• apps/web/e2e/nodes/ollama.spec.ts<br>• docs/test/node-audit-rows/ollama.md<br>• apps/web/src/features/editor/node-params/ollama.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；standard 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=standard pnpm --filter @rxwf/web test:e2e ollama |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/ollama.test.ts`、`apps/web/e2e/nodes/ollama.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test ollama.test.ts`；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web exec playwright test ollama --retries=0`（audit row 创建前）
- 失败摘要（预期内）：单测 `Cannot find module './ollama.js'`；E2E audit row `ENOENT: docs/test/node-audit-rows/ollama.md`

### Green

- 实现文件：`packages/node-runner/src/executors/ollama.ts`、`ollama.test.ts`、`apps/web/e2e/nodes/ollama.spec.ts`、`docs/test/node-audit-rows/ollama.md`
- 运行命令：`pnpm --filter @rxwf/node-runner test ollama.test.ts`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9905 RXWF_E2E_WEB_PORT=9345 pnpm --filter @rxwf/web exec playwright test ollama --retries=0`
- 通过摘要：5/5 单测；3/3 lite E2E（audit row + 面板 Model/Prompt + OllamaModelParamField）

### Refactor

- 变更说明：从 `register-plus.ts` 内联逻辑提取 `createOllamaExecutor` / `runOllamaChat` 至 `ollama.ts`；E2E `@standard` 按 Ollama 可达性条件 skip 成功/失败路径

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test ollama.test.ts` | 5 passed | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9905 RXWF_E2E_WEB_PORT=9345 pnpm --filter @rxwf/web exec playwright test ollama --retries=0` | 3 passed | 是 |
| E2E standard | `RXWF_E2E_TRACK=standard RXWF_E2E_API_PORT=9906 RXWF_E2E_WEB_PORT=9346 pnpm --filter @rxwf/web exec playwright test ollama --retries=0` | global-setup 失败：Docker daemon 未运行 | 否（本机环境） |
| 验收标准核对 | `docs/test/node-audit-rows/ollama.md` 四维 ok + status ok；E2003/E3001 单测；E2E spec 含 `@standard` 执行用例 | 代码就绪；standard 需 CI Docker + 可选本机 Ollama | 部分 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/ollama.ts`
- `packages/node-runner/src/executors/ollama.test.ts`
- `apps/web/e2e/nodes/ollama.spec.ts`
- `docs/test/node-audit-rows/ollama.md`
- `docs/workflow/tasks/T-055.md`

## 实现说明

审查结论：面板（schema + `OllamaModelParamField`）、校验（无额外保存错误码）、Plus 执行器（`registerPlusExecutors` 内联，逻辑已提取至 `ollama.ts`）、错误码 E2003/E3001 均已就绪。补充单测、E2E spec 与 audit row；未新增独立 `ollama.tsx`（通用面板已覆盖）。

## 未解决问题

- 本机 Docker Desktop 未启动，standard 轨 E2E 未能实际执行；CI 启 Docker 后应跑通 `@standard`（Ollama 不可达时走 failed 路径，可达时走 success 路径）
- `register-plus.ts` 仍内联 ollama 执行器（所有权外）；后续可改为 `createOllamaExecutor(deps)` 导入以消除重复
