# T-020

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 1 |
| 描述 | credential-types API 路由与 apply-auth 集成 |
| 追溯 | FR-05 / AC-015 / apps/api credential |
| 依赖 | - |
| TDD Red 用例 | • POST credential 未加密存储应失败<br>• apply-auth 未注入 header 应失败 |
| 建议测试文件 | • apps/api/src/routes/credentials.test.ts<br>• packages/credential/src/apply-auth.test.ts |
| 文件所有权 | • apps/api/src/routes/credentials.ts<br>• apps/api/src/routes/credentials.test.ts |
| 验收标准 | API CRUD + apply-auth green |
| 基本验证提示 | pnpm --filter @rxwf/api test credentials |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/api/src/routes/credentials.test.ts`（新增 4 用例：加密存储校验、DELETE CRUD、apply-auth 成功/失败）
- 运行命令：`pnpm --filter @rxwf/api test credentials`
- 失败摘要（预期内）：apply-auth 端点 404（2 failed | 6 passed）；加密存储与 CRUD 用例已通过（既有实现已满足）

### Green

- 实现文件：`apps/api/src/routes/credentials.ts`（新增 `POST /api/credentials/:credentialId/apply-auth`，集成 `resolveForAuth` + `applyAuth`，空 header 返回 400）
- 运行命令：`pnpm --filter @rxwf/api test credentials`
- 通过摘要：8/8 passed

### Refactor

- 变更说明：无额外重构；apply-auth 空 header 校验内联于路由 handler

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/api test credentials` | 8/8 passed | 是 |
| linter/build | `pnpm --filter @rxwf/api build` | 失败（`knowledge-bases.ts` 预存 TS2305，与本次变更无关） | 是（变更文件无 lint 问题，测试全绿） |
| 冒烟（若适用） | — | 不适用 | 是 |

**结论**：`passed`

## 实际变更文件

- `apps/api/src/routes/credentials.ts`
- `apps/api/src/routes/credentials.test.ts`

## 实现说明

新增 `POST /api/credentials/:credentialId/apply-auth` 端点：解密凭据后调用 `applyAuth` 返回 auth headers；若未产生有效 header 则 400。补充加密存储断言、DELETE CRUD 与 apply-auth 集成测试。

## 未解决问题

- `@rxwf/api` 全量 `build` 因 `knowledge-bases.ts` 预存类型错误失败，非本 task 范围
