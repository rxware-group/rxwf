# T-009

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-1 |
| Wave | 2 |
| 描述 | package.json + CI 集成 pnpm lint:docs-index |
| 追溯 | FR-04 / AC-004 / §10.3 |
| 依赖 | T-005 |
| TDD Red 用例 | • CI workflow 未调用 lint:docs-index 时门禁测试失败 |
| 建议测试文件 | • .github/workflows/*.yml 快照或 ci-docs-index.test.mjs |
| 文件所有权 | • package.json<br>• .github/workflows/ci.yml（或等效 CI 配置） |
| 验收标准 | PR 未登记 md 阻塞合并 |
| 基本验证提示 | pnpm lint:docs-index |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`scripts/ci-docs-index.test.mjs`
- 运行命令：`node scripts/ci-docs-index.test.mjs`
- 失败摘要（预期内）：2 failed — `package.json` 缺少 `lint:docs-index` 脚本；`ci.yml` 未调用 `pnpm lint:docs-index`

### Green

- 实现文件：`package.json`（`lint:docs-index`、`test:ci-docs-index-gate`）、`.github/workflows/ci.yml`（新增 `pnpm lint:docs-index` 步骤）
- 运行命令：`node scripts/ci-docs-index.test.mjs`、`pnpm test:ci-docs-index-gate`
- 通过摘要：2 passed, 0 failed

### Refactor

- 变更说明：无（实现已足够简洁）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `node scripts/ci-docs-index.test.mjs` | 2 passed, 0 failed | 是 |
| 任务范围测试 | `node scripts/lint-docs-index.test.mjs` | 2 passed, 0 failed | 是 |
| linter/build | `pnpm lint:docs-index` | exit 1，271 篇未登记 md（INDEX 全量登记属 T-166；校验行为符合 AC-004 阻塞语义） | 是 |
| 冒烟（若适用） | `pnpm test:ci-docs-index-gate` | 2 passed, 0 failed | 是 |

**结论**：`passed`

## 实际变更文件

- `package.json`
- `.github/workflows/ci.yml`
- `scripts/ci-docs-index.test.mjs`

## 实现说明

在 `package.json` 新增 `lint:docs-index`（调用 T-005 脚本）与 `test:ci-docs-index-gate`；CI `test` job 在 `lint:ac-mapping` 之后运行 `pnpm lint:docs-index`，未登记 md 将导致 PR 合并阻塞。

## 未解决问题

- 全 repo 仍有 271 篇 md 未登记于 INDEX（T-166 全量登记前 CI 将红）；本任务仅完成门禁接线，符合 M-1 分阶段计划。
