# T-071

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `toolHttp`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner toolHttp |
| 依赖 | T-034 |
| TDD Red 用例 | • `toolHttp` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E toolHttp spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/agent-satellite-tools.test.ts（或新建）<br>• apps/web/e2e/nodes/toolHttp.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/agent-satellite-tools.ts<br>• packages/node-runner/src/executors/agent-satellite-tools.test.ts<br>• apps/web/e2e/nodes/toolHttp.spec.ts<br>• docs/test/node-audit-rows/toolHttp.md<br>• apps/web/src/features/editor/node-params/toolHttp.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e toolHttp |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`agent-satellite-tools.test.ts`（toolHttp 卫星/registry/audit 用例）、`toolHttp.spec.ts`（audit row / E2E）
- 运行命令：`npx vitest run agent-satellite-tools.test.ts -t "toolHttp"`（Red 前 audit row 缺失）；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e toolHttp`（spec 缺失或面板断言不符时失败）
- 失败摘要（预期内）：audit row 文件不存在；E2E 未覆盖面板/E2003/validate

### Green

- 实现文件：无 `agent-satellite-tools.ts` 业务变更（既有 `buildAgentToolDefinitions` toolHttp 分支与 `run-ai-agent-node` HTTP invoke 已就绪）；新增/完善 `toolHttp.md`、`toolHttp.spec.ts`、单测
- 运行命令：`npx vitest run agent-satellite-tools.test.ts -t "toolHttp"`；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e toolHttp`
- 通过摘要：5 项 toolHttp 单测通过（含 audit row E2003/E3012）；E2E 5/5（audit row、面板 `方法`/URL/Headers/Body/Tool 描述、E2003 debug-node、validate）

### Refactor

- 变更说明：audit row 单测补充 E2003/E3012 断言（与 toolMcp 等卫星审查一致）；无业务代码 refactor

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `npx vitest run agent-satellite-tools.test.ts -t "toolHttp"` | 5 passed（2026-06-20 复核） | 是 |
| 任务范围测试 | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e toolHttp` | 5 passed（2026-06-20 复核） | 是 |
| linter/build | 未单独跑 typecheck（无 TS 实现变更） | N/A | 是 |
| 冒烟（若适用） | plus compose 在本环境不可用；lite E2E 覆盖面板/E2003/validate | lite 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/agent-satellite-tools.test.ts`
- `apps/web/e2e/nodes/toolHttp.spec.ts`（新建）
- `docs/test/node-audit-rows/toolHttp.md`（新建）

## 实现说明

审查确认 `toolHttp` 为卫星节点：`buildAgentToolDefinitions` 注册 `http` source，`run-ai-agent-node` 负责 HTTP 执行；无独立 executor。补充单测、审查行与 E2E（面板、E2003、validate）。

## 未解决问题

- `RXWF_E2E_TRACK=plus` 在本环境因 `docker-compose.plus.yml` 缺少 api build context 未能启动；确定性用例已在 lite `@any` 轨验证。
