# T-049

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 2 |
| 描述 | 审查并修复 nodeType `wait`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-028, AC-030 / node-runner wait |
| 依赖 | T-034 |
| TDD Red 用例 | • `wait` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E wait spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/control-flow/wait.test.ts（或新建）<br>• apps/web/e2e/nodes/wait.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/control-flow/wait.ts<br>• packages/node-runner/src/executors/control-flow/wait.test.ts<br>• apps/web/e2e/nodes/wait.spec.ts<br>• docs/test/node-audit-rows/wait.md<br>• apps/web/src/features/editor/node-params/wait.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；lite 轨验收通过 |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e wait |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/control-flow/wait.test.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test wait`
- 失败摘要（预期内）：3/4 failed — 执行器忽略 `config.ms`（仍用 `amount`/`unit` 默认 1s），fake timers 仅 advance 250ms 导致超时；负值 `ms` 未抛错

### Green

- 实现文件：`packages/node-runner/src/executors/control-flow/wait.ts`、`apps/web/e2e/nodes/wait.spec.ts`、`docs/test/node-audit-rows/wait.md`
- 运行命令：`pnpm --filter @rxwf/node-runner test wait`
- 通过摘要：4/4 passed — 执行器读取 `ms`、负值 E2003、builtin registry 注册、透传 items

### Refactor

- 变更说明：无；E2E 使用 `page.request` 继承认证态（避免裸 `request` fixture Unauthorized）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test wait` | 4 passed / 0 failed | 是 |
| 冒烟（E2E lite） | `pnpm --filter @rxwf/web test:e2e wait` | 4 passed / 0 failed（面板 ms 字段 + debug-node 50ms 延迟透传） | 是 |
| 验收标准核对 | `docs/test/node-audit-rows/wait.md` 审查结论 panel/validation/executor/error_codes 均为 ok；E2E-N-wait spec 存在 | 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/control-flow/wait.ts`
- `packages/node-runner/src/executors/control-flow/wait.test.ts`
- `apps/web/e2e/nodes/wait.spec.ts`
- `docs/test/node-audit-rows/wait.md`
- `docs/workflow/tasks/T-049.md`

## 实现说明

修复面板参数 `ms` 与执行器 `amount`/`unit` 不一致：执行器改为读取 `config.ms`（默认 1000），非法值抛 `E2003`；新增 lite 轨 E2E（属性面板 + debug-node 执行）及审查行 `wait.md`。

## 未解决问题

- `docs/test/e2e-coverage-matrix.md` 与 `node-audit-matrix.md` 行状态由 T-081/T-080 收口更新（本任务不写）
