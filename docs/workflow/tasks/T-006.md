# T-006

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-1 |
| Wave | 1 |
| 描述 | 实现 scripts/e2e-compose.mjs（up/down/wait-health） |
| 追溯 | FR-14 / AC-073 / §6.3 Docker harness |
| 依赖 | - |
| TDD Red 用例 | • e2e-compose healthcheck 超时未就绪应失败<br>• down 后容器仍存在应失败 |
| 建议测试文件 | • scripts/e2e-compose.test.mjs |
| 文件所有权 | • scripts/e2e-compose.mjs<br>• scripts/e2e-compose.test.mjs |
| 验收标准 | standard/plus compose 可启停；Verify passed |
| 基本验证提示 | node scripts/e2e-compose.mjs up standard --dry-run（或集成测） |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`scripts/e2e-compose.test.mjs`
- 运行命令：`node --test scripts/e2e-compose.test.mjs`
- 失败摘要（预期内）：`ERR_MODULE_NOT_FOUND: Cannot find module 'e2e-compose.mjs'`（实现文件尚不存在）

### Green

- 实现文件：`scripts/e2e-compose.mjs`
- 运行命令：`node --test scripts/e2e-compose.test.mjs`
- 通过摘要：8 tests / 5 suites 全部通过；含 healthcheck 超时失败、down 后容器残留失败两条 Red 用例

### Refactor

- 变更说明：抽取 `allServicesHealthy`、`parsePsJson`、`execCompose` 便于单测注入；修复测试 mock `now()` 恒为 0 导致无限循环

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `node --test scripts/e2e-compose.test.mjs` | 8 pass / 0 fail | 是 |
| linter/build | 无针对 scripts/*.mjs 的专用 lint；Node 22 直接执行通过 | N/A | 是 |
| 冒烟（若适用） | `node scripts/e2e-compose.mjs up standard --dry-run`；`node scripts/e2e-compose.mjs up plus --dry-run`；`node scripts/e2e-compose.mjs down standard --dry-run` | 均 exit 0，输出正确 compose 命令（standard/plus 双轨） | 是 |

**结论**：`passed`

## 实际变更文件

- `scripts/e2e-compose.mjs`
- `scripts/e2e-compose.test.mjs`

## 实现说明

实现 `up`/`down`/`wait-health` 三命令，支持 `standard`/`plus` 轨映射至 `deploy/docker-compose.*.yml`，项目名 `rxwf-e2e`；`up` 默认等待 healthcheck，`down` 执行 `down -v` 后校验无残留容器（AC-073）；`--dry-run` 仅打印 docker compose 命令不执行。

## 未解决问题

- 无。真实 `up`/`down` 需设置 `RXWF_POSTGRES_PASSWORD`/`RXWF_REDIS_PASSWORD`，由 T-011 global-setup 或 CI 环境注入。
