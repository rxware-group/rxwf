# T-005

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-1 |
| Wave | 1 |
| 描述 | 实现 scripts/lint-docs-index.mjs 与单元测试（未登记 md 失败） |
| 追溯 | FR-04 / AC-004 / §10.3 CI 校验 |
| 依赖 | - |
| TDD Red 用例 | • 新增未登记 docs/foo.md 时 lint 应 exit 1<br>• 已登记 INDEX 条目应 exit 0 |
| 建议测试文件 | • scripts/lint-docs-index.test.mjs |
| 文件所有权 | • scripts/lint-docs-index.mjs<br>• scripts/lint-docs-index.test.mjs |
| 验收标准 | 脚本本地可重复；P95 < 30s；Verify passed |
| 基本验证提示 | node scripts/lint-docs-index.test.mjs |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`scripts/lint-docs-index.test.mjs`
- 运行命令：`node scripts/lint-docs-index.test.mjs`
- 失败摘要（预期内）：`ERR_MODULE_NOT_FOUND: Cannot find module '.../lint-docs-index.mjs'`

### Green

- 实现文件：`scripts/lint-docs-index.mjs`
- 运行命令：`node scripts/lint-docs-index.test.mjs`
- 通过摘要：2 passed, 0 failed；未登记 `docs/foo.md` exit 1；已登记条目 exit 0

### Refactor

- 变更说明：CLI 默认以 `process.cwd()` 为 repo 根；导出 `lintDocsIndex` / `parseIndexEntryPaths` 供单测直接调用；排除 `docs/INDEX.md` 自身免于登记

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `node scripts/lint-docs-index.test.mjs` | 2 passed, 0 failed | 是 |
| linter/build | `node scripts/lint-docs-index.mjs`（真实仓库） | exit 1，列出 264 个未登记 md（INDEX 尚未全量登记，符合 FR-04 校验行为） | 是 |
| 冒烟（若适用） | 重复运行测试 + 脚本耗时 ~259ms / ~120ms | P95 ≪ 30s；可重复 | 是 |

**结论**：`passed`

## 实际变更文件

- `scripts/lint-docs-index.mjs`
- `scripts/lint-docs-index.test.mjs`

## 实现说明

实现纯 Node 脚本：解析 `docs/INDEX.md` frontmatter `entries[].path`，扫描 `docs/**/*.md`，未登记文件或 orphan INDEX 条目时 exit 1；支持 `--check`、`--docs-dir`、`--root` CLI 参数。

## 未解决问题

- 全仓 docs 尚未在 INDEX 全量登记（依赖 T-002/T-166）；当前对真实仓库运行会 exit 1，属预期。
