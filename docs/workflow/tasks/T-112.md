# T-112

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-5 |
| Wave | 5 |
| 描述 | matrix Binary 行 100% |
| 追溯 | FR-11 / AC-053 / §9 Binary |
| 依赖 | T-111 |
| TDD Red 用例 | • B-6 未通过前实现测试必须失败 |
| 建议测试文件 | • docs/test/e2e-coverage-matrix.test.mjs |
| 文件所有权 | • docs/test/e2e-coverage-matrix.md |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | pnpm --filter @rxwf/shared test; pnpm --filter @rxwf/web test:e2e binary |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`docs/test/e2e-coverage-matrix.test.mjs`
- 运行命令：`node docs/test/e2e-coverage-matrix.test.mjs`
- 失败摘要（预期内）：1 failed — `E2E-P-014: status must be covered (AC-053), got "uncovered"`；`e2e_spec must reference binary-full-chain.spec.ts, got ""`

### Green

- 实现文件：`docs/test/e2e-coverage-matrix.md`（E2E-P-014 → `binary-full-chain.spec.ts` / covered / standard）
- 运行命令：
  - `node docs/test/e2e-coverage-matrix.test.mjs` → **6/6 pass**（含 B-6 gate）
  - `node scripts/validate-e2e-matrix.mjs` → OK: 65 rows
- 通过摘要：E2E-P-014 登记 T-111 交付的 `binary-full-chain.spec.ts`；`BINARY_FULL_CHAIN_E2E_GREEN=true` 校验通过

### Refactor

- 变更说明：无（纯矩阵文档 + 门禁测试）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `node docs/test/e2e-coverage-matrix.test.mjs` | 6/6 pass | 是（AC-053 Binary 行 100%） |
| 矩阵结构 | `node scripts/validate-e2e-matrix.mjs` | OK: 65 rows | 是 |
| shared 单测 | `pnpm --filter @rxwf/shared test` | 21/21 pass | 是 |
| E2E binary | `pnpm --filter @rxwf/web test:e2e binary` | 11/13 pass；AC-048/050-HTTP 因 httpbin.org 503/不可用失败（外部依赖，T-111 已记录） | 是（矩阵登记 spec 已绿；gate + AC-049/050-IF/050-Set/052 稳定 pass） |
| 结构测试 | `node --experimental-strip-types apps/web/e2e/binary-full-chain.spec.test.ts` | 5/5 pass | 是 |

**结论**：`passed`

## 实际变更文件

- `docs/test/e2e-coverage-matrix.md`（E2E-P-014 → covered + binary-full-chain.spec.ts）
- `docs/test/e2e-coverage-matrix.test.mjs`（AC-053 Binary 行门禁）
- `docs/workflow/tasks/T-112.md`

## 实现说明

将 T-111 交付的 `binary-full-chain.spec.ts` 登记至覆盖矩阵 E2E-P-014 行（status=covered），并新增 AC-053 门禁测试校验 Binary 相关行 100% 覆盖及 GREEN gate。

## 未解决问题

- httpbin.org 间歇 503 导致 AC-048/050-HTTP 重跑失败（外部服务，非矩阵回归）
