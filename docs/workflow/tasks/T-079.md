# T-079

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `toolWebSearch`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner toolWebSearch |
| 依赖 | T-034 |
| TDD Red 用例 | • `toolWebSearch` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E toolWebSearch spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/agent-satellite-tools.test.ts（或新建）<br>• apps/web/e2e/nodes/toolWebSearch.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/agent-satellite-tools.ts<br>• packages/node-runner/src/executors/agent-satellite-tools.test.ts<br>• apps/web/e2e/nodes/toolWebSearch.spec.ts<br>• docs/test/node-audit-rows/toolWebSearch.md<br>• apps/web/src/features/editor/node-params/toolWebSearch.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e toolWebSearch |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/agent-satellite-tools.test.ts`（toolWebSearch satellite/registry/audit-row + invokeAgentTool E1071/embedded/agent）、`apps/web/e2e/nodes/toolWebSearch.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner exec vitest run agent-satellite-tools.test.ts -t "toolWebSearch"`（audit row 用例前 `toolWebSearchAuditRowPath is not defined` / `toolWebSearch.md` 不存在）；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e toolWebSearch`（spec 创建前无匹配）
- 失败摘要（预期内）：单测 `toolWebSearch M-3 audit row` 因 audit row 文件缺失失败；E2E spec 不存在时 playwright 无匹配

### Green

- 实现文件：审查确认 M-2 `agent-satellite-tools.ts` 已注册 `web_search` 双路径；补充 `toolWebSearch.md`、`toolWebSearch.spec.ts`、单测
- 运行命令：`pnpm --filter @rxwf/node-runner exec vitest run agent-satellite-tools.test.ts -t "toolWebSearch"`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9971 RXWF_E2E_WEB_PORT=9411 pnpm --filter @rxwf/web test:e2e toolWebSearch --retries=0`
- 通过摘要：5/5 toolWebSearch 单测；3/3 lite E2E（audit row + 面板 inheritConfig/凭证/Provider + 内置 Tool 描述）

### Refactor

- 变更说明：无执行器逻辑变更；仅补充 audit 文档、E2E 与单测；未新增 `toolWebSearch.tsx`（通用 schema + `BuiltinSatelliteToolDescription` 已覆盖）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner exec vitest run agent-satellite-tools.test.ts -t "toolWebSearch"` | 5 passed（2026-06-20 复验） | 是 |
| invoke 路径 | `pnpm --filter @rxwf/node-runner exec vitest run agent-satellite-tools.test.ts -t "toolWebSearch\|web_search\|E1071 when embedded webSearch\|delegates web_search"` | 8 passed（2026-06-20 复验） | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9971 RXWF_E2E_WEB_PORT=9411 pnpm --filter @rxwf/web test:e2e toolWebSearch --retries=0` | 3 passed（2026-06-20 复验） | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus RXWF_E2E_API_PORT=9977 RXWF_E2E_WEB_PORT=9417 pnpm --filter @rxwf/web test:e2e toolWebSearch --retries=0` | global-setup 失败：`docker-compose.plus.yml` api 无 image/build（2026-06-20 复验） | 否（本机 compose 配置） |
| 验收标准核对 | `docs/test/node-audit-rows/toolWebSearch.md` 四维 ok + executor satellite + status ok；E1071–E1074 文档化；单测 E2003/E1071/embedded/agent 路径 | 代码与 audit row 就绪；`@plus` E2003/aiAgent 接线用例待 CI compose 修复后执行 | 部分 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/agent-satellite-tools.test.ts`
- `docs/test/node-audit-rows/toolWebSearch.md`
- `apps/web/e2e/nodes/toolWebSearch.spec.ts`
- `docs/workflow/tasks/T-079.md`

## 实现说明

审查结论：面板（`node-param-schemas` inheritConfig/credentialMode/provider/credentialId + `BuiltinSatelliteToolDescription`）、校验（fixed-capability 内置描述；运行时 Provider/query 校验 E1071）、执行器（卫星 `buildAgentToolDefinitions` + `invokeAgentTool` Embedded/Agent 双路径，M-2 T-018 provider 已补齐）、错误码 E1071–E1074 均已就绪。补充 registry E2003、invoke E1071/embedded/agent 单测、E2E spec 与 audit row。

## 未解决问题

- 本机 `deploy/docker-compose.plus.yml` 报 `service "api" has neither an image nor a build context`，plus 轨 E2E global-setup 未能启动；CI 修复 compose 后应跑通 `@plus`（E2003 直接 debug-node + aiAgent 卫星接线）
- `docs/test/e2e-coverage-matrix.md` E2E-N-toolWebSearch 行由 T-080 矩阵合并任务更新为 covered
