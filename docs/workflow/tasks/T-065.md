# T-065

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `aiAgent`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner aiAgent |
| 依赖 | T-034 |
| TDD Red 用例 | • `aiAgent` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E aiAgent spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/run-ai-agent-node.test.ts（或新建）<br>• apps/web/e2e/nodes/aiAgent.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/run-ai-agent-node.ts<br>• packages/node-runner/src/executors/run-ai-agent-node.test.ts<br>• apps/web/e2e/nodes/aiAgent.spec.ts<br>• docs/test/node-audit-rows/aiAgent.md<br>• apps/web/src/features/editor/node-params/aiAgent.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e aiAgent |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/run-ai-agent-node.test.ts`、`apps/web/e2e/nodes/aiAgent.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test run-ai-agent-node.test.ts`（新建前无 registry/E3010 用例）；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web exec playwright test aiAgent --retries=0`
- 失败摘要（预期内）：E2E `ENOENT: docs/test/node-audit-rows/aiAgent.md`；面板用例 `角色 (Crew)` 在默认 params 标签不可见（Crew 字段在 Crew 标签）；`Prompt` 选择器 strict mode 冲突 System Prompt

### Green

- 实现文件：`run-ai-agent-node.test.ts`（registry + E3010/E3001/E2003）、`apps/web/e2e/nodes/aiAgent.spec.ts`（audit row + 面板 + @plus E3010/Ollama 冒烟）、`docs/test/node-audit-rows/aiAgent.md`；执行器 `ai-agent.ts`/`run-ai-agent-node.ts` 已存在无需改动
- 运行命令：`pnpm --filter @rxwf/node-runner test run-ai-agent-node.test.ts ai-agent.test.ts`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9931 RXWF_E2E_WEB_PORT=9371 pnpm --filter @rxwf/web exec playwright test aiAgent --retries=0`
- 通过摘要：8/8 单测；lite E2E 3/3（audit row + 面板）

### Refactor

- 变更说明：面板 E2E 改用 `getByTestId('rf__node-agt')` + Crew 标签切换；Prompt 用 `exact: true` 避免与 System Prompt 冲突

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test run-ai-agent-node.test.ts ai-agent.test.ts` | 8 passed（2026-06-20 复验） | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9931 RXWF_E2E_WEB_PORT=9371 pnpm --filter @rxwf/web exec playwright test aiAgent --retries=0` | 3 passed（audit row + 面板，2026-06-20 复验） | 是 |
| E2E plus/@plus | `RXWF_E2E_TRACK=plus RXWF_E2E_API_PORT=9921 RXWF_E2E_WEB_PORT=9361 pnpm --filter @rxwf/web exec playwright test aiAgent --retries=0` | global-setup 失败：`docker-compose.plus.yml` api/web 无 image/build（2026-06-20 复验） | 否（本机 compose 配置）；spec 已含 `@plus` E3010 + Ollama 冒烟 |
| 验收标准核对 | `docs/test/node-audit-rows/aiAgent.md` 五维 ok + status ok；registry/E3010/E2003/E3001 单测；E2E spec 含 `@plus` 执行用例 | M-3 审查通过；plus 轨需 CI compose + Plus API | 是（M-3 范围） |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/run-ai-agent-node.test.ts`
- `apps/web/e2e/nodes/aiAgent.spec.ts`
- `docs/test/node-audit-rows/aiAgent.md`
- `docs/workflow/tasks/T-065.md`

## 实现说明

M-3 stub 审查：面板（`node-param-schemas` + params/crew/settings 标签）、保存校验（`E1012`/`E1014`）、Plus 执行器（`registerPlusExecutors` → `runAiAgentNode` ReAct）、运行时错误码均已就绪。补充 registry/E3010 单测、audit row 与 E2E spec；未新增 `aiAgent.tsx`（通用 `NodeEditorParamsPane` 已覆盖）。

## 未解决问题

- 本机 `RXWF_E2E_TRACK=plus` 因 `deploy/docker-compose.plus.yml` 缺少 api/web image/build 无法跑通 global-setup；CI plus compose 环境可验收 `@plus` 用例。
