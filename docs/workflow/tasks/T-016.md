# T-016

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 1 |
| 描述 | skillRun toolWrite provider 补齐至 spec AC |
| 追溯 | FR-05 / AC-013 / M-2 skill-runtime |
| 依赖 | - |
| TDD Red 用例 | • write 工具未注册时 skillRun 应 E104x 失败<br>• 写入文件后应可读回内容 |
| 建议测试文件 | • packages/skill-runtime/src/tools/write.test.ts<br>• packages/node-runner/src/executors/skill-run.test.ts |
| 文件所有权 | • packages/skill-runtime/src/tools/write.ts<br>• packages/skill-runtime/src/tools/write.test.ts |
| 验收标准 | write 子能力 green + Verify |
| 基本验证提示 | pnpm --filter @rxwf/skill-runtime test |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/skill-runtime/src/tools/write.test.ts`
- 运行命令：`pnpm --filter @rxwf/skill-runtime test write`
- 失败摘要（预期内）：`Cannot find module './write.js'` — 实现文件尚未创建

### Green

- 实现文件：`packages/skill-runtime/src/tools/write.ts`
- 运行命令：`pnpm --filter @rxwf/skill-runtime test write`
- 通过摘要：5 passed — 未注册 E1041、写入可读回、append、toolInvoke 委托与失败传播

### Refactor

- 变更说明：无（首版实现已满足测试）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/skill-runtime test write` | 5 passed / 0 failed | 是 |
| 全包回归 | `pnpm --filter @rxwf/skill-runtime test` | 42 passed / 0 failed | 是 |
| linter/build | `pnpm --filter @rxwf/skill-runtime build` | tsc 成功 | 是 |
| 冒烟（若适用） | N/A（provider 模块，T-026 集成 skill-run） | 跳过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/skill-runtime/src/tools/write.ts`
- `packages/skill-runtime/src/tools/write.test.ts`
- `docs/workflow/tasks/T-016.md`

## 实现说明

新增 `dispatchWrite` provider：未接线 `toolWrite`（`writeToolRegistered: false`）时抛 E1041；支持本地 embedded 写入（scanRoots 校验）或委托 `ToolInvokeClient` 远程 filesystem write。

## 未解决问题

- skill-run 端到端集成（write 卫星调用链）属 T-026，本 task 仅交付 skill-runtime provider 层
