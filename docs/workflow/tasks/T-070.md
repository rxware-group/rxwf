# T-070

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `toolMcp`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner toolMcp |
| 依赖 | T-034 |
| TDD Red 用例 | • `toolMcp` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E toolMcp spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/agent-satellite-tools.test.ts（或新建）<br>• apps/web/e2e/nodes/toolMcp.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/agent-satellite-tools.ts<br>• packages/node-runner/src/executors/agent-satellite-tools.test.ts<br>• apps/web/e2e/nodes/toolMcp.spec.ts<br>• docs/test/node-audit-rows/toolMcp.md<br>• apps/web/src/features/editor/node-params/toolMcp.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e toolMcp |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/agent-satellite-tools.test.ts`、`apps/web/e2e/nodes/toolMcp.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner exec vitest run agent-satellite-tools.test.ts -t "toolMcp"`（audit row / registry 用例创建前）；`RXWF_E2E_TRACK=lite ... playwright test toolMcp`（spec 创建前 ENOENT audit row；面板用例创建后 strict mode 失败）
- 失败摘要（预期内）：audit row 文件缺失；registry/audit 用例缺失；E2E `Tool` label strict mode 与 `Tool 描述` 冲突

### Green

- 实现文件：`agent-satellite-tools.test.ts`（补 registry/E2003、buildAgentToolDefinitions mcp 路径、audit row、mcp invoke 路由）、`docs/test/node-audit-rows/toolMcp.md`、`apps/web/e2e/nodes/toolMcp.spec.ts`；`agent-satellite-tools.ts` 已有 toolMcp 实现无需改动
- 运行命令：`pnpm --filter @rxwf/node-runner exec vitest run agent-satellite-tools.test.ts -t "toolMcp"`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9930 RXWF_E2E_WEB_PORT=9370 pnpm --filter @rxwf/web exec playwright test toolMcp --retries=0`
- 通过摘要：7/7 toolMcp 单测；lite E2E 3/3（audit row + 面板）

### Refactor

- 变更说明：E2E 面板用 `hasText: /^Tool$/` 避免与「Tool 描述」标签冲突；未新增 `toolMcp.tsx`（`McpClientFields` + schema 已覆盖）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner exec vitest run agent-satellite-tools.test.ts -t "toolMcp"` | 7 passed（2026-06-20 复验） | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9930 RXWF_E2E_WEB_PORT=9370 pnpm --filter @rxwf/web exec playwright test toolMcp --retries=0` | 3 passed（2026-06-20 复验） | 是 |
| E2E plus/@plus | `RXWF_E2E_TRACK=plus RXWF_E2E_API_PORT=9941 RXWF_E2E_WEB_PORT=9381 pnpm --filter @rxwf/web exec playwright test toolMcp --retries=0` | global-setup 失败：`docker-compose.plus.yml` api 无 image/build（2026-06-20 复验） | 否（本机 compose 配置）；spec 已含 `@plus` E2003 + validate 用例 |
| 验收标准核对 | `docs/test/node-audit-rows/toolMcp.md` 五维 ok + status ok；E2003/E3012 单测；E2E spec 含 `@plus` 执行用例 | M-3 stub 审查通过；plus 轨需 CI compose + Plus API | 是（M-3 范围） |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/agent-satellite-tools.test.ts`
- `docs/test/node-audit-rows/toolMcp.md`
- `apps/web/e2e/nodes/toolMcp.spec.ts`
- `docs/workflow/tasks/T-070.md`

## 实现说明

M-3 卫星审查：`toolMcp` 无独立 executor，经 `buildAgentToolDefinitions` 注册 `source.type: mcp`，`run-ai-agent-node.ts` 经 `callMcpTool` 执行；面板与 `mcpClient` 共用 `McpClientFields` + `toolDescription`。补充 registry/E2003、MCP tool 构建、audit row 与 E2E spec。

## 未解决问题

- 本机 plus compose `docker-compose.plus.yml` 报 api 无 build context，`@plus` E2E 未能启动（CI 应正常）
