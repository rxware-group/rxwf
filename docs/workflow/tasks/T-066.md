# T-066

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `aiChatModel`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner aiChatModel |
| 依赖 | T-034 |
| TDD Red 用例 | • `aiChatModel` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E aiChatModel spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/agent-satellite-tools.test.ts（或新建）<br>• apps/web/e2e/nodes/aiChatModel.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/agent-satellite-tools.ts<br>• packages/node-runner/src/executors/agent-satellite-tools.test.ts<br>• apps/web/e2e/nodes/aiChatModel.spec.ts<br>• docs/test/node-audit-rows/aiChatModel.md<br>• apps/web/src/features/editor/node-params/aiChatModel.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e aiChatModel |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/agent-satellite-tools.test.ts`、`apps/web/e2e/nodes/aiChatModel.spec.ts`
- 运行命令：`pnpm exec vitest run agent-satellite-tools.test.ts -t "aiChatModel|modelFromChatModelNode"`；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web exec playwright test aiChatModel`（audit row / spec 创建前）
- 失败摘要（预期内）：`modelFromChatModelNode is not a function`；`existsSync(aiChatModelAuditRowPath)` false；E2E `ENOENT: docs/test/node-audit-rows/aiChatModel.md`

### Green

- 实现文件：`agent-satellite-tools.ts`（导出 `modelFromChatModelNode`）、`agent-satellite-tools.test.ts`（卫星 registry + ModelRef 单测 + audit row 断言）、`apps/web/e2e/nodes/aiChatModel.spec.ts`、`docs/test/node-audit-rows/aiChatModel.md`
- 运行命令：`pnpm exec vitest run agent-satellite-tools.test.ts -t "aiChatModel|modelFromChatModelNode"`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9930 RXWF_E2E_WEB_PORT=9370 pnpm --filter @rxwf/web exec playwright test aiChatModel --retries=0`
- 通过摘要：5/5 单测；lite E2E 6/6（含 audit row + 面板 + W1010/E1043/E1012 validate）

### Refactor

- 变更说明：E2E validate 用例改为先创建合法 shell 工作流再 POST validate（避免 createWorkflow 400 拦截）；未新增 `aiChatModel.tsx`（通用面板已覆盖）
- 二次审查：`uniqueWorkflowName` 防重名；validate 用例加 `{ page, request }` 保持 auth 上下文（避免 request-only 401/ENOENT）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm exec vitest run agent-satellite-tools.test.ts -t "aiChatModel|modelFromChatModelNode"` | 5 passed | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=19879 RXWF_E2E_WEB_PORT=19324 pnpm --filter @rxwf/web exec playwright test aiChatModel --retries=0` | 6 passed（setup+5 chromium） | 是 |
| E2E plus/@plus | `RXWF_E2E_TRACK=plus RXWF_E2E_API_PORT=19880 RXWF_E2E_WEB_PORT=19325 pnpm --filter @rxwf/web exec playwright test aiChatModel --retries=0` | global-setup 失败：`docker-compose.plus.yml` api/web 无 image/build | 否（本机 compose）；spec 已含 `@plus` Ollama 冒烟 |
| 验收标准核对 | `docs/test/node-audit-rows/aiChatModel.md` 五维 ok + executor satellite + status ok；E2003/E1043/E1012 单测与 E2E | M-3 stub 审查通过 | 是（M-3 范围） |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/agent-satellite-tools.ts`
- `packages/node-runner/src/executors/agent-satellite-tools.test.ts`
- `apps/web/e2e/nodes/aiChatModel.spec.ts`
- `docs/test/node-audit-rows/aiChatModel.md`
- `docs/workflow/tasks/T-066.md`

## 实现说明

M-3 stub 审查：`aiChatModel` 为 Agent 卫星（无 standalone executor）；面板经 `node-param-schemas` + `CredentialSelect` 已就绪；校验 E1012/E1014/E1043/W1010 已有。新增 `modelFromChatModelNode` 集中 ModelRef 解析、registry E2003 单测、audit row 与 E2E spec（面板/validate/plus 冒烟）。

## 未解决问题

- 本机 plus compose `docker-compose.plus.yml` 报 api/web 无 build context，plus 轨 E2E 未能启动（CI 应正常）
- `@plus` aiAgent+Model 冒烟依赖 Ollama 可达，不可达时 skip
