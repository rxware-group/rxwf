# T-002

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-1 |
| Wave | 1 |
| 描述 | 创建 docs/INDEX.md：YAML frontmatter + 人类可读总目录骨架 |
| 追溯 | FR-01 / AC-001 / §10.2 INDEX 规范 |
| 依赖 | - |
| TDD Red 用例 | • INDEX 解析测试：缺少 frontmatter.version 应失败<br>• entries 数组为空应失败 |
| 建议测试文件 | • scripts/lint-docs-index.test.mjs |
| 文件所有权 | • docs/INDEX.md |
| 验收标准 | INDEX.md 存在且 frontmatter 合法；Verify passed |
| 基本验证提示 | node scripts/lint-docs-index.mjs --check docs/INDEX.md |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：内联 Node 校验（文件所有权仅 `docs/INDEX.md`，T-005 负责 `scripts/lint-docs-index.test.mjs`）
- 运行命令：`node --input-type=module -e "…validateIndex…"`（临时 fixture：缺 version、空 entries；INDEX.md 尚未创建）
- 失败摘要（预期内）：
  - `missing frontmatter.version` — PASS
  - `entries array empty` — PASS
  - `docs/INDEX.md not yet created (Red expected)` — PASS

### Green

- 实现文件：`docs/INDEX.md`
- 运行命令：同上内联校验 + `node --input-type=module -e "import { parseIndexFile } from './scripts/lint-docs-index.mjs'…"`
- 通过摘要：
  - `version=1`, `entries=24`
  - 人类可读目录含 `# RX-Workflow 文档总索引` 及 9 个分类节（规格/需求/架构/工作流/契约/ADR/帮助/节点/测试）

### Refactor

- 变更说明：无（骨架初版结构清晰，未做额外重构）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | 内联 validateIndex + `parseIndexFile('docs/INDEX.md')` | version=1, entries=24, 0 orphan | 是 |
| T-002 Red 用例 | 临时 fixture 缺 version / 空 entries | 均 throw 预期错误 | 是 |
| linter/build | `node scripts/lint-docs-index.test.mjs` | 2 passed, 0 failed | 是 |
| 冒烟（若适用） | `node scripts/lint-docs-index.mjs --check docs/INDEX.md` | exit 1（265 未登记 — 骨架预期，全量登记属 T-166） | 是（AC-001 仅要求 frontmatter 合法 + 人类目录，非全量登记） |

**结论**：`passed`

## 实际变更文件

- `docs/INDEX.md`（新增）

## 实现说明

创建 `docs/INDEX.md`：YAML frontmatter（version/categories/entries 24 条核心 M-1 骨架）+ 人类可读分类总目录（含 FR/nodeType 占位节，完整交叉引用留 T-010）。

## 未解决问题

- 全 repo `lint-docs-index` 仍有 265 篇未登记 md（T-166 全量登记）；当前 skeleton 无 orphan entries。
