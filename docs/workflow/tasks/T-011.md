# T-011

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-1 |
| Wave | 3 |
| 描述 | 扩展 apps/web/e2e/global-setup.ts：compose 生命周期 + auth |
| 追溯 | FR-14 / AC-073 / §6.4 global-setup |
| 依赖 | T-006 |
| TDD Red 用例 | • RXWF_E2E_TRACK=standard 时未调用 compose up 应失败（mock 测试） |
| 建议测试文件 | • apps/web/e2e/global-setup.test.ts |
| 文件所有权 | • apps/web/e2e/global-setup.ts<br>• apps/web/e2e/global-setup.test.ts |
| 验收标准 | setup 按 track 启 compose 并写 .e2e-env |
| 基本验证提示 | RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e --grep @smoke |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/web/e2e/global-setup.test.ts`
- 运行命令：`cd apps/api && node --import tsx/esm --test ../web/e2e/global-setup.test.ts`
- 失败摘要（预期内）：`SyntaxError: The requested module './global-setup.ts' does not provide an export named 'buildE2eEnvLines'`（实现尚未导出 compose 生命周期）

### Green

- 实现文件：`apps/web/e2e/global-setup.ts`
- 运行命令：`cd apps/api && node --import tsx/esm --test ../web/e2e/global-setup.test.ts`
- 通过摘要：7 tests / 3 suites 全部通过；含 standard 轨必须调用 `cmdUp`、lite 跳过 compose、写入 `.e2e-env`、清理 `.auth`

### Refactor

- 变更说明：抽取 `resolveE2eTrack`、`ensureComposeEnv`、`buildE2eEnvLines`、`formatE2eEnv`、`runGlobalSetup` 便于 mock 注入；默认 E2E 密码 `e2e-test-pass` 供 compose 启停

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `cd apps/api && node --import tsx/esm --test ../web/e2e/global-setup.test.ts` | 7 pass / 0 fail | 是 |
| linter/build | `pnpm --filter @rxwf/web typecheck` | 既有 src/ 历史 TS 错误；本任务 e2e 文件不在 tsconfig include，无新增 lint | 是 |
| 冒烟（若适用） | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e --grep @smoke` | `@smoke` 用例尚未入库（No tests found）；替代：`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e workflow-manual-node --project lite-chromium` → 2 passed，`.e2e-env` 已写入 | 是 |

**结论**：`passed`

## 实际变更文件

- `apps/web/e2e/global-setup.ts`
- `apps/web/e2e/global-setup.test.ts`

## 实现说明

`runGlobalSetup` 按 `RXWF_E2E_TRACK` 分支：`standard`/`plus` 调用 `scripts/e2e-compose.mjs` 的 `cmdUp` 并注入默认 compose 密码；`lite` 跳过 compose；各轨均清理 `.auth` 并写入 `e2e/.e2e-env`（含 track、端口及 standard/plus 的 DB/Redis URL）。

## 未解决问题

- 仓库尚无 `@smoke` 标记用例，dev-leader 提示的 grep 冒烟命令暂无法直接执行；待后续 E2E 任务补充 tag 后可复验。
