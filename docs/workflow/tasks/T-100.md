# T-100

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-5 |
| Wave | 1 |
| 描述 | n8n Binary 对标审查文档 |
| 追溯 | FR-11 / AC-045 / §9 Binary |
| 依赖 | T-099 |
| TDD Red 用例 | • B-6 未通过前实现测试必须失败 |
| 建议测试文件 | • docs/architecture/binary-n8n-review.test.mjs |
| 文件所有权 | • docs/architecture/binary-n8n-review.md |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | pnpm --filter @rxwf/shared test; pnpm --filter @rxwf/web test:e2e binary |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`docs/architecture/binary-n8n-review.test.mjs`
- 运行命令：`node docs/architecture/binary-n8n-review.test.mjs`
- 失败摘要（预期内）：3 failed — `binary-n8n-review.md` 缺失，`b2N8nReviewGate=blocked`；断言「未完成 n8n 对标审查应阻塞 B-2 门禁」

### Green

- 实现文件：`docs/architecture/binary-n8n-review.md`；测试修正 N8N-06 表格单元格内 `|` 解析问题
- 运行命令：`node docs/architecture/binary-n8n-review.test.mjs`
- 通过摘要：3 passed — frontmatter `b2N8nReviewGate: cleared`、`b6ImplementationGate: blocked`；N8N-01～N8N-06 对标对照表齐全

### Refactor

- 变更说明：导出 `validateBinaryN8nReview` 供后续 CI 门禁复用；`parseFrontmatter` 兼容 Windows CRLF

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `node docs/architecture/binary-n8n-review.test.mjs` | 3 passed | 是（AC-045 B-2 gate cleared） |
| 验收标准核对 | 对照 §3 六项对标域 + §5 B-2 门禁 | n8n Item/HTTP/Webhook/表达式/存储五维已文档化；`b6ImplementationGate` 仍为 blocked | 是 |
| 受影响模块测试 | `pnpm --filter @rxwf/shared test` | 6 passed | 是 |
| E2E 冒烟 | `pnpm --filter @rxwf/web test:e2e binary` | 跳过（M-5 E2E 属 T-113+；本 task 为 B-2 文档门禁） | 是（AC-045 不要求 E2E） |

**结论**：`passed`

## 实际变更文件

- `docs/architecture/binary-n8n-review.md`（新建）
- `docs/architecture/binary-n8n-review.test.mjs`（新建）
- `docs/workflow/tasks/T-100.md`

## 实现说明

产出 AC-045 / B-2 n8n Binary 对标审查：对照 n8n `IBinaryData`、HTTP Request File/Binary 响应、multipart upload、Webhook multipart、表达式/`getBinaryDataBuffer`、filesystem blob 外置，记录 rxwf 六项差距（2 项部分对齐、2 项缺失、1 项表达式部分对齐、1 项待 B-6 确认）；`b2N8nReviewGate: cleared`，实现仍 `b6ImplementationGate: blocked`。

## 未解决问题

- T-099 `binary-current-state.md` 尚未合入时，§1 以 `binary-type-support-analysis.md` 为快照来源；T-099 完成后须交叉核对 §3 对照表 rxwf 现状列。
