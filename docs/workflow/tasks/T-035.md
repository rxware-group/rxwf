# T-035

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 2 |
| 描述 | 审查并修复 nodeType `manualTrigger`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-028, AC-030 / node-runner manualTrigger |
| 依赖 | T-034 |
| TDD Red 用例 | • `manualTrigger` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E manualTrigger spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/triggers/manual.test.ts（或新建）<br>• apps/web/e2e/nodes/manualTrigger.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/triggers/manual.ts<br>• packages/node-runner/src/executors/triggers/manual.test.ts<br>• apps/web/e2e/nodes/manualTrigger.spec.ts<br>• docs/test/node-audit-rows/manualTrigger.md<br>• apps/web/src/features/editor/node-params/manualTrigger.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；lite 轨验收通过 |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e manualTrigger |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/triggers/manual.test.ts`、`apps/web/e2e/nodes/manualTrigger.spec.ts`
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test src/executors/triggers/manual.test.ts` → `throws E1002 for invalid JSON string` 失败（旧实现返回 `_parseError` 而非抛错）
  - `pnpm exec playwright test e2e/nodes/manualTrigger.spec.ts` → audit row ENOENT；面板/执行用例待 Green
- 失败摘要（预期内）：无效 JSON 未抛 E1002；审查行文件缺失；E2E spec 不存在

### Green

- 实现文件：`manual.ts`（无效 JSON 改抛 `AwfError E1002`）、`manual.test.ts`（registry + E1002）、`manualTrigger.spec.ts`、`docs/test/node-audit-rows/manualTrigger.md`
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test src/executors/triggers/manual.test.ts` → 7/7 passed
  - `pnpm --filter @rxwf/web test:e2e manualTrigger` → 5/5 passed
- 通过摘要：执行器注册 E2003/E1002 单测通过；E2E 覆盖面板 JSON 字段与 debug-node 单/多 item 输出

### Refactor

- 变更说明：E2E `createWorkflow` 使用时间戳唯一名称避免重复创建失败；面板选择器对齐 `.rxwf-param-codemirror`

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test src/executors/triggers/manual.test.ts` | 7 passed | 是 |
| 冒烟（lite E2E） | `pnpm --filter @rxwf/web test:e2e manualTrigger` | 5 passed | 是 |
| 验收标准核对 | audit row `status=ok`；panel/validation/executor 结论；E2E-N-manualTrigger 可执行 | 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/triggers/manual.ts`
- `packages/node-runner/src/executors/triggers/manual.test.ts`
- `apps/web/e2e/nodes/manualTrigger.spec.ts`
- `docs/test/node-audit-rows/manualTrigger.md`
- `docs/workflow/tasks/T-035.md`

## 实现说明

审查结论：面板（`node-param-schemas` JSON 字段 + `JsonParamEditor`）、保存校验（`editor-json-params`）均已就绪；执行器已在 `register-builtin` 注册。修复点：运行时无效 JSON 字符串由静默 `_parseError` 改为抛出 `E1002`，与 json 节点及编辑器校验一致。新增 lite 轨 E2E 与 `AUDIT-N-manualTrigger` 审查行。

## 未解决问题

- `node-audit-matrix.md` 主表行仍 pending（由 T-080 合并 audit rows 时更新）
- `e2e-coverage-matrix.md` E2E-N-manualTrigger 行仍 uncovered（由 T-081 批量更新）
