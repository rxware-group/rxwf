# T-040

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 2 |
| 描述 | 审查并修复 nodeType `if`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-028, AC-030 / node-runner if |
| 依赖 | T-034 |
| TDD Red 用例 | • `if` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E if spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/control-flow/if.test.ts（或新建）<br>• apps/web/e2e/nodes/if.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/control-flow/if.ts<br>• packages/node-runner/src/executors/control-flow/if.test.ts<br>• apps/web/e2e/nodes/if.spec.ts<br>• docs/test/node-audit-rows/if.md<br>• apps/web/src/features/editor/node-params/if.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；lite 轨验收通过 |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e if |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`if.test.ts`（registry + 空条件 E2003）、`if.test.tsx`（面板/校验）、`if.spec.ts`（E2E）
- 运行命令：`pnpm --filter @rxwf/node-runner test if.test`；`pnpm vitest run src/features/editor/node-params/if.test.tsx`
- 失败摘要（预期内）：空 `condition` 执行返回 `success` 而非 `failed`；`if.test.tsx` 无法 resolve `./if.js`

### Green

- 实现文件：`if.ts`（执行器空条件 E2003）、`if.tsx`（`IfConditionPanel` + `validateIfParameters`）、`if.md`（审查结论）
- 运行命令：同上 + `pnpm --filter @rxwf/web test:e2e if`（ports 9879/9324）
- 通过摘要：node-runner 10/10；web panel 5/5；E2E 4/4（lite）

### Refactor

- 变更说明：`if.tsx` 校验使用本地 `{{ }}` 检测，避免 web 包直接依赖 `@rxwf/expression`

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test if.test` | 10 passed | 是 |
| 任务范围测试 | `pnpm vitest run src/features/editor/node-params/if.test.tsx`（apps/web） | 5 passed | 是 |
| 冒烟（lite E2E） | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e if` | 4 passed | 是 |
| 验收标准核对 | `docs/test/node-audit-rows/if.md` 四维 + e2e 结论 ok；lite 轨 E2E 通过 | 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/control-flow/if.ts`
- `packages/node-runner/src/executors/control-flow/if.test.ts`
- `apps/web/src/features/editor/node-params/if.tsx`
- `apps/web/src/features/editor/node-params/if.test.tsx`
- `apps/web/e2e/nodes/if.spec.ts`
- `docs/test/node-audit-rows/if.md`
- `docs/workflow/tasks/T-040.md`

## 实现说明

审查 IF 节点四维：执行器补充空 `condition` 时 `E2003` 失败与 registry 单测；新增 `validateIfParameters`/`IfConditionPanel` 覆盖面板与保存校验规则；E2E 覆盖编辑器条件字段、true/false 分流与空条件 debug 失败。

## 未解决问题

- `validateIfParameters` 尚未接入 `validateWorkflowDefinition`（与 switch 相同，超出本 task 所有权）
- `IfConditionPanel` 未替换 `NodeEditorParamsPane` 通用 schema 渲染（E2E 已覆盖现有 `.if-condition-form-field` 路径）
