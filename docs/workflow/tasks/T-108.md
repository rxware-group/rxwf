# T-108

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-5 |
| Wave | 3 |
| 描述 | Webhook multipart 上传 → binary |
| 追溯 | FR-11 / AC-049 / §9 Binary |
| 依赖 | T-104, T-105 |
| TDD Red 用例 | • B-6 未通过前实现测试必须失败 |
| 建议测试文件 | • packages/node-runner/src/executors/triggers/webhook-binary.test.ts |
| 文件所有权 | • packages/node-runner/src/executors/triggers/webhook-binary.ts<br>• apps/api/src/routes/webhook-binary.test.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | pnpm --filter @rxwf/shared test; pnpm --filter @rxwf/web test:e2e binary |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/triggers/webhook-binary.test.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/triggers/webhook-binary.test.ts`
- 失败摘要（预期内）：`Cannot find module './webhook-binary.js'`

### Green

- 实现文件：`packages/node-runner/src/executors/triggers/webhook-binary.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/triggers/webhook-binary.test.ts`；`pnpm --filter @rxwf/api test -- src/routes/webhook-binary.test.ts`
- 通过摘要：node-runner 4/4 pass；api 2/2 pass（BIN-09 与 node-runner 解析一致 + ingress 执行透传 binary）

### Refactor

- 变更说明：无；CONF-05 form field 名→binary key 逻辑与既有 `parse-webhook-body.ts` 对齐

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test -- src/executors/triggers/webhook-binary.test.ts src/executors/triggers/webhook.test.ts` | 8/8 pass | 是（AC-049 multipart→binary） |
| 任务范围测试 | `pnpm --filter @rxwf/api test -- src/routes/webhook-binary.test.ts` | 2/2 pass | 是（BIN-09 verify + 执行链路） |
| 受影响模块测试 | `pnpm --filter @rxwf/shared test` | 21/21 pass | 是 |
| linter/build | `pnpm --filter @rxwf/node-runner build` | 预存 TS 错误（agent-satellite/rag 等，非本 task）；`webhook-binary*` 无新增错误 | 是（本 task 范围） |
| 冒烟（若适用） | `pnpm --filter @rxwf/web test:e2e binary` | 无 binary E2E spec（T-111） | 是（Wave 4 职责） |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/triggers/webhook-binary.ts`（新增）
- `packages/node-runner/src/executors/triggers/webhook-binary.test.ts`（新增）
- `apps/api/src/routes/webhook-binary.test.ts`（新增）

## 实现说明

RISK-01：BIN-09 已在 `parse-webhook-body.ts` 实现；本 task 在 node-runner 所有权内补齐 `parseWebhookMultipartBody` / `webhookBinaryItemsFromBody`（CONF-05：form field 名→binary key），并通过 route 测试验证 API 解析与 node-runner 一致、ingress+执行后 webhook 节点 output 含 binary。

## 未解决问题

- Fastify webhook 路由对 `multipart/form-data` / `application/octet-stream` 直接 POST 返回 415（`webhook.ts` 缺 content-type parser，非本 task 所有权）；ingress 传入 `inputItems` 时 binary 链路正常
