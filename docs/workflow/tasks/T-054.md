# T-054

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 3 |
| 描述 | 审查并修复 nodeType `postgres`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-025, AC-028, AC-030 / node-runner postgres |
| 依赖 | T-034 |
| TDD Red 用例 | • `postgres` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E postgres spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/postgres.test.ts（或新建）<br>• apps/web/e2e/nodes/postgres.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/postgres.ts<br>• packages/node-runner/src/executors/postgres.test.ts<br>• apps/web/e2e/nodes/postgres.spec.ts<br>• docs/test/node-audit-rows/postgres.md<br>• apps/web/src/features/editor/node-params/postgres.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；standard 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=standard pnpm --filter @rxwf/web test:e2e postgres |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/postgres.test.ts`、`apps/web/src/features/editor/node-params/postgres.test.tsx`
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/postgres.test.ts`；`pnpm --filter @rxwf/web vitest run src/features/editor/node-params/postgres.test.tsx`
- 失败摘要（预期内）：`Cannot find module './postgres.js'` / `Failed to resolve import "./postgres.js"`

### Green

- 实现文件：`packages/node-runner/src/executors/postgres.ts`、`apps/web/src/features/editor/node-params/postgres.tsx`；`register-plus.ts` 替换 stub 为 `createPostgresExecutor`
- 运行命令：同上
- 通过摘要：node-runner 9/9 通过；web panel 3/3 通过

### Refactor

- 变更说明：提取 `resolveDatabaseUrl` / `resolveQueryForItem`；`pg.Pool` 单次执行后 `end()`；E2E spec 从 `.e2e-env` 解析 `connectionString`（Playwright webServer 未注入 `RXWF_DATABASE_URL`）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test -- src/executors/postgres.test.ts` | 9/9 通过 | 是 |
| 任务范围测试 | `pnpm --filter @rxwf/web vitest run src/features/editor/node-params/postgres.test.tsx` | 3/3 通过 | 是 |
| linter/build | `pnpm --filter @rxwf/node-runner build` | tsc 通过 | 是 |
| 冒烟（若适用） | `RXWF_E2E_TRACK=standard pnpm --filter @rxwf/web test:e2e postgres` | 4/4 通过（standard-chromium） | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/postgres.ts`（新建）
- `packages/node-runner/src/executors/postgres.test.ts`（新建）
- `packages/node-runner/src/executors/register-plus.ts`（stub → import，集成必需）
- `packages/node-runner/package.json`（添加 `pg`、`@types/pg`）
- `apps/web/src/features/editor/node-params/postgres.tsx`（新建）
- `apps/web/src/features/editor/node-params/postgres.test.tsx`（新建）
- `apps/web/e2e/nodes/postgres.spec.ts`（新建；Verify 阶段补充 `connectionString` 解析）
- `docs/test/node-audit-rows/postgres.md`（新建）

## 实现说明

将 `register-plus.ts` 内联 stub（空 rows）替换为真实 `createPostgresExecutor`：通过 `pg.Pool` 执行 SQL，连接串优先级为 `connectionString` → `deps.databaseUrl` → `RXWF_DATABASE_URL`；输出 `{ query, rows, rowCount }`；空白 query 报 E2002，无连接报 E2003。E2E debug-node 用例经 `resolveE2ePostgresUrl()` 注入 `connectionString`（对齐 compose 凭证）。

## 未解决问题

- 无
