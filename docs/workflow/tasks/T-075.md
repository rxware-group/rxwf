# T-075

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `toolRead`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner toolRead |
| 依赖 | T-034 |
| TDD Red 用例 | • `toolRead` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E toolRead spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/agent-satellite-tools.test.ts（或新建）<br>• apps/web/e2e/nodes/toolRead.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/agent-satellite-tools.ts<br>• packages/node-runner/src/executors/agent-satellite-tools.test.ts<br>• apps/web/e2e/nodes/toolRead.spec.ts<br>• docs/test/node-audit-rows/toolRead.md<br>• apps/web/src/features/editor/node-params/toolRead.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e toolRead |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/agent-satellite-tools.test.ts`（toolRead satellite/audit row/invoke 用例）；`apps/web/e2e/nodes/toolRead.spec.ts`（新建）
- 运行命令：`npx vitest run src/executors/agent-satellite-tools.test.ts -t "toolRead"`；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e toolRead --retries=0`
- 失败摘要（预期内）：audit row `existsSync(toolReadAuditRowPath)` 为 false；E2E audit row 测试 `ENOENT: docs/test/node-audit-rows/toolRead.md`

### Green

- 实现文件：`docs/test/node-audit-rows/toolRead.md`（新建）；`agent-satellite-tools.test.ts`（补充 toolRead 单测）；`apps/web/e2e/nodes/toolRead.spec.ts`（新建）；`agent-satellite-tools.ts` 无需改动（既有 `filesystem/read` 实现已满足审查）
- 运行命令（2026-06-20 复核）：
  - `npx vitest run src/executors/agent-satellite-tools.test.ts -t "toolRead"` → 6 passed
  - `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9879 RXWF_E2E_WEB_PORT=9324 pnpm --filter @rxwf/web test:e2e toolRead --retries=0 --workers=1` → 3 passed（auth + audit + 面板）
  - `RXWF_E2E_TRACK=standard RXWF_E2E_API_PORT=9880 RXWF_E2E_WEB_PORT=9325 pnpm --filter @rxwf/web test:e2e toolRead --retries=0 --workers=1` → 5 passed（含 @plus @any 直执 E2003 + aiAgent 卫星接线）
- 通过摘要：卫星注册/invoke（path、target_file、E1056、E1041）、audit row、面板内置描述、debug-node 直执 E2003、aiAgent 卫星无 E2003

### Refactor

- 变更说明：E1041 单测改为读取目录（不存在文件经 realpath 映射 E1056）；E2E aiAgent 用例仅验证卫星接线无 E2003（不依赖 live LLM）；无独立 `toolRead.tsx`（`BuiltinSatelliteToolDescription` 已覆盖）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `npx vitest run src/executors/agent-satellite-tools.test.ts -t "toolRead"` | 6 passed（2026-06-20） | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite … test:e2e toolRead --retries=0 --workers=1`（备用端口 9879/9324） | 3 passed | 是 |
| E2E standard/@plus @any | `RXWF_E2E_TRACK=standard … test:e2e toolRead --retries=0 --workers=1`（备用端口 9880/9325） | 5 passed | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e toolRead --retries=0` | global-setup 失败：`docker-compose.plus.yml` api 无 image/build | 否（本机环境；spec 已就绪；standard 轨已覆盖 @plus @any） |
| 验收标准核对 | `docs/test/node-audit-rows/toolRead.md` 四维 ok + executor satellite + status ok；E1056/E1041/E2003 单测；E2E spec 含面板与执行路径 | 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/agent-satellite-tools.test.ts`
- `apps/web/e2e/nodes/toolRead.spec.ts`（新增）
- `docs/test/node-audit-rows/toolRead.md`（新增）
- `docs/workflow/tasks/T-075.md`

## 实现说明

审查结论：`toolRead` 为固定能力卫星节点，面板由 `BuiltinSatelliteToolDescription` + 空 schema 覆盖；执行经 `agent-satellite-tools` 注册 `filesystem/read` 并由 `invokeAgentTool` 调用 embedded/agent gateway；无 standalone `registerPlusExecutors` 条目（直执 E2003）。补充 audit row、单测（E2003/E1056/E1041/path 别名）与 E2E spec。

## 未解决问题

- 本机 plus 轨 E2E global-setup 因 `deploy/docker-compose.plus.yml` 配置问题未能启动 Docker 栈；CI 修复 compose 后应跑通 `@plus` 轨（standard 轨已覆盖 @plus @any 用例）
