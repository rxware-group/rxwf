# T-026

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 2 |
| 描述 | skillRun 执行器集成 write/grep/web_search 子工具 |
| 追溯 | FR-05 / AC-013 / skill-run executor |
| 依赖 | T-016, T-017, T-018 |
| TDD Red 用例 | • skillRun 调用 write 工具端到端应成功 |
| 建议测试文件 | • packages/node-runner/src/executors/skill-run.test.ts |
| 文件所有权 | • packages/node-runner/src/executors/skill-run.ts<br>• packages/node-runner/src/executors/skill-run.test.ts |
| 验收标准 | AC-013 skillRun 集成 green |
| 基本验证提示 | pnpm --filter @rxwf/node-runner test skill-run |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/skill-run.test.ts`（`invokes write satellite tool end-to-end and writes file`）
- 运行命令：`pnpm --filter @rxwf/node-runner test skill-run`
- 失败摘要（预期内）：`AwfError: Unknown filesystem method: write` — embedded 路径未实现 write 调用链

### Green

- 实现文件：`packages/node-runner/src/executors/skill-run.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test skill-run`
- 通过摘要：15 passed — write/grep/web_search 卫星端到端 + 既有用例全绿

### Refactor

- 变更说明：抽取 `invokeSkillRunFilesystem` / `buildSkillRunAgentToolCtx` / `invokeSkillRunSatelliteTool`，统一 embedded gateway 与 `dispatchWebSearch` 委托

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test skill-run` | 15 passed / 0 failed | 是 |
| linter/build | `pnpm --filter @rxwf/node-runner build` | tsc exit 0 | 是 |
| 冒烟（若适用） | write/grep/web_search 端到端单测覆盖卫星调用链 | 3 项集成用例通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/skill-run.ts`
- `packages/node-runner/src/executors/skill-run.test.ts`
- `docs/workflow/tasks/T-026.md`

## 实现说明

skill-run 为 toolWrite/toolGrep/toolRead 卫星接入 embedded filesystem gateway（write/grep/read），经 `resolveRunnerForToolNode` 路由；toolWebSearch 在 embedded 模式委托 `dispatchWebSearch`（T-018 provider）并校验 skill `network` 权限。

## 未解决问题

- 无
