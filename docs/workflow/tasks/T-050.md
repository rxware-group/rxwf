# T-050

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 2 |
| 描述 | 审查并修复 nodeType `code`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-028, AC-030 / node-runner code |
| 依赖 | T-034 |
| TDD Red 用例 | • `code` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E code spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/code.test.ts（或新建）<br>• apps/web/e2e/nodes/code.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/code.ts<br>• packages/node-runner/src/executors/code.test.ts<br>• apps/web/e2e/nodes/code.spec.ts<br>• docs/test/node-audit-rows/code.md<br>• apps/web/src/features/editor/node-params/code.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；lite 轨验收通过 |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e code |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/code.test.ts`（新增注册/元数据/错误传播用例）、`apps/web/e2e/nodes/code.spec.ts`（新建）
- 运行命令：
  - `pnpm --filter @rxwf/web test:e2e code`（spec 不存在）→ `Error: No tests found`
  - `pnpm --filter @rxwf/node-runner test -- code.test.ts -t "registered"`（用例未添加前仅 2 项）
- 失败摘要（预期内）：E2E 无匹配 spec；审查用例待补全

### Green

- 实现文件：无 `code.ts` 变更（审查确认已正确）；新增/扩展测试与审查记录
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test -- code.test.ts` → 8 passed
  - `pnpm --filter @rxwf/web test:e2e code` → 4 passed（含 auth setup）
- 通过摘要：执行器注册、沙箱委托、遗留 `code` 字段、超时与元数据传递、E2002 传播；E2E 面板 + debug-node 执行 + `$log` 收集

### Refactor

- 变更说明：无（实现已满足要求，仅补测试与 audit-row）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test -- code.test.ts` | 8 passed | 是 |
| 任务范围测试 | `pnpm --filter @rxwf/web test:e2e code` | 4 passed | 是 |
| 验收标准核对 | `docs/test/node-audit-rows/code.md` 结论 ok；lite E2E 绿 | 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/code.test.ts`
- `apps/web/e2e/nodes/code.spec.ts`
- `docs/test/node-audit-rows/code.md`
- `docs/workflow/tasks/T-050.md`

## 实现说明

审查确认 `code` 执行器已在 `register-builtin` 注册，面板经 `node-param-schemas` + `CodeJsEditor` 渲染，无专属保存校验；失败码为沙箱 `E2002` 与 registry `E2003`。补全单测与 lite 轨 E2E，写入 `node-audit-rows/code.md`（panel/validation/executor/error_codes 均为 ok）。

## 未解决问题

- 矩阵主表 `node-audit-matrix.md` 行仍 pending，待 T-080 合并 audit-rows。
