# T-001

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-1 |
| Wave | 1 |
| 描述 | 增强 docs/README.md：分类目录、每文件一行摘要、维护规则 |
| 追溯 | FR-01 / AC-002 / §10 文档索引 |
| 依赖 | - |
| TDD Red 用例 | • lint-docs-index 或 README 结构测试应失败直至 README 含 categories 与 maintenance 节 |
| 建议测试文件 | • scripts/lint-docs-index.test.mjs（新增）或 docs/README 快照测试 |
| 文件所有权 | • docs/README.md<br>• docs/README.test.md（可选） |
| 验收标准 | README 满足 AC-002；Verify passed |
| 基本验证提示 | 人工打开 README 核对分类与维护规则 |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`docs/README.test.mjs`
- 运行命令：`node docs/README.test.mjs`
- 失败摘要（预期内）：5/5 失败——缺少 `## 分类目录`、`## 维护规则`；无分类子节；catalog 表格行 0 条（要求 ≥30）；维护节不存在

### Green

- 实现文件：`docs/README.md`
- 运行命令：`node docs/README.test.mjs`
- 通过摘要：5/5 通过——含 `## 分类目录`（11 个 `###` 子类、118 条文件一行摘要）、`## 维护规则`（6 条 INDEX/CI/同 PR 同步规则）

### Refactor

- 变更说明：无额外 refactor；保留原「本地运行」章节于维护规则之后

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `node docs/README.test.mjs` | 5/5 pass | 是（AC-002：分类目录、一行摘要、维护规则） |
| linter/build | 人工核对 README 结构 | 分类/摘要/维护规则完整 | 是 |
| 冒烟（若适用） | 人工打开 `docs/README.md` | 分类可导航；维护规则含 INDEX 登记与 CI 说明 | 是 |

**结论**：`passed`

## 实际变更文件

- `docs/README.md` — 重构为分类目录 + 维护规则 + 本地运行
- `docs/README.test.mjs` — AC-002 结构测试（node:test）
- `docs/README.test.md` — 测试运行说明

## 实现说明

将 README 增强为 AC-002 要求的「分类目录（每文件一行摘要）+ 维护规则」结构，并链至 INDEX.md / PRD / spec；新增 node:test 结构测试锁定必需章节与表格格式。

## 未解决问题

- `docs/INDEX.md` 尚未创建（T-002）；维护规则已引用 INDEX，待后续 task 落地后 CI 可完整校验
