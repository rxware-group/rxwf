# T-014

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-1 |
| Wave | 4 |
| 描述 | M-1 基线 E2E：帮助路由 + 索引/文档 smoke |
| 追溯 | FR-13 / AC-011 / §3.2 帮助加载 |
| 依赖 | T-009, T-011 |
| TDD Red 用例 | • /help 路由 404 应失败<br>• 已有 manual 节点 E2E 回归失败应修复 |
| 建议测试文件 | • apps/web/e2e/help-route-baseline.spec.ts<br>• apps/web/e2e/docs-index-smoke.spec.ts |
| 文件所有权 | • apps/web/e2e/help-route-baseline.spec.ts<br>• apps/web/e2e/docs-index-smoke.spec.ts |
| 验收标准 | M-1 E2E 入库且全量套件 green |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/web/e2e/help-route-baseline.spec.ts`、`apps/web/e2e/docs-index-smoke.spec.ts`（尚未创建）
- 运行命令：`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e help-route-baseline docs-index-smoke --project lite-chromium`
- 失败摘要（预期内）：`Error: No tests found`（spec 未入库）
- 测试文件创建后首次运行：3 failed / 16 passed — 断言需对齐实际 Markdown 渲染（无反引号文本）、not-found 页双「概览」链、INDEX 尚未登记 `e2e-coverage-matrix.md`

### Green

- 实现文件：`apps/web/e2e/help-route-baseline.spec.ts`、`apps/web/e2e/docs-index-smoke.spec.ts`
- 运行命令：`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e help-route-baseline docs-index-smoke workflow-manual-node --project lite-chromium`
- 通过摘要：20 passed（1 auth setup + 6 help-route + 12 docs-index + 1 manual-node）；manual 节点回归无需修复

### Refactor

- 变更说明：无（测试断言微调已并入 Green）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e help-route-baseline docs-index-smoke workflow-manual-node --project lite-chromium` | 20 passed / 0 failed | 是 |
| linter/build | 无 TS 编译变更（纯 Playwright spec） | N/A | 是 |
| 冒烟（若适用） | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e help-route-baseline docs-index-smoke --grep @smoke --project lite-chromium` | 18 passed / 0 failed（@smoke 子集，含 auth setup） | 是 |

**结论**：`passed`

## 实际变更文件

- `apps/web/e2e/help-route-baseline.spec.ts`
- `apps/web/e2e/docs-index-smoke.spec.ts`

## 实现说明

新增 M-1 基线 E2E：`help-route-baseline.spec.ts` 覆盖 `/help` HTTP 非 404、首页/节点 Markdown 渲染、未知 slug not-found 页与导航树；`docs-index-smoke.spec.ts` 解析 `docs/INDEX.md` entries，逐条验证已登记 `docs/help/zh/**` 路由可加载，并 smoke 检查 INDEX 核心治理文档登记与 help 首页 `docs/` 说明。

## 未解决问题

- **e2e-coverage-matrix 行更新**（E2E-P-007 → `help-route-baseline.spec.ts` / covered；E2E-P-017 → `docs-index-smoke.spec.ts` / covered）不在本 task 文件所有权，需编排器或 T-081 等任务合并。
- **裸 `pnpm --filter @rxwf/web test:e2e`**（无 spec 路径过滤）因 T-011 遗留的 `global-setup.test.ts` 被 Playwright 扫描导入、同步执行 `node:test` TAP，导致 `lite-setup` auth 偶发 `Project "lite-setup" not found`；显式 spec 路径或 `--grep @smoke` 运行全绿。建议后续在 `playwright.config.ts` 增加 `testIgnore: ['**/*.test.ts']` 排除单元测试文件。
