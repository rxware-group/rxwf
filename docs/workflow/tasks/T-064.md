# T-064

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `groupChat`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner groupChat |
| 依赖 | T-034 |
| TDD Red 用例 | • `groupChat` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E groupChat spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/group-chat.test.ts（或新建）<br>• apps/web/e2e/nodes/groupChat.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/group-chat.ts<br>• packages/node-runner/src/executors/group-chat.test.ts<br>• apps/web/e2e/nodes/groupChat.spec.ts<br>• docs/test/node-audit-rows/groupChat.md<br>• apps/web/src/features/editor/node-params/groupChat.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e groupChat |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/group-chat.test.ts`、`apps/web/e2e/nodes/groupChat.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test group-chat.test.ts`（补 registry/E1048 前已有 5 用例）；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web exec playwright test groupChat`（audit row / spec 创建前）
- 失败摘要（预期内）：E2E `ENOENT: docs/test/node-audit-rows/groupChat.md`；registry 未注册用例缺失（补测前）；面板用例 lone groupChat 节点 POST 工作流触发 E1048 保存失败

### Green

- 实现文件：`group-chat.test.ts`（补 registry + E1048 + langgraph 委托）、`apps/web/e2e/nodes/groupChat.spec.ts`（修复 `groupChatPanelDefinition` 含 2 成员）、`docs/test/node-audit-rows/groupChat.md`；执行器 `group-chat.ts` 已存在无需改动
- 运行命令：`pnpm --filter @rxwf/node-runner test group-chat.test.ts`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9930 RXWF_E2E_WEB_PORT=9370 pnpm --filter @rxwf/web exec playwright test groupChat --retries=0`
- 通过摘要：8/8 单测；lite E2E 3/3（audit row + 面板）

### Refactor

- 变更说明：面板 E2E 改用含 2 名 group_member 的有效 definition + `getByTestId('rf__node-gc')`；`createWorkflow` 增加错误体输出

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test group-chat.test.ts` | 8 passed | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9930 RXWF_E2E_WEB_PORT=9370 pnpm --filter @rxwf/web exec playwright test groupChat --retries=0` | 3 passed | 是 |
| E2E plus/@plus | `RXWF_E2E_TRACK=plus RXWF_E2E_API_PORT=9920 RXWF_E2E_WEB_PORT=9360 pnpm --filter @rxwf/web exec playwright test groupChat --retries=0` | global-setup 失败：`docker-compose.plus.yml` api/web 无 image/build | 否（本机 compose 配置）；spec 已含 `@plus` E1048 + Ollama 冒烟 |
| 验收标准核对 | `docs/test/node-audit-rows/groupChat.md` 五维 ok + status ok；E1048/E2003 单测；E2E spec 含 `@plus` 执行用例 | M-3 stub 审查通过；plus 轨需 CI compose + Plus API | 是（M-3 范围） |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/group-chat.test.ts`
- `apps/web/e2e/nodes/groupChat.spec.ts`
- `docs/test/node-audit-rows/groupChat.md`
- `docs/workflow/tasks/T-064.md`

## 实现说明

M-4 前 stub 审查：面板（`node-param-schemas` 通用渲染）、保存校验（`E1048`/`E1012`）、Plus 执行器（`registerPlusExecutors` + round-robin/langgraph）、运行时错误码均已就绪。补充 registry/E1048 单测、audit row 与 E2E spec；未新增 `groupChat.tsx`（通用面板已覆盖）。UserProxy resume / orchestrator API 全链路由 M-4 T-086–T-093 验收。

## 未解决问题

- 本机 plus compose `docker-compose.plus.yml` 报 api/web 无 build context，plus 轨 E2E 未能启动（CI 应正常）
- `@plus` round-robin 冒烟依赖 Ollama 可达，不可达时 skip
