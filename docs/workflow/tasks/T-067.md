# T-067

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `aiMemory`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner aiMemory |
| 依赖 | T-034 |
| TDD Red 用例 | • `aiMemory` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E aiMemory spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/agent-satellite-tools.test.ts（或新建）<br>• apps/web/e2e/nodes/aiMemory.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/agent-satellite-tools.ts<br>• packages/node-runner/src/executors/agent-satellite-tools.test.ts<br>• apps/web/e2e/nodes/aiMemory.spec.ts<br>• docs/test/node-audit-rows/aiMemory.md<br>• apps/web/src/features/editor/node-params/aiMemory.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e aiMemory |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/agent-satellite-tools.test.ts`、`apps/web/e2e/nodes/aiMemory.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner exec vitest run agent-satellite-tools.test.ts -t "aiMemory"`（audit row 创建前）
- 失败摘要（预期内）：`aiMemory M-3 audit row` — `existsSync(aiMemoryAuditRowPath)` false；E2E spec 依赖 audit row 文件

### Green

- 实现文件：`docs/test/node-audit-rows/aiMemory.md`、`apps/web/e2e/nodes/aiMemory.spec.ts`；`agent-satellite-tools.ts` 无需改动（记忆由 `run-ai-agent-node.ts` / `crew-memory-bridge.ts` 消费）
- 运行命令：`pnpm --filter @rxwf/node-runner exec vitest run agent-satellite-tools.test.ts -t "aiMemory"`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9927 RXWF_E2E_WEB_PORT=9367 pnpm --filter @rxwf/web test:e2e aiMemory --retries=0 --workers=1`
- 通过摘要：3/3 aiMemory 单测通过；4/4 lite E2E 通过（含 audit row、面板、W1010 validate）

### Refactor

- 变更说明：audit row 单测补充 E1014/W1010 断言（与 aiOutputParser 对齐）；卫星节点无独立 executor，审查结论为 `satellite`

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner exec vitest run agent-satellite-tools.test.ts -t "aiMemory"` | 3 passed（2026-06-20） | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9927 RXWF_E2E_WEB_PORT=9367 pnpm --filter @rxwf/web test:e2e aiMemory --retries=0 --workers=1` | 4 passed（2026-06-20） | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e aiMemory --retries=0` | global-setup 失败：`docker-compose.plus.yml` api 服务无 image/build | 否（本机 Docker compose，同 T-057/T-066）；spec 已含 `@plus` Ollama 冒烟 |
| 验收标准核对 | `docs/test/node-audit-rows/aiMemory.md` 四维 ok + executor satellite + status ok；E2003/E1014/W1010 文档化；E2E 面板 + W1010 + @plus Ollama 冒烟（skip 当不可达） | matrix 行 AUDIT-N-aiMemory 审查结论齐全；lite 轨 @any 覆盖面板与 validate | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/agent-satellite-tools.test.ts`
- `apps/web/e2e/nodes/aiMemory.spec.ts`
- `docs/test/node-audit-rows/aiMemory.md`
- `docs/workflow/tasks/T-067.md`

## 实现说明

审查结论：面板（`node-param-schemas` sessionId/maxTurns）、校验（E1014 重复 Memory、W1010 孤立卫星）、执行器（卫星，经 aiAgent `run-ai-agent-node.ts` 与 Crew `crew-memory-bridge.ts` 消费）、错误码 E1014/W1010 均已就绪。补充 aiMemory 单测、E2E spec 与 audit row；未新增独立 `aiMemory.tsx`（通用面板已覆盖）。

## 未解决问题

- 本机 `RXWF_E2E_TRACK=plus` global-setup 因 `deploy/docker-compose.plus.yml` 中 api 服务缺少 image/build 失败；CI 修复 compose 后应跑通 plus 轨 `@plus` Ollama 冒烟
- `@plus` debug-node aiAgent+Memory 用例在 Ollama 不可达时 skip
