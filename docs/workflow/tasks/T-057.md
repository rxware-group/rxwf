# T-057

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `ragRetrieve`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner ragRetrieve |
| 依赖 | T-034 |
| TDD Red 用例 | • `ragRetrieve` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E ragRetrieve spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/rag.test.ts（或新建）<br>• apps/web/e2e/nodes/ragRetrieve.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/rag.ts<br>• packages/node-runner/src/executors/rag.test.ts<br>• apps/web/e2e/nodes/ragRetrieve.spec.ts<br>• docs/test/node-audit-rows/ragRetrieve.md<br>• apps/web/src/features/editor/node-params/ragRetrieve.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e ragRetrieve |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/rag.test.ts`、`apps/web/e2e/nodes/ragRetrieve.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test rag.test.ts`；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e ragRetrieve --retries=0`（audit row / spec 创建前）
- 失败摘要（预期内）：单测 `ragRetrieve M-3 audit row` — `existsSync(ragRetrieveAuditRowPath)` false；E2E spec 文件不存在

### Green

- 实现文件：`packages/node-runner/src/executors/rag.test.ts`（补充 ragRetrieve 单测）、`apps/web/e2e/nodes/ragRetrieve.spec.ts`、`docs/test/node-audit-rows/ragRetrieve.md`；`rag.ts` 无需改动
- 运行命令：`pnpm --filter @rxwf/node-runner test rag.test.ts`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9926 RXWF_E2E_WEB_PORT=9366 pnpm --filter @rxwf/web test:e2e ragRetrieve --retries=0`
- 通过摘要：21/21 单测；6/6 lite E2E 通过 + 1 skipped（Ollama 不可达时跳过成功检索冒烟）

### Refactor

- 变更说明：无代码 refactor；E2E debug-node 用例置于 `@any`（与 postgres 一致），lite 轨可验证执行路径；工作流名称加时间戳避免 E1040 重名

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test rag.test.ts` | 21 passed（2026-06-20） | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e ragRetrieve --retries=0 --workers=1` | 6 passed, 1 skipped（Ollama 不可达） | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e ragRetrieve --retries=0` | global-setup 失败：`docker-compose.plus.yml` api 服务无 image/build | 否（本机 Docker compose 配置） |
| 验收标准核对 | `docs/test/node-audit-rows/ragRetrieve.md` 四维 ok + status ok；E1004/E3001/E3003/E2003 单测；E2E 面板 + debug-node E1004/E3003 | matrix 行 AUDIT-N-ragRetrieve 审查结论齐全；lite 轨 @any 覆盖面板与 debug-node | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/rag.test.ts`
- `apps/web/e2e/nodes/ragRetrieve.spec.ts`
- `docs/test/node-audit-rows/ragRetrieve.md`
- `docs/workflow/tasks/T-057.md`

## 实现说明

审查结论：面板（`node-param-schemas` query + `KnowledgeBaseIdsField`）、校验（无额外保存错误码，运行时 E1004）、Plus 执行器（`createRagExecutors` / `registerPlusExecutors`）、错误码 E1004/E3001/E3003 均已就绪。补充 ragRetrieve 单测、E2E spec 与 audit row；未新增独立 `ragRetrieve.tsx`（通用面板已覆盖）。

## 未解决问题

- 本机 `RXWF_E2E_TRACK=plus` global-setup 因 `deploy/docker-compose.plus.yml` 中 api 服务缺少 image/build 失败；CI 修复 compose 后应跑通 plus 轨
- Ollama 不可达时 E2E 成功检索用例 skip；本机启 Ollama 后可验证完整 RAG 检索路径
