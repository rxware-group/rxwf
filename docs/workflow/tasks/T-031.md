# T-031

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 3 |
| 描述 | schemaVersion:1 工作流导入回归测试（M-2 变更） |
| 追溯 | FR-17 / AC-021 / packages/workflow |
| 依赖 | T-026, T-030 |
| TDD Red 用例 | • fixtures/v1 工作流导入失败应报错 |
| 建议测试文件 | • packages/workflow/src/schema-compat.test.ts |
| 文件所有权 | • packages/workflow/src/schema-compat.test.ts<br>• packages/workflow/fixtures/schema-v1/*.json |
| 验收标准 | AC-021 green |
| 基本验证提示 | pnpm --filter @rxwf/workflow test schema-compat |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/workflow/src/schema-compat.test.ts`
- 运行命令：`pnpm --filter @rxwf/workflow test schema-compat`
- 失败摘要（预期内）：`ENOENT: no such file or directory, scandir '.../fixtures/schema-v1'` — 测试与 `importSchemaV1Workflow` 已实现，fixtures 目录尚不存在；另 2 条负向用例（非 v1 schemaVersion / 校验失败）设计为断言 `ok: false`

### Green

- 实现文件：`packages/workflow/fixtures/schema-v1/*.json`（4 个 M-2 代表性 v1 fixture）
- 运行命令：`pnpm --filter @rxwf/workflow test schema-compat`
- 通过摘要：6/6 passed — 4 fixture 回归 + 2 负向导入报错用例

### Refactor

- 变更说明：无；`importSchemaV1Workflow` 与 `formatImportFailure` 已足够精简

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/workflow test schema-compat` | 6 passed / 0 failed | 是 |
| linter/build | `pnpm --filter @rxwf/workflow build` + `pnpm --filter @rxwf/workflow test` | tsc exit 0；94/94 passed | 是 |
| 冒烟（若适用） | fixture 覆盖 M-2 变更域：switch 动态分支、HTTP credentialId、skillRun+model、基础 trigger | 4 fixture 全部 import green | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/workflow/src/schema-compat.test.ts`
- `packages/workflow/fixtures/schema-v1/basic-manual-trigger.json`
- `packages/workflow/fixtures/schema-v1/switch-dynamic-branches.json`
- `packages/workflow/fixtures/schema-v1/http-credential-ref.json`
- `packages/workflow/fixtures/schema-v1/skill-run-with-model.json`
- `docs/workflow/tasks/T-031.md`

## 实现说明

新增 `importSchemaV1Workflow` 回归助手与 4 个 schema-v1 fixture，覆盖 M-2 半实现项（switch 动态分支、credential 引用、skillRun 卫星）及基线工作流；负向用例确保非 v1 或校验失败时导入报错，满足 AC-021 不破坏 v1 导入兼容性。

## 未解决问题

- 无
