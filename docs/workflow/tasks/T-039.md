# T-039

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `subworkflowTrigger`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner subworkflowTrigger |
| 依赖 | T-034 |
| TDD Red 用例 | • `subworkflowTrigger` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E subworkflowTrigger spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/triggers/subworkflow.test.ts（或新建）<br>• apps/web/e2e/nodes/subworkflowTrigger.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/triggers/subworkflow.ts<br>• packages/node-runner/src/executors/triggers/subworkflow.test.ts<br>• apps/web/e2e/nodes/subworkflowTrigger.spec.ts<br>• docs/test/node-audit-rows/subworkflowTrigger.md<br>• apps/web/src/features/editor/node-params/subworkflowTrigger.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e subworkflowTrigger |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/triggers/subworkflow.test.ts`（补 registry 用例）、`apps/web/e2e/nodes/subworkflowTrigger.spec.ts`（新建）
- 运行命令：
  - `pnpm --filter @rxwf/web test:e2e subworkflowTrigger` → `Error: No tests found`
  - registry 用例写入前：缺 E2003 / registerBuiltin 覆盖
- 失败摘要（预期内）：无 E2E spec；审查行缺失；registry 单测未覆盖

### Green

- 实现文件：执行器已存在（`subworkflow.ts` 无需改动）；新增审查行 + E2E + registry 单测
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test -- src/executors/triggers/subworkflow.test.ts` → 4 passed
  - `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e subworkflowTrigger` → 3 passed（@any spec）
- 通过摘要：registry E2003 + 透传单测；audit row 断言；面板「入参模式」+ debug-node 空 item + pin 下游 set

### Refactor

- 变更说明：E2E 合并为单 browser 用例（避免 worker auth 竞态）；下游 set 使用 `mode: expression` 验证 pin 数据

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test -- src/executors/triggers/subworkflow.test.ts` | 4 passed | 是 |
| 任务范围测试 | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9879 RXWF_E2E_WEB_PORT=9324 pnpm --filter @rxwf/web test:e2e subworkflowTrigger` | 3 passed（2026-06-20 复验） | 是（@any；默认端口 9878 占用时需换端口） |
| linter/build | 执行器/单测无 TS 报错 | 通过 | 是 |
| 验收标准核对 | `docs/test/node-audit-rows/subworkflowTrigger.md` 四维 ok + status ok + e2e_spec | 审查行已填写 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/triggers/subworkflow.test.ts`
- `apps/web/e2e/nodes/subworkflowTrigger.spec.ts`
- `docs/test/node-audit-rows/subworkflowTrigger.md`
- `docs/workflow/tasks/T-039.md`

## 实现说明

审查 `subworkflowTrigger`：面板（`SubworkflowTriggerFields`）、校验（E1051/E1052/E1056–E1058）、执行器注册与透传均已就绪；补充 registry 单测、audit row 与 E2E 证据。执行器实现无变更。

## 未解决问题

- `RXWF_E2E_TRACK=plus` 需 Docker Compose（`docker-compose.plus.yml` 本机 invalid），plus 轨 E2E 未实测；spec 标 `@any`，lite/CI 可验证
- `run-debug-execution.ts` 的 `TRIGGER_TYPES` 未含 `subworkflowTrigger`，直接 debug-node 无 pin 时仅得空 item（非本 task 所有权）
