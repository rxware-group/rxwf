# T-058

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `ragAnswer`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner ragAnswer |
| 依赖 | T-034 |
| TDD Red 用例 | • `ragAnswer` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E ragAnswer spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/rag.test.ts（或新建）<br>• apps/web/e2e/nodes/ragAnswer.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/rag.ts<br>• packages/node-runner/src/executors/rag.test.ts<br>• apps/web/e2e/nodes/ragAnswer.spec.ts<br>• docs/test/node-audit-rows/ragAnswer.md<br>• apps/web/src/features/editor/node-params/ragAnswer.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e ragAnswer |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/rag.test.ts`（ragAnswer 段）、`apps/web/e2e/nodes/ragAnswer.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test rag.test.ts`
- 失败摘要（预期内）：audit row `ENOENT`（2/13 ragAnswer 相关失败）；`fallbackToChat` 运算符优先级 bug 导致非 E3003 错误误回退（1/13）

### Green

- 实现文件：`packages/node-runner/src/executors/rag.ts`（修复 fallback 条件括号）、`rag.test.ts`、`apps/web/e2e/nodes/ragAnswer.spec.ts`、`docs/test/node-audit-rows/ragAnswer.md`
- 运行命令：`pnpm --filter @rxwf/node-runner test rag.test.ts`；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web run test:e2e -- ragAnswer --retries=0`
- 通过摘要：21/21 单测（含 T-057 ragRetrieve 段）；3/3 lite E2E（audit row + 面板知识库/Model/问题/模板/回退）

### Refactor

- 变更说明：E2E 工作流名称加 `Date.now()` 后缀避免持久化 DB 重名（E1040）；`createWorkflow` 失败时输出响应体；对齐 `ragRetrieve.spec.ts` 使用 `@plus` 轨 debug-node；无独立 `ragAnswer.tsx`（通用面板已覆盖）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test rag.test.ts` | 21 passed | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web run test:e2e -- ragAnswer --retries=0` | 3 passed | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web run test:e2e -- ragAnswer --retries=0` | global-setup 失败：`docker-compose.plus.yml` api/web 无 build context | 否（本机环境） |
| 验收标准核对 | `docs/test/node-audit-rows/ragAnswer.md` 四维 ok + status ok；E1004/E3001/E3003 单测；E2E spec 含 `@plus` 执行用例 | 代码就绪；plus 需 CI Docker + 可选本机 Ollama | 部分 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/rag.ts`
- `packages/node-runner/src/executors/rag.test.ts`
- `apps/web/e2e/nodes/ragAnswer.spec.ts`
- `docs/test/node-audit-rows/ragAnswer.md`
- `docs/workflow/tasks/T-058.md`

## 实现说明

审查结论：面板（schema + `KnowledgeBaseIdsField` + `OllamaModelParamField`）、校验（无额外保存错误码）、Plus 执行器（`createRagExecutors` 注册 `ragAnswer`）、错误码 E1004/E3001/E3003 均已就绪。修复 `fallbackToChat` 条件运算符优先级 bug；E2E 工作流名唯一化；补充 ragAnswer 单测、E2E spec 与 audit row。

## 未解决问题

- 本机 `docker-compose.plus.yml` 无效，plus 轨 E2E 未能实际执行；CI 启 Docker 后应跑通 `@plus`（E1004 路径 + Ollama 可达时 fallback/live 冒烟）
- `rag.test.ts` 与 T-057 共享所有权，文件内同时含 ragRetrieve 审查用例
