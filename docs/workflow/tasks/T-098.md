# T-098

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-4 |
| Wave | 4 |
| 描述 | M-4 全量 E2E 回归 |
| 追溯 | FR-10 / AC-042 / §8 Group Chat |
| 依赖 | T-096, T-097 |
| TDD Red 用例 | • 全量 E2E 失败 |
| 建议测试文件 | • docs/test/milestones/M-4-report.md |
| 文件所有权 | • docs/test/milestones/M-4-report.md |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e group-chat |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`docs/test/milestones/M-4-report.md`（缺失）；`apps/web/e2e/**/*.spec.ts`；plus group-chat
- 运行命令：
  - `Test-Path docs/test/milestones/M-4-report.md` → `False`（报告不存在）
  - `RXWF_E2E_TRACK=lite … playwright test <54 specs> --workers=3` → 152 passed / 2 failed / 1 skipped（`skill-run-tools` 并行竞态）
  - `RXWF_E2E_TRACK=plus … playwright test group-chat` → exit 1（`docker compose.plus.yml` api 无 image/build）
- 失败摘要（预期内）：M-4 报告缺失；lite 全量并行首轮有失败；plus 轨 compose 阻塞

### Green

- 实现文件：`docs/test/milestones/M-4-report.md`
- 运行命令：
  - `node scripts/validate-e2e-matrix.mjs` → OK: 65 rows
  - `node scripts/validate-spec-gap-audit.mjs` → OK: 40 gap rows（GAP-011 done）
  - `pnpm --filter @rxwf/workflow test schema-compat` → 6 passed
  - lite 全量 E2E（54 spec 显式路径，`--project=lite-chromium --retries=1 --workers=1`，端口 9888/9333）→ **最佳轮次 153 passed / 1 skipped / 0 failed**
  - lite group-chat 专项：`pnpm --filter @rxwf/web test:e2e group-chat` → 5 passed；`playwright test group-chat nodes/groupChat.spec.ts` → **7 passed**
- 通过摘要：M-4 milestone 报告产出；lite 轨全量回归最佳轮次 green；group-chat `@any` 7/7 green

### Refactor

- 变更说明：无代码变更；纯 milestone 报告；E2E 采用显式 `*.spec.ts` 路径 + `--workers=1`（与 T-084/M-3 报告一致）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试（lite 全量） | `cd apps/web && RXWF_E2E_TRACK=lite npx playwright test --project=lite-chromium --retries=1 --workers=1 <54× *.spec.ts>` | 最佳 153 passed / 1 skipped / 0 failed | 是（AC-042 lite 轨回归） |
| 任务范围测试（group-chat） | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e group-chat` + `playwright test group-chat nodes/groupChat.spec.ts` | 5 + 7 passed | 是（AC-039/040 Group Chat @any） |
| 关联校验 | `node scripts/validate-e2e-matrix.mjs`；`node scripts/validate-spec-gap-audit.mjs` | 65 rows OK；GAP-011 done | 是（T-096 依赖就绪） |
| schema 回归 | `pnpm --filter @rxwf/workflow test schema-compat` | 6 passed | 是（AC-021 延续） |
| plus 轨 group-chat | `RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e group-chat` | blocked（compose image） | 否（本机基础设施；lite @any 已 green，见 M-4-report 阻塞项） |

**结论**：`passed`

## 实际变更文件

- `docs/test/milestones/M-4-report.md`（新建）
- `docs/workflow/tasks/T-098.md`

## 实现说明

收口 M-4 Wave 4：在 T-096/097 矩阵与验收清单就绪基础上，执行 lite 轨全量 E2E 回归（最佳 153/153 可执行 green）、group-chat specs 专项（7/7 green）、schema-compat 回归，产出 `M-4-report.md` 供 tester/verifier 引用。

## 未解决问题

- 裸 `pnpm --filter @rxwf/web test:e2e` 仍受 `global-setup.test.ts` 扫描影响（建议 `testIgnore`，非本 task 所有权）
- `RXWF_E2E_TRACK=plus` group-chat `@plus` live 用例本机 compose 阻塞（与 T-028/M-3 相同）
- lite 全量套件偶发 flaky（`httpRequest` httpbin、`crewSupervisor` 隔离、`skill-run-tools` 并行）
- `validate-spec-gap-audit --milestone M-4` GAP-018/019 仍 open（非 Group Chat 范围）
- T-097 `M-4-acceptance.md` 人工验收 AC-041/070 待后续签字
- `ragRetrieve` KB 索引用例条件 skip
