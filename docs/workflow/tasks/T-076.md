# T-076

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `toolWrite`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner toolWrite |
| 依赖 | T-034 |
| TDD Red 用例 | • `toolWrite` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E toolWrite spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/agent-satellite-tools.test.ts（或新建）<br>• apps/web/e2e/nodes/toolWrite.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/agent-satellite-tools.ts<br>• packages/node-runner/src/executors/agent-satellite-tools.test.ts<br>• apps/web/e2e/nodes/toolWrite.spec.ts<br>• docs/test/node-audit-rows/toolWrite.md<br>• apps/web/src/features/editor/node-params/toolWrite.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e toolWrite |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`agent-satellite-tools.test.ts`（toolWrite satellite/registry/audit/build/invoke 用例）、`toolWrite.spec.ts`（新建）、`toolWrite.md`（新建）
- 运行命令：`pnpm --filter @rxwf/node-runner exec vitest run src/executors/agent-satellite-tools.test.ts -t "toolWrite|writes a file|appends to an existing|write path"`（实现前）
- 失败摘要（预期内）：audit row `ENOENT`；`invokeAgentTool` write → `E2002 Unknown filesystem method: write`；E2E spec 不存在

### Green

- 实现文件：`agent-satellite-tools.ts` 补齐 `resolveWritePath` + `invokeEmbeddedRunnerTool` write/append；`toolWrite.md`、`toolWrite.spec.ts`、单测
- 运行命令：同上 vitest（实现后）；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9920 RXWF_E2E_WEB_PORT=9360 pnpm --filter @rxwf/web test:e2e toolWrite -- --retries=0 --workers=1`
- 通过摘要：7/7 toolWrite 相关单测；3/3 lite E2E（audit row + 面板内置描述/defaultEncoding）

### Refactor

- 变更说明：无额外重构；`resolveWritePath` 与 skill-run/skill-runtime 写入语义对齐

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner exec vitest run src/executors/agent-satellite-tools.test.ts -t "toolWrite|writes a file|appends to an existing|write path"` | 7 passed（2026-06-20 复验） | 是 |
| E2E @any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9920 RXWF_E2E_WEB_PORT=9360 pnpm --filter @rxwf/web test:e2e toolWrite -- --retries=0 --workers=1` | 3 passed（2026-06-20 复验） | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e toolWrite` | global-setup 失败：compose plus 无 image/build（2026-06-20 复验） | 否（本机基础设施） |
| linter/build | 未改 TS 导出签名；单测覆盖 write 路径 | — | 是 |
| 验收标准核对 | `toolWrite.md` 四维 ok + executor satellite + status ok；embedded write；E2E 面板+直执 E2003（@plus，lite 轨跳过） | 矩阵行待 T-080 合并；审查行有结论 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/agent-satellite-tools.ts`
- `packages/node-runner/src/executors/agent-satellite-tools.test.ts`
- `apps/web/e2e/nodes/toolWrite.spec.ts`
- `docs/test/node-audit-rows/toolWrite.md`
- `docs/workflow/tasks/T-076.md`

## 实现说明

审查结论：面板（`defaultEncoding` schema + 内置 Tool 描述）、校验（固定能力无保存期错误码）、卫星执行（`buildAgentToolDefinitions` + `invokeAgentTool`）已就绪；缺口为 **aiAgent embedded 路径** 未实现 `skill:filesystem` write。已补齐 `resolveWritePath` 与 write/append 逻辑；M-2 skill-run embedded write 保持不变。无独立 `toolWrite.tsx`。

## 未解决问题

- 本机 `RXWF_E2E_TRACK=plus` 因 `deploy/docker-compose.plus.yml` 无法启动；CI 修复后应复跑 `@plus` 直执 E2003 与 aiAgent 接线用例
- `e2e-coverage-matrix.md` E2E-N-toolWrite 行由 T-080 合并为 covered
