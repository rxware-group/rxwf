# T-099

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-5 |
| Wave | 1 |
| 描述 | Binary 现状快照矩阵（类型/HTTP/节点透传/DB） |
| 追溯 | FR-11 / AC-045 / §9 Binary |
| 依赖 | T-098 |
| TDD Red 用例 | • B-6 未通过前实现测试必须失败 |
| 建议测试文件 | • docs/architecture/binary-current-state.test.mjs |
| 文件所有权 | • docs/architecture/binary-current-state.md |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | pnpm --filter @rxwf/shared test; pnpm --filter @rxwf/web test:e2e binary |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`docs/architecture/binary-current-state.test.mjs`
- 运行命令：`node docs/architecture/binary-current-state.test.mjs`
- 失败摘要（预期内）：2 failed / 1 passed — `binary-current-state.md` 缺失；AC-045 矩阵断言失败；B-6 门禁测试通过（`m5BinaryPlanGate=blocked`，`assertM5BinaryImplementationAllowed` throw）

### Green

- 实现文件：`docs/architecture/binary-current-state.md`（BIN-01～BIN-21 能力矩阵 + §5 门禁）
- 运行命令：`node docs/architecture/binary-current-state.test.mjs`
- 通过摘要：3 passed — frontmatter `trace=AC-045`、`m5BinaryPlanGate=blocked`；21 行矩阵齐全；B-6 实现门禁 assert throw 验证通过

### Refactor

- 变更说明：导出 `validateBinaryCurrentState` / `assertM5BinaryImplementationAllowed` 供 T-104+ 实现测试复用；`parseFrontmatter` 兼容 CRLF

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `node docs/architecture/binary-current-state.test.mjs` | 3 passed | 是（AC-045 B-1 快照 + B-6 blocked 门禁） |
| 关联 binary 单测 | `pnpm --filter @rxwf/shared test` | 6 passed（含 binary-utils） | 是 |
| E2E 冒烟 | `pnpm --filter @rxwf/web test:e2e binary` | 跳过 — 无 binary e2e spec（T-111 所有权） | 是（本 task 为 B-1 文档门禁） |
| 验收标准核对 | 矩阵覆盖类型/HTTP/节点透传/DB 四域；引用 architecture.md §9 | BIN-01～21 + §3.1 分层摘要 | 是 |

**结论**：`passed`

## 实际变更文件

- `docs/architecture/binary-current-state.md`（新建）
- `docs/architecture/binary-current-state.test.mjs`（新建）
- `docs/workflow/tasks/T-099.md`

## 实现说明

产出 M-5 B-1 现状快照：对照代码库扫描 WorkflowItem.binary 在类型、HTTP、节点透传、Lite DB 四层的能力矩阵（21 项），记录代码已超前于 B-6 的风险；`m5BinaryPlanGate` 保持 `blocked`，导出实现门禁断言供后续 Wave 3+ 任务使用。

## 未解决问题

- B-6 未确认前 `m5BinaryPlanGate` 须保持 blocked（T-103 改 cleared）
- Standard profile blob（BIN-21）与 Merge combineAll（BIN-15）留待 B-4/B-7
- `binary-type-support-analysis.md` 部分章节过时，B-2 起以本矩阵为真相源
