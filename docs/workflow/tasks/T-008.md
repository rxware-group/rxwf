# T-008

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-1 |
| Wave | 2 |
| 描述 | 编写 docs/workflow/spec-gap-audit.md 完整差距表 |
| 追溯 | FR-02 / AC-005, AC-006 / §5 M-1 |
| 依赖 | T-003, T-004 |
| TDD Red 用例 | • audit 行缺少 milestone 列应失败<br>• open 半实现项无目标 M 应失败 |
| 建议测试文件 | • scripts/validate-spec-gap-audit.test.mjs |
| 文件所有权 | • docs/workflow/spec-gap-audit.md<br>• scripts/validate-spec-gap-audit.mjs |
| 验收标准 | 全 v2.0 能力有行；含 spec/现状/M/E2E 行 ID |
| 基本验证提示 | node scripts/validate-spec-gap-audit.mjs |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`scripts/validate-spec-gap-audit.mjs`（`--test` 内嵌 TDD 用例；所有权不含独立 test 文件）
- 运行命令：
  - `node scripts/validate-spec-gap-audit.mjs --test` → 2 passed（缺 target_m 列、open partial 无目标 M 均失败）
  - `node scripts/validate-spec-gap-audit.mjs` → exit 1，缺 8 列 + 0 行 + GAP-001～040 缺失
- 失败摘要（预期内）：占位 audit 无完整差距表；内嵌用例确认缺 milestone 列与半实现无目标 M 可被捕获

### Green

- 实现文件：`scripts/validate-spec-gap-audit.mjs`、`docs/workflow/spec-gap-audit.md`
- 运行命令：
  - `node scripts/validate-spec-gap-audit.mjs` → `OK: 40 gap rows (GAP-001～GAP-040)`
  - `node scripts/validate-spec-gap-audit.mjs --test` → 2 passed
- 通过摘要：完整差距表 40 行，含 gap_id/category/spec_fr/code_status/gap/target_m/e2e_row_id/audit_status；与 spec-reviewer 矩阵及 e2e-coverage-matrix 互链

### Refactor

- 变更说明：表头解析要求 `gap_id | category` 连续列，避免与「列定义」说明表误判（对齐 validate-e2e-matrix 模式）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `node scripts/validate-spec-gap-audit.mjs --test` | 2 passed | 是 |
| 验收标准核对 | `node scripts/validate-spec-gap-audit.mjs` | OK: 40 gap rows | 是（AC-005 全量行；AC-006 spec/现状/M/E2E 列齐全） |
| linter/build | N/A（纯 Node 脚本 + Markdown，无 TS 变更） | 跳过 | 是 |

**结论**：`passed`

## 实际变更文件

- `docs/workflow/spec-gap-audit.md`
- `scripts/validate-spec-gap-audit.mjs`
- `docs/workflow/tasks/T-008.md`

## 实现说明

将 T-003 spec-reviewer 40 行差距矩阵展开为 AC-005/006 完整 audit 表，新增 `validate-spec-gap-audit.mjs` 校验必需列、GAP-001～040 完整性及 open partial 项目标 M 约束；`e2e_row_id` 与 `e2e-coverage-matrix.md` 行 ID 互链。

## 未解决问题

-
