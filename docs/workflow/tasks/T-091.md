# T-091

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-4 |
| Wave | 2 |
| 描述 | UserProxy 超时失败终止（>0 超时，默认 -1 不超时） |
| 追溯 | FR-10 / AC-037 / §8 Group Chat |
| 依赖 | T-090 |
| TDD Red 用例 | • 超时后仍 waiting 应失败<br>• 默认 -1 不触发 sweeper |
| 建议测试文件 | • packages/execution/src/hitl/group-chat-timeout.test.ts |
| 文件所有权 | • packages/execution/src/hitl/group-chat-timeout.ts<br>• packages/execution/src/hitl/group-chat-timeout.test.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e group-chat |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/execution/src/hitl/group-chat-timeout.test.ts`
- 运行命令：`pnpm --filter @rxwf/execution test -- src/hitl/group-chat-timeout.test.ts`
- 失败摘要（预期内）：`Cannot find module './group-chat-timeout.js'`（实现文件尚未创建）

### Green

- 实现文件：`packages/execution/src/hitl/group-chat-timeout.ts`
- 运行命令：`pnpm --filter @rxwf/execution test -- src/hitl/group-chat-timeout.test.ts`
- 通过摘要：5 passed — 默认 -1 不触发 sweeper；超时后 sweeper 调用 `failUserProxyTimeout` 返回 E1048 failed；`buildUserProxyTimeoutHitlFields` 仅在 `>0` 时写入 expiresAt

### Refactor

- 变更说明：无额外 refactor；复用 `parseHitlMetadata` / `parseGroupChatUserProxyCheckpoint` 区分 groupChat UserProxy waiting 与普通 HITL

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/execution test -- src/hitl/group-chat-timeout.test.ts` | 5 passed | 是 |
| 受影响模块测试 | `pnpm --filter @rxwf/execution test -- src/hitl/` | 22 passed (5 files) | 是 |
| linter/build | `pnpm --filter @rxwf/execution build` | exit 0 | 是 |
| 冒烟（若适用） | E2E 未跑（sweeper/API 集成由 T-093 验收） | N/A | 是（AC-037 模块层） |

**结论**：`passed`

## 实际变更文件

- `packages/execution/src/hitl/group-chat-timeout.ts`
- `packages/execution/src/hitl/group-chat-timeout.test.ts`
- `docs/workflow/tasks/T-091.md`

## 实现说明

新增 `group-chat-timeout` 模块：`resolveUserProxyTimeoutMs`（默认 -1）、`buildUserProxyTimeoutHitlFields`（>0 写入 expiresAt/action）、`shouldSweepGroupChatUserProxyWaiting` / `isGroupChatUserProxyWaitingExpired` 检测、`buildGroupChatUserProxyTimeoutFailure`（E1048）及 `runGroupChatUserProxyTimeoutSweepOnce` sweeper 入口。供 T-093 API/runtime 集成复用。

## 未解决问题

- `apps/api` HITL sweeper 尚未接入 `runGroupChatUserProxyTimeoutSweepOnce`（T-093）；`buildGroupChatUserProxyWaitingMetadata` 尚未合并 timeout 字段（node-runner 集成后续 wave）
