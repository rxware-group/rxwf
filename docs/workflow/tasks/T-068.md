# T-068

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `aiKnowledge`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner aiKnowledge |
| 依赖 | T-034 |
| TDD Red 用例 | • `aiKnowledge` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E aiKnowledge spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/crew-knowledge-bridge.test.ts（或新建）<br>• apps/web/e2e/nodes/aiKnowledge.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/crew-knowledge-bridge.ts<br>• packages/node-runner/src/executors/crew-knowledge-bridge.test.ts<br>• apps/web/e2e/nodes/aiKnowledge.spec.ts<br>• docs/test/node-audit-rows/aiKnowledge.md<br>• apps/web/src/features/editor/node-params/aiKnowledge.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e aiKnowledge |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/crew-knowledge-bridge.test.ts`（卫星 audit 段）、`apps/web/e2e/nodes/aiKnowledge.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test crew-knowledge-bridge.test.ts`（新增用例前 2/2）；E2E audit row 用例在 `aiKnowledge.md` 缺失时 ENOENT
- 失败摘要（预期内）：E2E `readAuditRowField` → audit row 文件不存在；卫星 registry / E3003 单测在实现已存在时直接 Green

### Green

- 实现文件：`crew-knowledge-bridge.test.ts`（补充单测）、`docs/test/node-audit-rows/aiKnowledge.md`、`apps/web/e2e/nodes/aiKnowledge.spec.ts`；`crew-knowledge-bridge.ts` 无需改动
- 运行命令：`pnpm --filter @rxwf/node-runner test crew-knowledge-bridge.test.ts`；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web run test:e2e -- aiKnowledge --retries=0 --workers=1`
- 通过摘要：7/7 单测；4/4 lite E2E（audit row + 面板知识库 + debug-node Agent E3003）

### Refactor

- 变更说明：E2E API 调用改用 `page.request`（对齐 toolSkill），避免 standalone `request` fixture 在本机 auth 不稳定导致 401；E3003 路径对齐 `ragRetrieve.spec.ts` 置于 `@any`；工作流名 `uniqueWorkflowName` 防重名；无独立 `aiKnowledge.tsx`（通用 `KnowledgeBaseIdsField` 已覆盖）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test crew-knowledge-bridge.test.ts` | 7 passed | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web run test:e2e -- aiKnowledge --retries=0 --workers=1` | 4 passed（audit + 面板 + E3003） | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web run test:e2e -- aiKnowledge --retries=0` | 未在本机执行（需 Docker compose + 可选 Ollama） | 部分（spec 含 `@plus` 冒烟，CI 环境验证） |
| 验收标准核对 | `docs/test/node-audit-rows/aiKnowledge.md` 五维结论 + status ok；卫星 executor 单测；E2E spec 含 `@plus` Ollama 冒烟 | 代码就绪 | 是（lite 轨全绿） |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/crew-knowledge-bridge.test.ts`
- `apps/web/e2e/nodes/aiKnowledge.spec.ts`
- `docs/test/node-audit-rows/aiKnowledge.md`
- `docs/workflow/tasks/T-068.md`

## 实现说明

审查结论：`aiKnowledge` 为卫星节点（`SATELLITE_NODE_TYPES`），面板经 `NodeEditorParamsPane` + `KnowledgeBaseIdsField` 就绪，校验含 E1014/W1010，运行时经 `run-ai-agent-node`（Agent）与 `enrichCrewIrWithKnowledge`（Crew）消费。补充卫星 registry 单测、Crew bridge E3003/跳过路径/manager enrichment 单测、audit row 与 E2E spec；E2E 改用 `page.request` 修复 auth 401。

## 未解决问题

- 本机 plus 轨 `@plus` Ollama 冒烟未实际执行；CI 启 Docker + Ollama 后应跑通
- 无独立 `apps/web/src/features/editor/node-params/aiKnowledge.tsx`（与 ragRetrieve 相同，非缺陷）
