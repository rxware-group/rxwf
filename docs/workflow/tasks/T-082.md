# T-082

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 5 |
| 描述 | error-codes.md 与节点失败码映射补全 |
| 追溯 | FR-09 / AC-030 / docs/error-codes |
| 依赖 | T-079 |
| TDD Red 用例 | • 节点 E2xx 未文档化应失败 |
| 建议测试文件 | • scripts/validate-error-codes.test.mjs |
| 文件所有权 | • docs/error-codes.md<br>• scripts/validate-error-codes.mjs |
| 验收标准 | 失败可诊断 |
| 基本验证提示 | node scripts/validate-error-codes.mjs |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`scripts/validate-error-codes.test.mjs`（新建）
- 运行命令：`node scripts/validate-error-codes.test.mjs`
- 失败摘要（预期内）：`repo error-codes.md passes validation` 失败——缺 `## 节点失败码映射`、40 个 nodeType 无映射行（2026-06-20）

### Green

- 实现文件：`scripts/validate-error-codes.mjs`、`docs/error-codes.md`（E2000/E2013/E2015/E2016、E2002 多义说明、40 行节点映射表）
- 运行命令：`node scripts/validate-error-codes.test.mjs`；`node scripts/validate-error-codes.mjs`
- 通过摘要：3/3 单测；校验器 OK（7 个 E2xx 码覆盖 40 节点）

### Refactor

- 变更说明：`collectNodeE2Codes` 仅从 audit row 的 `error_codes` 列与 `## 错误码` 表提取 E2xx，避免正文提及的通用码误报

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `node scripts/validate-error-codes.test.mjs` | 3 passed（2026-06-20） | 是 |
| 冒烟 | `node scripts/validate-error-codes.mjs` | OK: 7 E2xx codes across 40 nodes（2026-06-20） | 是 |
| 验收标准核对 | AC-030：节点失败 E2xx 可映射 `docs/error-codes.md` + audit row；未文档化 E2xx 校验失败 | 映射表 + 校验脚本就绪 | 是 |

**结论**：`passed`

## 实际变更文件

- `docs/error-codes.md`
- `scripts/validate-error-codes.mjs`
- `scripts/validate-error-codes.test.mjs`
- `docs/workflow/tasks/T-082.md`

## 实现说明

新增 `validate-error-codes.mjs`：从 `docs/test/node-audit-rows/*.md` 收集 E2xx，校验已在 `error-codes.md` 码表文档化且节点映射表含对应行。`error-codes.md` 补全 Runner/执行域 E2000/E2013/E2015/E2016，细化 E2002 多义说明，并增加 40 个 nodeType 的 AC-030 映射表。

## 未解决问题

- `humanApproval`、`merge` 等 audit row 的 E2xx 仅在正文提及、无 `error_codes` 列/错误码表，未纳入映射（与采集规则一致）
- E107x Skill/Web Search 码与 `workflow_run` 运行时 E1073 话术冲突仍待 T-080 统一（见 T-060 备注）
