# T-069

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `aiOutputParser`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner aiOutputParser |
| 依赖 | T-034 |
| TDD Red 用例 | • `aiOutputParser` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E aiOutputParser spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/agent-satellite-tools.test.ts（或新建）<br>• apps/web/e2e/nodes/aiOutputParser.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/agent-satellite-tools.ts<br>• packages/node-runner/src/executors/agent-satellite-tools.test.ts<br>• apps/web/e2e/nodes/aiOutputParser.spec.ts<br>• docs/test/node-audit-rows/aiOutputParser.md<br>• apps/web/src/features/editor/node-params/aiOutputParser.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e aiOutputParser |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/agent-satellite-tools.test.ts`（新增 aiOutputParser 段）、`apps/web/e2e/nodes/aiOutputParser.spec.ts`
- 运行命令：`pnpm exec vitest run agent-satellite-tools.test.ts -t "aiOutputParser"`（audit row 创建前）；E2E audit row 用例在 `aiOutputParser.md` 缺失时 ENOENT
- 失败摘要（预期内）：`aiOutputParser M-3 audit row` — `existsSync(aiOutputParserAuditRowPath)` false；E2E `readAuditRowField` 依赖 audit row 文件

### Green

- 实现文件：`agent-satellite-tools.ts`（`outputSchemaFromParserNode` / `parseOutputParserAnswer`）、`agent-satellite-tools.test.ts`（卫星 E2003 + schema 解析单测）、`docs/test/node-audit-rows/aiOutputParser.md`、`apps/web/e2e/nodes/aiOutputParser.spec.ts`
- 运行命令：`pnpm exec vitest run agent-satellite-tools.test.ts -t "aiOutputParser|outputSchemaFromParserNode|parseOutputParserAnswer"`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9891 RXWF_E2E_WEB_PORT=9336 pnpm --filter @rxwf/web test:e2e aiOutputParser --retries=0`
- 通过摘要：11/11 aiOutputParser 相关单测；4/4 lite E2E（audit row + 面板 Output JSON Schema + W1010 validate）

### Refactor

- 变更说明：无独立 `aiOutputParser.tsx`（通用 `NodeEditorParamsPane` + `node-param-schemas.ts` json 字段已覆盖）；`run-ai-agent-node.ts` 保留本地 `outputSchemaFromParserNode` 副本（父 Agent 运行时消费，不在本 task 所有权内）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm exec vitest run agent-satellite-tools.test.ts -t "aiOutputParser\|outputSchemaFromParserNode\|parseOutputParserAnswer"` | 11 passed（2026-06-20） | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9891 RXWF_E2E_WEB_PORT=9336 pnpm --filter @rxwf/web test:e2e aiOutputParser --retries=0` | 4 passed（2026-06-20） | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e aiOutputParser --retries=0` | global-setup 失败：`docker-compose.plus.yml` api 服务无 image/build | 否（本机 Docker compose，同 T-066/T-068）；spec 已含 `@plus` Ollama 冒烟（skip 当不可达） |
| 验收标准核对 | `docs/test/node-audit-rows/aiOutputParser.md` 五维 ok + executor satellite + status ok；E2003/E1014/E3013/W1010 文档化；E2E 面板 + W1010 + @plus debug-node | matrix 行 AUDIT-N-aiOutputParser 审查结论齐全；lite 轨 @any 覆盖面板与 validate | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/agent-satellite-tools.ts`
- `packages/node-runner/src/executors/agent-satellite-tools.test.ts`
- `apps/web/e2e/nodes/aiOutputParser.spec.ts`
- `docs/test/node-audit-rows/aiOutputParser.md`
- `docs/workflow/tasks/T-069.md`

## 实现说明

审查结论：`aiOutputParser` 为卫星节点（`SATELLITE_NODE_TYPES`），无独立 registry executor（直接执行 E2003）；面板经 `node-param-schemas.ts` `jsonSchema` 字段（Output JSON Schema）；校验含 E1014（多 Parser）与 W1010（孤立卫星）；运行时经 `run-ai-agent-node` 收集 `ai_outputParser` 连线、注入 `outputSchema`、产出 `parsed` 字段；补充 `outputSchemaFromParserNode`/`parseOutputParserAnswer` 单测与 E2E spec。

## 未解决问题

- 本机 `docker-compose.plus.yml` 无效，plus 轨 `@plus` Ollama 冒烟未能实际执行；CI 启 Docker 后应跑通
- 无独立 `apps/web/src/features/editor/node-params/aiOutputParser.tsx`（通用 schema 渲染已覆盖，非缺陷）
