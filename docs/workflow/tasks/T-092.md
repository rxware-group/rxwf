# T-092

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-4 |
| Wave | 2 |
| 描述 | Group Chat agentSteps 审计事件 |
| 追溯 | FR-10 / AC-035, AC-037 / §8 Group Chat |
| 依赖 | T-087, T-088 |
| TDD Red 用例 | • 缺少 groupChatUserProxy  step 应失败 |
| 建议测试文件 | • packages/node-runner/src/executors/group-chat-helpers.test.ts |
| 文件所有权 | • packages/node-runner/src/executors/group-chat-helpers.ts<br>• packages/node-runner/src/executors/group-chat-helpers.test.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e group-chat |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/group-chat-helpers.test.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/group-chat-helpers.test.ts`
- 失败摘要（预期内）：4 failed — `buildGroupChatUserProxyStep` / `buildGroupChatTurnStep` / `validateGroupChatAgentSteps` 等函数尚未实现（`is not a function`）

### Green

- 实现文件：`packages/node-runner/src/executors/group-chat-helpers.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/group-chat-helpers.test.ts`
- 通过摘要：4 passed — `buildGroupChatUserProxyStep` 创建审计记录；`toGroupChatAgentStepRecords` 包装为 `agent_step`；`validateGroupChatAgentSteps` 在缺少 `groupChatUserProxy` 时抛 `AwfError(E1047)`

### Refactor

- 变更说明：无额外重构；helper 函数按 discriminated step type 拆分 builder + validator

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test -- src/executors/group-chat-helpers.test.ts` | 4 passed | 是 |
| 受影响模块测试 | `pnpm --filter @rxwf/node-runner test -- src/executors/group-chat` | 17 passed（3 files） | 是 |
| linter/build | `pnpm --filter @rxwf/node-runner build` | 既有无关 TS 错误（rag.test.ts 等）；本 task 变更文件无 lint 问题 | 是（变更范围） |
| 冒烟（若适用） | E2E 属 T-094/T-095 所有权，本 task 仅 helper 层 | 跳过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/group-chat-helpers.ts`
- `packages/node-runner/src/executors/group-chat-helpers.test.ts`
- `docs/workflow/tasks/T-092.md`

## 实现说明

在 `group-chat-helpers.ts` 新增 Group Chat `agentSteps` 审计事件 builder（`buildGroupChatTurnStep` / `buildGroupChatUserProxyStep` / `buildGroupChatFinishStep`）、`toGroupChatAgentStepRecords` 转换器，以及 `validateGroupChatAgentSteps` 校验器——当 `userProxyRounds` 指定轮次缺少 `groupChatUserProxy` 步骤时抛 `E1047`，供执行器与 timeline 消费。

## 未解决问题

- `group-chat.ts` 尚未调用新 helper 在 UserProxy 暂停时写入 `groupChatUserProxy` 审计步骤（超出本 task 文件所有权，待后续 task 接线）
