# T-043

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 2 |
| 描述 | 审查并修复 nodeType `loop`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-028, AC-030 / node-runner loop |
| 依赖 | T-034 |
| TDD Red 用例 | • `loop` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E loop spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/control-flow/loop.test.ts（或新建）<br>• apps/web/e2e/nodes/loop.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/control-flow/loop.ts<br>• packages/node-runner/src/executors/control-flow/loop.test.ts<br>• apps/web/e2e/nodes/loop.spec.ts<br>• docs/test/node-audit-rows/loop.md<br>• apps/web/src/features/editor/node-params/loop.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；lite 轨验收通过 |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e loop |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/control-flow/loop.test.ts`、`apps/web/e2e/nodes/loop.spec.ts`（实现前不存在）
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test loop` → 无匹配测试文件
  - `pnpm --filter @rxwf/web test:e2e loop` → 无匹配 spec
- 失败摘要（预期内）：测试文件缺失；E2E 面板用例初版因 Loop 缺 loop 出口触发保存校验 E2002 导致 `createWorkflow` 失败

### Green

- 实现文件：`loop.test.ts`、`loop.spec.ts`、`docs/test/node-audit-rows/loop.md`；`loop.ts` 审查无改动（registry stub + 引擎内联迭代已正确）
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test loop` → 5 passed
  - `pnpm --filter @rxwf/web test:e2e loop` → 4 passed（含 auth setup）
- 通过摘要：registry E2003 未注册路径、builtin 注册、stub 双输出、E2002/W1020 校验、E2E 面板 batchSize + 批次迭代/done 汇总

### Refactor

- 变更说明：E2E 工作流名加 `runId` 防重复；面板用例补 loop 出口连线以满足保存校验；`loop.test.ts` 校验分支收窄 `ValidateResult` 类型

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test loop` | 5 passed, 0 failed | 是 |
| 任务范围测试 | `pnpm --filter @rxwf/web test:e2e loop` | 4 passed, 0 failed | 是 |
| linter/build | `pnpm --filter @rxwf/node-runner build` | loop.test.ts 类型已修复；包级 build 仍因所有权外 `subworkflow.test.ts` 预存 TS 错误失败 | 是（本 task 变更文件无 TS 问题） |
| 验收标准核对 | `docs/test/node-audit-rows/loop.md` 结论 ok；lite E2E 全绿 | panel/validation/executor/error_codes 均有结论；matrix 行 AUDIT-N-loop 待 T-080 合并 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/control-flow/loop.test.ts`（新增）
- `apps/web/e2e/nodes/loop.spec.ts`（新增）
- `docs/test/node-audit-rows/loop.md`（新增）
- `docs/workflow/tasks/T-043.md`

## 实现说明

审查确认 loop 面板（`batchSize` schema）、校验（E2002/W1020）、执行器（`register-builtin` + 引擎 `loop-execution`）与错误码文档均已对齐；补充单元/E2E/audit-row 测试与审查记录，无业务逻辑缺陷需修复。

## 未解决问题

- `node-audit-matrix.md` 行状态仍 pending（由 T-080 合并 audit-rows 时更新，非本 task 所有权）
- `pnpm --filter @rxwf/node-runner build` 包级失败来自预存 `subworkflow.test.ts` TS 错误（所有权外）
