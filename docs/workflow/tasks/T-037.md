# T-037

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 2 |
| 描述 | 审查并修复 nodeType `scheduleTrigger`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-028, AC-030 / node-runner scheduleTrigger |
| 依赖 | T-034 |
| TDD Red 用例 | • `scheduleTrigger` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E scheduleTrigger spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/triggers/schedule.test.ts（或新建）<br>• apps/web/e2e/nodes/scheduleTrigger.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/triggers/schedule.ts<br>• packages/node-runner/src/executors/triggers/schedule.test.ts<br>• apps/web/e2e/nodes/scheduleTrigger.spec.ts<br>• docs/test/node-audit-rows/scheduleTrigger.md<br>• apps/web/src/features/editor/node-params/scheduleTrigger.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；lite 轨验收通过 |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e scheduleTrigger |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/triggers/schedule.test.ts`、`apps/web/e2e/nodes/scheduleTrigger.spec.ts`（新建）
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/triggers/schedule.test.ts`
- 失败摘要（预期内）：4 failed — `parseScheduleTriggerOutput is not a function`；executor 输出 `[{ json: {} }]` 而非含 `cron` 的 item

### Green

- 实现文件：`packages/node-runner/src/executors/triggers/schedule.ts`
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test -- src/executors/triggers/schedule.test.ts` → 6 passed
  - `pnpm --filter @rxwf/node-runner test -- src/executors/triggers/` → 全 trigger 套件通过
  - `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e scheduleTrigger` → 2 passed
- 通过摘要：新增 `parseScheduleTriggerOutput` + registry 单测；E2E 面板 Cron 字段与 debug-node 输出 `cron`

### Refactor

- 变更说明：E2E 合并为单测（panel + API）并使用 `page.request` 保持会话；工作流定义简化为单 trigger 节点

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test -- src/executors/triggers/schedule.test.ts` | 6 passed | 是 |
| 任务范围测试 | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e scheduleTrigger` | 2 passed | 是（@any spec；standard 轨需 Docker，本环境不可用） |
| linter/build | `pnpm --filter @rxwf/node-runner test -- src/executors/triggers/` | 全通过 | 是 |
| 验收标准核对 | `docs/test/node-audit-rows/scheduleTrigger.md` 四维结论 + status ok | 审查行已填写 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/triggers/schedule.ts`
- `packages/node-runner/src/executors/triggers/schedule.test.ts`
- `apps/web/e2e/nodes/scheduleTrigger.spec.ts`
- `docs/test/node-audit-rows/scheduleTrigger.md`
- `docs/workflow/tasks/T-037.md`

## 实现说明

审查 `scheduleTrigger`：面板沿用通用 `cron` 字段；执行器补充 `parseScheduleTriggerOutput`（无 input 时输出 `{ cron }` 便于调试/下游）；新增 registry/E2E/audit-row 证据。无专用失败错误码（成功路径不抛错）。

## 未解决问题

- `RXWF_E2E_TRACK=standard` 需 Docker Compose（Postgres/Redis），本机 Docker 未运行，standard 轨 E2E 未实测；spec 标 `@any`，CI/有 Docker 环境可跑 standard 项目
