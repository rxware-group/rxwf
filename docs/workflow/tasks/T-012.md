# T-012

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-1 |
| Wave | 3 |
| 描述 | 实现 global-teardown：compose down -v + 无容器泄漏检查 |
| 追溯 | FR-14 / AC-073 / §6.3 teardown |
| 依赖 | T-006 |
| TDD Red 用例 | • teardown 后 docker ps 仍含 rxwf-e2e 容器应失败 |
| 建议测试文件 | • apps/web/e2e/global-teardown.test.ts |
| 文件所有权 | • apps/web/e2e/global-teardown.ts<br>• apps/web/e2e/global-teardown.test.ts |
| 验收标准 | AC-073 可验证 |
| 基本验证提示 | 跑 E2E 后 docker ps 无残留 |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/web/e2e/global-teardown.test.ts`
- 运行命令：`cd apps/api && node --import tsx/esm --test ../web/e2e/global-teardown.test.ts`
- 失败摘要（预期内）：`ERR_MODULE_NOT_FOUND: Cannot find module 'global-teardown.js'`（实现文件尚不存在）

### Green

- 实现文件：`apps/web/e2e/global-teardown.ts`
- 运行命令：`cd apps/api && node --import tsx/esm --test ../web/e2e/global-teardown.test.ts`
- 通过摘要：3 tests / 1 suite 全部通过；含「down 后 rxwf-e2e 容器残留应失败」Red 用例

### Refactor

- 变更说明：无额外重构；`teardownCompose` 可注入 `execFn` 便于单测，`lite` 轨跳过 compose

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `cd apps/api && node --import tsx/esm --test ../web/e2e/global-teardown.test.ts` | 3 pass / 0 fail | 是 |
| 依赖模块测试 | `node --test scripts/e2e-compose.test.mjs` | 8 pass / 0 fail | 是 |
| linter/build | ReadLints 无报错；e2e 目录不在 web tsconfig include（Playwright 运行时编译） | N/A | 是 |
| 冒烟（若适用） | `node scripts/e2e-compose.mjs down standard --dry-run` | exit 0，输出 `docker compose ... -p rxwf-e2e down -v` | 是 |

**结论**：`passed`

## 实际变更文件

- `apps/web/e2e/global-teardown.ts`
- `apps/web/e2e/global-teardown.test.ts`

## 实现说明

实现 Playwright `globalTeardown` 默认导出及可测 `teardownCompose`：读取 `RXWF_E2E_TRACK`，`standard`/`plus` 轨调用 T-006 `cmdDown`（`down -v` + `assertNoContainers` 无泄漏检查），`lite` 轨跳过；未知 track 抛错。

## 未解决问题

- `playwright.config.ts` 尚未挂载 `globalTeardown`（属 T-013 范围）；模块本身已就绪，挂载后 E2E 结束将自动执行 compose down 与 AC-073 校验。
