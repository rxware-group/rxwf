# T-081

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 5 |
| 描述 | 更新 e2e-coverage-matrix 全部 nodeType 行 |
| 追溯 | FR-18 / AC-029 / E2E matrix |
| 依赖 | T-079 |
| TDD Red 用例 | • nodeType 行 status !== covered 应失败 |
| 建议测试文件 | • scripts/validate-e2e-matrix.test.mjs |
| 文件所有权 | • docs/test/e2e-coverage-matrix.md |
| 验收标准 | AC-029 green |
| 基本验证提示 | node scripts/validate-e2e-matrix.mjs --nodes |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`scripts/validate-e2e-matrix.mjs`（`--nodes`；所有权不含独立 test 文件，对齐 T-032/T-004 内嵌校验模式）
- 运行命令：`node scripts/validate-e2e-matrix.mjs --nodes`
- 失败摘要（预期内）：exit 1 — 44 条 `E2E-N-*` 行 `status !== covered`（仅 E2E-N-skillRun、E2E-N-switch 已 covered）

### Green

- 实现文件：`docs/test/e2e-coverage-matrix.md`
- 运行命令：
  - `node scripts/validate-e2e-matrix.mjs --nodes` → OK: 65 rows (46 nodeType + 19 platform capabilities)
  - `node scripts/validate-e2e-matrix.mjs --test` → 2 passed, 0 failed
- 通过摘要：46 条 nodeType 行均登记 `e2e_spec` 且 `status=covered`；`E2E-N-switch` 由 M-3 `nodes/switch.spec.ts` 收口（替代 M-2 `switch-dynamic.spec.ts`）

### Refactor

- 变更说明：无（纯矩阵文档同步）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `node scripts/validate-e2e-matrix.mjs --nodes` | OK: 65 rows (46 nodeType + 19 platform) | 是（AC-029：全部 nodeType 行 covered 且关联 spec） |
| 验收标准核对 | 脚本校验 46 node spec 文件存在 | All 46 node specs exist | 是 |
| linter/build | `node scripts/validate-e2e-matrix.mjs --test` | 2 passed, 0 failed | 是 |
| 冒烟（若适用） | `node scripts/validate-e2e-matrix.mjs` | OK: 65 rows（平台行仍 uncovered，符合 M-3 范围） | 是 |

**结论**：`passed`

## 实际变更文件

- `docs/test/e2e-coverage-matrix.md`（46 条 E2E-N-* 行 → covered + e2e_spec）
- `docs/workflow/tasks/T-081.md`

## 实现说明

将 M-3 节点审查产出的 `apps/web/e2e/nodes/*.spec.ts`（及既有 `skill-run-tools.spec.ts`）登记至覆盖矩阵，使 `--nodes` 校验 green，满足 AC-029。

## 未解决问题

-
