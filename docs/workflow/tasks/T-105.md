# T-105

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-5 |
| Wave | 3 |
| 描述 | BinaryBlobService + execution_blobs 存储 |
| 追溯 | FR-11 / AC-051 / §9 Binary |
| 依赖 | T-103 |
| TDD Red 用例 | • B-6 未通过前实现测试必须失败 |
| 建议测试文件 | • packages/shared/src/binary-blob-service.test.ts |
| 文件所有权 | • packages/shared/src/binary-blob-service.ts<br>• packages/providers-lite/src/blob-repository.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | pnpm --filter @rxwf/shared test; pnpm --filter @rxwf/web test:e2e binary |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/shared/src/binary-blob-service.test.ts`；`packages/providers/lite/src/blob-repository.test.ts`
- 运行命令：`pnpm --filter @rxwf/shared test src/binary-blob-service.test.ts`
- 失败摘要（预期内）：`Cannot find module './binary-blob-service.js'`（模块未实现）

### Green

- 实现文件：`packages/shared/src/binary-blob-service.ts`；`packages/providers/lite/src/blob-repository.ts`
- 运行命令：`pnpm --filter @rxwf/shared test src/binary-blob-service.test.ts`；`pnpm --filter @rxwf/providers-lite test src/blob-repository.test.ts`
- 通过摘要：shared 7/7 pass（256 KiB inline、32 MiB E2002、hydrate/externalize、env 阈值、Standard 规划）；lite 3/3 pass（execution_blobs CRUD + BlobStore 端到端）

### Refactor

- 变更说明：`createLiteBlobStore` 组合文件存储与 `createLiteBlobRepository`；共享层 `createBinaryBlobService` 统一 hydrate/externalize 与阈值 enforcement

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/shared test src/binary-blob-service.test.ts` | 7/7 pass | 是 |
| 任务范围测试 | `pnpm --filter @rxwf/providers-lite test src/blob-repository.test.ts` | 3/3 pass | 是 |
| linter/build | `pnpm --filter @rxwf/providers-lite build` | pass | 是 |
| linter/build | `pnpm --filter @rxwf/shared build` | fail：`workflow-item.test.ts` 预存 TS 错误（非本 task 所有权） | 是（本 task 源文件无 TS 错误） |
| 冒烟（若适用） | `pnpm --filter @rxwf/web test:e2e binary` | 无 binary E2E spec（T-111/T-112） | 是（本 task 为内核层） |

**结论**：`passed`

## 实际变更文件

- `packages/shared/src/binary-blob-service.ts`（新增）
- `packages/shared/src/binary-blob-service.test.ts`（新增）
- `packages/providers/lite/src/blob-repository.ts`（新增）
- `packages/providers/lite/src/blob-repository.test.ts`（新增）

## 实现说明

共享层 `BinaryBlobService` 封装 store/load + hydrate/externalize，OPT-01 默认 256 KiB inline / 32 MiB Item 上限（`E2002`），支持 `RXWF_BINARY_INLINE_MAX_BYTES` env；Lite `blob-repository` 实现 ADR-005 §2.5 `execution_blobs` 元数据 CRUD 与 `createLiteBlobStore` 文件后端；`STANDARD_BINARY_BLOB_PLAN` 记录 GAP-02 Standard 轨规划。

## 未解决问题

- `createLiteBinaryBlobService`（lite 既有）尚未迁移至 `blob-repository`（非本 task 所有权）；`index.ts` 未导出 `createBinaryBlobService`（后续 wiring task）
