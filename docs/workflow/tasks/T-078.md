# T-078

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `toolShell`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner toolShell |
| 依赖 | T-034 |
| TDD Red 用例 | • `toolShell` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E toolShell spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/agent-satellite-tools.test.ts（或新建）<br>• apps/web/e2e/nodes/toolShell.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/agent-satellite-tools.ts<br>• packages/node-runner/src/executors/agent-satellite-tools.test.ts<br>• apps/web/e2e/nodes/toolShell.spec.ts<br>• docs/test/node-audit-rows/toolShell.md<br>• apps/web/src/features/editor/node-params/toolShell.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e toolShell |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/agent-satellite-tools.test.ts`、`apps/web/e2e/nodes/toolShell.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner exec vitest run agent-satellite-tools.test.ts -t "toolShell M-3 audit row"`（satellite/audit row 块创建前）；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e toolShell --retries=0`（spec/audit row 创建前）
- 失败摘要（预期内）：单测 `toolShell M-3 audit row` → describe 块不存在 / `existsSync(toolShellAuditRowPath)` false；E2E audit row → `ENOENT: docs/test/node-audit-rows/toolShell.md`

### Green

- 实现文件：审查确认 `agent-satellite-tools.ts` 已有 toolShell 注册/调用；补全 `agent-satellite-tools.test.ts` toolShell satellite + audit row + build/invoke 块、`toolShell.spec.ts`、`toolShell.md` audit row
- 运行命令：`pnpm --filter @rxwf/node-runner exec vitest run agent-satellite-tools.test.ts -t "toolShell|shell source|E1057|delegates shell"`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9971 RXWF_E2E_WEB_PORT=9411 pnpm --filter @rxwf/web test:e2e toolShell --retries=0`
- 通过摘要：8/8 toolShell 相关单测（satellite E2003 + audit row + 注册/E1057/delegate）；3/3 lite E2E（audit row + 面板 Tool 描述/cwd）

### Refactor

- 变更说明：无（实现已就绪，仅补审查单测/E2E/audit row）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner exec vitest run agent-satellite-tools.test.ts -t "toolShell\|shell source\|E1057\|delegates shell"` | 8 passed | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9971 RXWF_E2E_WEB_PORT=9411 pnpm --filter @rxwf/web test:e2e toolShell --retries=0` | 3 passed（audit row + 面板） | 是 |
| E2E plus/@plus | `RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e toolShell --retries=0` | global-setup 失败：`docker-compose.plus.yml` api 服务无 image/build | 否（本机 Docker compose 配置） |
| 验收标准核对 | `docs/test/node-audit-rows/toolShell.md` 四维 ok + executor satellite + status ok；SATELLITE 注册/E1057/E2003 单测；E2E spec 含 `@plus` E3010 用例 | 代码就绪；plus 轨需 CI Docker 修复后跑通 | 部分 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/agent-satellite-tools.test.ts`
- `apps/web/e2e/nodes/toolShell.spec.ts`
- `docs/test/node-audit-rows/toolShell.md`
- `docs/workflow/tasks/T-078.md`

## 实现说明

审查结论：面板（`node-param-schemas` cwd + `BuiltinSatelliteToolDescription`）、校验（固定能力内置描述）、卫星执行器（`buildAgentToolDefinitions`/`invokeAgentTool` shell 路径）、错误码 E1057/E2002/E3012 均已就绪。Embedded v1 按设计返回 E1057；Agent Runner 经 gateway 委派。未新增独立 `toolShell.tsx`（通用面板已覆盖）。补全与其他卫星节点一致的 `toolShell satellite`（E2003）与 `toolShell M-3 audit row` 单测块。

## 未解决问题

- 本机 `RXWF_E2E_TRACK=plus` global-setup 因 `deploy/docker-compose.plus.yml` api 服务缺少 image/build 失败；`@plus` debug-node E3010 用例待 CI Docker 修复后验证
