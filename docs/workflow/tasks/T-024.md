# T-024

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 1 |
| 描述 | 工作流 ACL：identity workflow-access API 路由 |
| 追溯 | FR-05 / AC-014 / packages/identity ACL |
| 依赖 | - |
| TDD Red 用例 | • Viewer 调用 DELETE workflow 应 403<br>• Owner 可 share |
| 建议测试文件 | • packages/identity/src/workflow-access-service.test.ts<br>• apps/api/src/routes/workflow-collaborators.test.ts |
| 文件所有权 | • apps/api/src/routes/workflow-collaborators.ts<br>• apps/api/src/routes/workflow-collaborators.test.ts |
| 验收标准 | Owner/Editor/Viewer API green |
| 基本验证提示 | pnpm --filter @rxwf/api test collaborators |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/api/src/routes/workflow-collaborators.test.ts`
- 运行命令：`pnpm --filter @rxwf/api test collaborators`
- 失败摘要（预期内）：`Cannot find module './workflow-collaborators.js'` — 模块尚未创建

### Green

- 实现文件：`apps/api/src/routes/workflow-collaborators.ts`
- 运行命令：`pnpm --filter @rxwf/api test collaborators`
- 通过摘要：4 tests passed — owner share、viewer GET/PUT 403、editor share、viewer DELETE 403

### Refactor

- 变更说明：GET `/collaborators` 使用 `withViewAccess`（非 `withShareAccess`），使 Viewer 可只读查看协作者列表，符合 UX 规格 §7.2

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/api test collaborators` | 4 passed | 是 |
| linter/build | `pnpm --filter @rxwf/api run build` | 本文件无 lint 错误；全量 build 因 `knowledge-bases.ts` 既有 `KnowledgeBaseRole` 导出问题失败（非本任务范围） | 是（本任务文件） |
| 冒烟（若适用） | — | N/A | 是 |

**结论**：`passed`

## 实际变更文件

- `apps/api/src/routes/workflow-collaborators.ts`（新增）
- `apps/api/src/routes/workflow-collaborators.test.ts`（新增）

## 实现说明

提取工作流协作者 ACL 路由为独立模块 `registerWorkflowCollaboratorRoutes`，提供 GET/PUT `/api/workflows/:workflowId/collaborators` 与 GET `.../candidates`；基于 `WorkflowAccessService` 做权限校验。编排器后续需在 `bootstrap.ts` 注册并自 `workflows.ts` 移除重复路由。

## 未解决问题

- `bootstrap.ts` / `workflows.ts` 尚未接入本模块（并行任务文件所有权外）；当前生产路径仍走 `workflows.ts` 内联路由
