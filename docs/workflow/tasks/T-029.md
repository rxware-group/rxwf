# T-029

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 2 |
| 描述 | M-2 E2E：工作流 ACL 场景 |
| 追溯 | FR-13 / AC-018 / E2E Standard |
| 依赖 | T-024, T-025 |
| TDD Red 用例 | • Viewer 编辑工作流 E2E 应被阻止 |
| 建议测试文件 | • apps/web/e2e/workflow-acl.spec.ts |
| 文件所有权 | • apps/web/e2e/workflow-acl.spec.ts |
| 验收标准 | matrix ACL 行 covered |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e workflow-acl |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/web/e2e/workflow-acl.spec.ts`
- 运行命令：`pnpm --filter @rxwf/web test:e2e workflow-acl`
- 失败摘要（预期内）：首次运行 viewer 编辑用例在 owner GET 响应解析处失败（`ownerBody.definition` 为 undefined；API 返回 `{ workflow: { ... } }`）；ACL 拒绝保存断言（`无权修改`）已通过

### Green

- 实现文件：`apps/web/e2e/workflow-acl.spec.ts`（E2E 规格；ACL 行为由 T-024/T-025 与既有 API/UI 提供）
- 运行命令：`pnpm --filter @rxwf/web test:e2e workflow-acl`
- 通过摘要：8 passed（standard-chromium + plus-chromium × 3 用例 + setup）

### Refactor

- 变更说明：修正 GET workflow 响应体为 `workflow.definition`；移除未使用 import

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/web test:e2e workflow-acl` | 8 passed | 是（E2E-P-018：Viewer 编辑被拒、协作者 PUT 403、协作面板只读） |
| linter/build | `ReadLints apps/web/e2e/workflow-acl.spec.ts` | 无 lint 错误 | 是 |
| 冒烟（若适用） | 同上 E2E | 3 ACL 场景 green | 是 |

**结论**：`passed`

## 实际变更文件

- `apps/web/e2e/workflow-acl.spec.ts`（新增）
- `docs/workflow/tasks/T-029.md`

## 实现说明

新增 `workflow-acl.spec.ts`（`@standard @any`）：Admin 创建 Viewer 并分享工作流后，验证 Viewer 画布编辑触发自动保存失败（`无权修改`）且服务端 definition 未变；Viewer 协作者 PUT 返回 403/E4003；设置→协作面板无「添加协作者」按钮。

## 未解决问题

- Lite track 因 `grepInvert @standard` 不运行本 spec（matrix 行 E2E-P-018 为 standard）；编排器更新 `e2e-coverage-matrix.md` 由 T-032 负责
