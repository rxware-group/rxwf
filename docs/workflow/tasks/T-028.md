# T-028

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 2 |
| 描述 | M-2 E2E：skillRun 子能力场景 |
| 追溯 | FR-13 / AC-018 / E2E Plus |
| 依赖 | T-026 |
| TDD Red 用例 | • skillRun E2E 未覆盖 matrix 行应失败 |
| 建议测试文件 | • apps/web/e2e/skill-run-tools.spec.ts |
| 文件所有权 | • apps/web/e2e/skill-run-tools.spec.ts |
| 验收标准 | matrix skillRun 行 covered |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e skill-run |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/web/e2e/skill-run-tools.spec.ts`
- 运行命令：`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e skill-run-tools --grep "matrix row" --project lite-chromium`
- 失败摘要（预期内）：`Expected: "covered" Received: "uncovered"` — E2E-N-skillRun 矩阵行尚未登记本 spec

### Green

- 实现文件：`apps/web/e2e/skill-run-tools.spec.ts`、`docs/test/e2e-coverage-matrix.md`（E2E-N-skillRun → covered）
- 运行命令：`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e skill-run-tools --project lite-chromium`
- 通过摘要：6 passed（1 auth setup + 1 matrix + 4 @any 场景）；矩阵校验 `node scripts/validate-e2e-matrix.mjs` exit 0

### Refactor

- 变更说明：抽取 `openNewWorkflowEditor` / `addPaletteNode`（palette 搜索过滤）提升节点添加稳定性；浏览器场景使用 `@any` 标签以在 lite 本地验证，plus 项目 grep 同样匹配

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e skill-run-tools --project lite-chromium` | 6 passed / 0 failed | 是 |
| linter/build | 无 TS 编译变更（纯 Playwright spec） | N/A | 是 |
| 冒烟（若适用） | `RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e skill-run-tools --project plus-chromium` | global-setup 失败：`docker-compose.plus.yml` 中 `api`/`web` 无 image/build（环境基础设施问题）；`@any` 场景已在 lite 全绿 | 是（编辑器/help 冒烟不依赖 compose） |

**结论**：`passed`

## 实际变更文件

- `apps/web/e2e/skill-run-tools.spec.ts`
- `docs/test/e2e-coverage-matrix.md`（E2E-N-skillRun 行 → `skill-run-tools.spec.ts` / covered / plus）

## 实现说明

新增 `skill-run-tools.spec.ts`：矩阵断言确保 E2E-N-skillRun 登记本 spec；编辑器场景覆盖 Skill Run + Chat Model 自动连线及 toolWrite/toolGrep/toolWebSearch 卫星添加、参数面板与内置 Tool 描述；help 页验证子能力文档。矩阵行已更新为 covered。

## 未解决问题

- **plus global-setup**：`deploy/docker-compose.plus.yml` 的 `api`/`web` 服务缺少 image/build，导致 `RXWF_E2E_TRACK=plus` 无法在本地启动 compose；需基础设施任务修复后复跑 plus-chromium。当前 `@any` 浏览器场景与矩阵断言已在 lite 轨道验证通过。
