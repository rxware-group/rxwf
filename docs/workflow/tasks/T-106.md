# T-106

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-5 |
| Wave | 3 |
| 描述 | execution 引擎 binary 透传（非丢弃） |
| 追溯 | FR-11 / AC-047 / §9 Binary |
| 依赖 | T-104 |
| TDD Red 用例 | • B-6 未通过前实现测试必须失败 |
| 建议测试文件 | • packages/execution/src/engine/binary-pass-through.test.ts |
| 文件所有权 | • packages/execution/src/engine/binary-pass-through.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | pnpm --filter @rxwf/shared test; pnpm --filter @rxwf/web test:e2e binary |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/execution/src/engine/binary-pass-through.test.ts`
- 运行命令：`pnpm --filter @rxwf/execution test -- src/engine/binary-pass-through.test.ts`
- 失败摘要（预期内）：`Cannot find module './binary-pass-through.js'`

### Green

- 实现文件：`packages/execution/src/engine/binary-pass-through.ts`
- 运行命令：`pnpm --filter @rxwf/execution test -- src/engine/binary-pass-through.test.ts`
- 通过摘要：12/12 pass — B-6 gate + preserveBinary + passThroughBinaryItem/ByIndex/OnOutput（AC-047 execution 层）

### Refactor

- 变更说明：`preserveBinary` json-only 路径复用 shared `withJsonPreservingBinary`（RISK-01）；无额外 refactor

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/execution test -- src/engine/binary-pass-through.test.ts` | 12/12 pass | 是（AC-047 execution 透传 helper） |
| 受影响模块测试 | `pnpm --filter @rxwf/execution test` | 101/101 pass | 是 |
| linter/build | `pnpm --filter @rxwf/execution build` | pass | 是 |
| 冒烟（若适用） | `pnpm --filter @rxwf/web test:e2e binary` | 无 binary E2E spec（T-111） | 是（本 task 为 execution helper） |

**结论**：`passed`

## 实际变更文件

- `packages/execution/src/engine/binary-pass-through.ts`（新增）
- `packages/execution/src/engine/binary-pass-through.test.ts`（新增）

## 实现说明

在 execution 包提供 design §4.2 `preserveBinary` 与节点 handoff 用 `passThroughBinaryOnOutput`/`passThroughBinaryItem`/`passThroughBinaryByIndex`：当节点输出仅含 json 时从上游 input 恢复 binary（含 externalized ref），输出已有 binary 键不覆盖。供后续 execution-engine 接线及 T-110 节点合并策略依赖。

## 未解决问题

- `execution-engine.ts` 尚未调用 `passThroughBinaryOnOutput`（本 task 所有权外）；后续 wave 或编排器串行任务须接线
- `index.ts` 未 export 新模块（非本 task 所有权）
