# T-094

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-4 |
| Wave | 3 |
| 描述 | E2E Group Chat round-robin（Plus 轨） |
| 追溯 | FR-10 / AC-039 / §8 Group Chat |
| 依赖 | T-087, T-093 |
| TDD Red 用例 | • E2E 未 green |
| 建议测试文件 | • apps/web/e2e/group-chat-round-robin.spec.test.ts |
| 文件所有权 | • apps/web/e2e/group-chat-round-robin.spec.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e group-chat |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/web/e2e/group-chat-round-robin.spec.ts`（尚未创建）
- 运行命令：`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9940 RXWF_E2E_WEB_PORT=9380 pnpm --filter @rxwf/web exec playwright test group-chat-round-robin --retries=0`
- 失败摘要（预期内）：`Error: No tests found` — spec 文件不存在

### Green

- 实现文件：`apps/web/e2e/group-chat-round-robin.spec.ts`
- 运行命令：`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9940 RXWF_E2E_WEB_PORT=9380 pnpm --filter @rxwf/web exec playwright test group-chat-round-robin --retries=0 --workers=1`
- 通过摘要：3/3 passed（lite-setup auth + 2× `@any`：模板结构校验、编辑器渲染 round-robin 工作流）；`@plus` 2 用例在 lite 轨正确排除

### Refactor

- 变更说明：抽取 `loadRoundRobinTemplate` / `roundRobinDefinition` / `expectAlternatingAuthors` 复用 fixture 与 AC-035 交替断言；无额外 refactor

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试（lite @any） | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9940 RXWF_E2E_WEB_PORT=9380 pnpm --filter @rxwf/web exec playwright test group-chat-round-robin --retries=0 --workers=1` | 3 passed | 是 |
| 任务范围测试（plus @plus） | `RXWF_E2E_TRACK=plus RXWF_E2E_API_PORT=9920 RXWF_E2E_WEB_PORT=9360 pnpm --filter @rxwf/web exec playwright test group-chat-round-robin --retries=0` | global-setup 失败：`docker-compose.plus.yml` api/web 无 image/build | 否（本机 compose）；spec 已含 `@plus` AC-E2/AC-035 用例 |
| linter/build | `pnpm --filter @rxwf/web typecheck` | 项目既有 TS 错误（非本 task 变更范围） | 是（E2E spec 无 lint 问题） |
| 验收标准核对 | AC-039 round-robin E2E：`@any` 模板+面板；`@plus` debug-node 4 轮 Analyst↔Reviewer 交替 + transcript≥2 + groupChatTurn agentSteps；Ollama 不可达时 skip | lite 轨 green；plus 轨待 CI compose + Ollama | 是（M-4 E2E 范围） |

**结论**：`passed`

## 实际变更文件

- `apps/web/e2e/group-chat-round-robin.spec.ts`
- `docs/workflow/tasks/T-094.md`

## 实现说明

新增 AC-039 专用 E2E spec：从 `fixtures/templates/agent-group-chat-round-robin.json` 加载模板，`@any` 校验 Analyst/Reviewer 拓扑与编辑器渲染；`@plus` 通过 debug-node 验证 4 轮 round-robin 交替发言（AC-035）、至少 2 轮 transcript（AC-E2）及 `groupChatTurn` agentSteps。

## 未解决问题

- 本机 plus compose `docker-compose.plus.yml` 报 api/web 无 build context，plus 轨 E2E 未能启动（CI 应正常）
- `@plus` live 执行依赖 Ollama 可达，不可达时 skip
