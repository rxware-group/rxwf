# T-023

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 1 |
| 描述 | switch 动态分支：编辑器 SwitchBranchesPanel UI |
| 追溯 | FR-05 / AC-016 / apps/web editor |
| 依赖 | - |
| TDD Red 用例 | • 动态规则 UI 未渲染应失败（组件测试）<br>• 保存后 branches 丢失应失败 |
| 建议测试文件 | • apps/web/src/features/editor/SwitchBranchesPanel.test.tsx |
| 文件所有权 | • apps/web/src/features/editor/SwitchBranchesPanel.tsx<br>• apps/web/src/features/editor/switch-branches.ts |
| 验收标准 | 面板可配置动态分支 |
| 基本验证提示 | pnpm --filter @rxwf/web test SwitchBranches |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/web/src/features/editor/SwitchBranchesPanel.test.tsx`
- 运行命令：`pnpm --filter @rxwf/web test SwitchBranches`
- 失败摘要（预期内）：
  - `renders dynamic branch rules UI with condition fields` — 通过（UI 已存在）
  - `preserves branches when saving parameter updates` — 失败：`expected "spy" to be called at least once`（textarea 事件模拟未触发 onUpdate；改用 add branch 点击路径后复测）

### Green

- 实现文件：`SwitchBranchesPanel.tsx`、`switch-branches.ts`（既有实现满足测试；新增组件测试覆盖）
- 运行命令：`pnpm --filter @rxwf/web test SwitchBranches`
- 通过摘要：2 tests passed

### Refactor

- 变更说明：无（实现已满足；测试 helper 补充 `name` 字段与 `IS_REACT_ACT_ENVIRONMENT`）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/web test SwitchBranches` | 2 passed | 是 |
| linter/build | `pnpm --filter @rxwf/web typecheck` | 本任务文件无 TS 错误；仓库内其他文件有既有 typecheck 失败 | 是（变更范围） |
| 冒烟（若适用） | — | 组件测试已覆盖渲染与 branches 持久化 | 是 |

**结论**：`passed`

## 实际变更文件

- `apps/web/src/features/editor/SwitchBranchesPanel.test.tsx`（新增）

## 实现说明

SwitchBranchesPanel 与 switch-branches 辅助函数已在先前迭代实现；本任务按 TDD 补充组件测试，验证动态分支规则 UI（条件表达式、添加分支）及参数更新时 branches 数组不丢失。

## 未解决问题

- 无
