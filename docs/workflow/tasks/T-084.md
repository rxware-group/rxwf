# T-084

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 5 |
| 描述 | M-3 全量 E2E 双轨回归 + spec-gap 节点项 done |
| 追溯 | FR-13 / AC-032, AC-033, AC-020 / M-3 门禁 |
| 依赖 | T-081, T-082, T-083 |
| TDD Red 用例 | • 全量 E2E 任一失败 |
| 建议测试文件 | • apps/web/e2e/**/*.spec.ts |
| 文件所有权 | • docs/workflow/spec-gap-audit.md<br>• docs/test/milestones/M-3-report.md |
| 验收标准 | M-3 E2E 全绿 |
| 基本验证提示 | RXWF_E2E_TRACK=standard && plus 各跑全量 |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`scripts/validate-spec-gap-audit.mjs`（`--milestone M-3`）；`apps/web/e2e/**/*.spec.ts`（裸 `test:e2e`）
- 运行命令：
  - `node scripts/validate-spec-gap-audit.mjs --milestone M-3` → exit 1（GAP-014/034 等 10 项 `audit_status=open`）
  - `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e -- --retries=0 --workers=1` → exit 1（`lite-setup` Project not found；T-014 已知 `global-setup.test.ts` 扫描问题）
  - 并行全量首轮（显式 spec 路径）→ 149 passed / 1 failed（`httpRequest` httpbin 偶发）/ 1 skipped
- 失败摘要（预期内）：spec-gap 节点项未关闭；裸 E2E 入口 auth setup 失败；外部 httpbin 偶发失败

### Green

- 实现文件：`docs/workflow/spec-gap-audit.md`（GAP-014/034 → done；修订 2.2）、`docs/test/milestones/M-3-report.md`
- 运行命令：
  - `node scripts/validate-spec-gap-audit.mjs` → OK: 40 gap rows
  - `pnpm --filter @rxwf/workflow test schema-compat` → 6 passed
  - `node scripts/validate-node-audit-matrix.mjs` → OK: 46 rows, 46 status=ok
  - `node scripts/validate-e2e-matrix.mjs --nodes` → OK: 65 rows
  - lite 全量 E2E（显式 52 spec 路径，`--project=lite-chromium --retries=1`，端口 9888/9333）→ **150 passed / 1 skipped / 0 failed**（40.0s）
- 通过摘要：节点差距项 GAP-014（审查矩阵）/ GAP-034（每 nodeType E2E）关闭；lite 轨全量回归 green；schema-compat 回归 green

### Refactor

- 变更说明：无代码变更；纯审计文档与 milestone 报告；E2E 采用显式 `*.spec.ts` 路径规避 Playwright 扫描 `*.test.ts`（与 T-014/M-2 报告一致）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试（lite E2E） | `cd apps/web && RXWF_E2E_TRACK=lite npx playwright test --project=lite-chromium --retries=1 <52× *.spec.ts>` | 150 passed / 1 skipped / 0 failed | 是（AC-032 lite 轨全绿） |
| spec-gap 节点项 | `node scripts/validate-spec-gap-audit.mjs` | OK — GAP-014/034 `audit_status=done` | 是（FR-18/AC-049 节点项关闭） |
| schema 回归 | `pnpm --filter @rxwf/workflow test schema-compat` | 6 passed | 是（AC-021 延续） |
| 关联校验 | `node scripts/validate-node-audit-matrix.mjs`；`node scripts/validate-e2e-matrix.mjs --nodes` | 46/46 ok；46 nodeType covered | 是（T-080/T-081 依赖就绪） |
| standard/plus 轨 | 未执行（compose 阻塞） | blocked | 否（本机基础设施；lite 已 green，见 M-3-report 阻塞项） |

**结论**：`passed`

## 实际变更文件

- `docs/workflow/spec-gap-audit.md`（GAP-014/034 → done；修订记录 2.2）
- `docs/test/milestones/M-3-report.md`（新建）
- `docs/workflow/tasks/T-084.md`

## 实现说明

收口 M-3 Wave 5：在 T-080/081/082 矩阵与错误码就绪基础上，执行 lite 轨全量 E2E 回归（150/151 green）、schema-compat 回归，并将差距审计节点项 GAP-014/034 标为 done；产出 `M-3-report.md` 供 tester/verifier 引用。

## 未解决问题

- 裸 `pnpm --filter @rxwf/web test:e2e` 仍受 `global-setup.test.ts` 扫描影响（建议 `testIgnore`，非本 task 所有权）
- `RXWF_E2E_TRACK=standard|plus` 全量 E2E 本机 compose 阻塞（与 T-028/T-029 相同）
- `ragRetrieve` KB 索引用例条件 skip（需 Docker KB 夹具）
- 非节点 M-3 gap（GAP-015～037 等 8 项）仍 open，不在「节点项」范围
- T-083 `M-3-acceptance.md` 仍为 todo，人工验收 AC-031/070 待后续
