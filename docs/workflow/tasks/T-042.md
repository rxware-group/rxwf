# T-042

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 2 |
| 描述 | 审查并修复 nodeType `merge`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-028, AC-030 / node-runner merge |
| 依赖 | T-034 |
| TDD Red 用例 | • `merge` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E merge spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/control-flow/merge.test.ts（或新建）<br>• apps/web/e2e/nodes/merge.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/control-flow/merge.ts<br>• packages/node-runner/src/executors/control-flow/merge.test.ts<br>• apps/web/e2e/nodes/merge.spec.ts<br>• docs/test/node-audit-rows/merge.md<br>• apps/web/src/features/editor/node-params/merge.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；lite 轨验收通过 |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e merge |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`merge.test.ts`（新增注册/E2002 用例）、`node-params/merge.test.tsx`（新建）、`e2e/nodes/merge.spec.ts`（新建）
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test merge.test.ts`（新增 3 用例，实现已存在故 Green 同轮通过）
  - `pnpm --filter @rxwf/web test merge.test.tsx`（新建文件，首跑即需 helper 实现）
  - `pnpm --filter @rxwf/web test:e2e merge`（新建 spec 前无 E2E-N-merge 覆盖）
- 失败摘要（预期内）：E2E spec 文件不存在时 lite 轨无 merge 节点验收；面板/校验 helper 缺失时 `merge.test.tsx` 无法 import

### Green

- 实现文件：`merge.ts`（既有执行器，无需改动）、`node-params/merge.tsx`（新建 panel/validation helper）、`e2e/nodes/merge.spec.ts`
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test merge.test.ts` → 9 passed
  - `pnpm --filter @rxwf/web test merge.test.tsx` → 3 passed
  - `RXWF_E2E_API_PORT=9888 RXWF_E2E_WEB_PORT=9333 pnpm --filter @rxwf/web test:e2e merge` → 4 passed
- 通过摘要：执行器注册与三种 mode 行为单测全绿；面板/校验 helper 单测全绿；lite E2E 面板 + append + combineByKey 全绿

### Refactor

- 变更说明：无（执行器实现已满足需求，仅补测试与审查产物）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test merge.test.ts` | 9 passed | 是 |
| 任务范围测试 | `pnpm --filter @rxwf/web test merge.test.tsx` | 3 passed | 是 |
| 冒烟（lite E2E） | `pnpm --filter @rxwf/web test:e2e merge`（alt ports 9888/9333） | 4 passed | 是 |
| 验收标准核对 | `docs/test/node-audit-rows/merge.md` 含 panel/validation/executor/error_codes 结论；E2E-N-merge covered | 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/control-flow/merge.test.ts`
- `apps/web/src/features/editor/node-params/merge.tsx`
- `apps/web/src/features/editor/node-params/merge.test.tsx`
- `apps/web/e2e/nodes/merge.spec.ts`
- `docs/test/node-audit-rows/merge.md`
- `docs/workflow/tasks/T-042.md`

## 实现说明

审查确认 merge 执行器已在 builtin registry 注册且 append/combineByKey/combineAll 行为正确；补充注册/E2002 单测、面板与参数校验 helper（`node-params/merge.tsx`）、lite 轨 E2E 与 `node-audit-rows/merge.md` 审查结论。执行器本体无代码变更。

## 未解决问题

- `shouldShowMergeParamField` / `validateMergeParameters` 尚未接入 `NodeEditorParamsPane` 与 `packages/workflow/src/validate.ts`（非本 task 文件所有权）；矩阵行 status 待 T-080 合并至 `node-audit-matrix.md`。
