# T-073

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `toolSkill`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner toolSkill |
| 依赖 | T-034 |
| TDD Red 用例 | • `toolSkill` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E toolSkill spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/run-skill-tool.test.ts（或新建）<br>• apps/web/e2e/nodes/toolSkill.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/run-skill-tool.ts<br>• packages/node-runner/src/executors/run-skill-tool.test.ts<br>• apps/web/e2e/nodes/toolSkill.spec.ts<br>• docs/test/node-audit-rows/toolSkill.md<br>• apps/web/src/features/editor/node-params/toolSkill.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e toolSkill |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/run-skill-tool.test.ts`、`apps/web/e2e/nodes/toolSkill.spec.ts`
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test -- src/executors/run-skill-tool.test.ts` → 5 passed（registry/E1040/E3001/成功路径已就绪）
  - `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e toolSkill` → debug-node 用例 3/16 失败
- 失败摘要（预期内）：E2E `debug-node fails toolSkill when invoked as standalone node (E2003)` 对仅含孤立 `toolSkill` 节点的工作流调用 `/api/workflows/debug-node` 时 `res.ok()` 为 false（HTTP 非 2xx），与 toolRead/toolHttp 等卫星 spec 模式不一致

### Green

- 实现文件：`apps/web/e2e/nodes/toolSkill.spec.ts`（新增 `standaloneToolSkillWithTriggerDefinition`：manualTrigger → toolSkill 连线；debug-node 断言 `errorCode === 'E2003'`）；`docs/test/node-audit-rows/toolSkill.md`（审查结论）；`run-skill-tool.ts` / `run-skill-tool.test.ts`（审查确认无需改动）
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test -- src/executors/run-skill-tool.test.ts` → 5 passed
  - `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9941 RXWF_E2E_WEB_PORT=9381 pnpm --filter @rxwf/web test:e2e toolSkill` → 5 passed（audit + @any 面板/E2003/validate）
- 通过摘要：卫星 registry E2003；runSkillTool E3001/E1040/executeSkill 成功路径；audit row 四维 ok；E2E 面板 Tool 描述/Skill 名称/模式；Agent 工作流 validate

### Refactor

- 变更说明：E2E spec 对齐 toolRead/toolHttp 卫星 debug-node 模式（manualTrigger 上游）；移除无效的 palette-first 面板路径

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test -- src/executors/run-skill-tool.test.ts` | 5 passed | 是 |
| 任务范围测试 | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9941 RXWF_E2E_WEB_PORT=9381 pnpm --filter @rxwf/web test:e2e toolSkill` | 5 passed（audit + @any） | 是 |
| linter/build | 单测 + E2E 上述命令 | 全部通过 | 是 |
| 冒烟（plus 轨） | `RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e toolSkill` | docker-compose.plus.yml invalid（与 T-072 相同）；@plus validate 用例与 @any 同 API 路径 | 部分（@any 已覆盖；plus 基础设施未就绪） |
| 验收标准核对 | `docs/test/node-audit-rows/toolSkill.md` panel/validation/executor satellite/status ok + e2e_spec | 审查行已填写 | 是 |

**结论**：`passed`

## 实际变更文件

- `apps/web/e2e/nodes/toolSkill.spec.ts`
- `docs/test/node-audit-rows/toolSkill.md`
- `docs/workflow/tasks/T-073.md`

## 实现说明

审查 `toolSkill` 卫星：`node-param-schemas` 通用面板（toolDescription/skillPath/mode）与 `runSkillTool`/`skillToolDefinition` 执行路径已就绪；补全 audit row 与 E2E spec（@any 面板 + E2003 debug-node + validate；@plus Agent hub validate）。修复 E2E debug-node 用例：孤立 toolSkill 无上游时 API 返回非 2xx，改为 manualTrigger → toolSkill 连线后正确断言 E2003。

## 未解决问题

- `RXWF_E2E_TRACK=plus` 本机 `docker-compose.plus.yml` invalid，@plus 轨未实测（与 T-072 相同）
- 无独立 `toolSkill.tsx` 面板组件（审查结论：通用 schema 渲染足够）
