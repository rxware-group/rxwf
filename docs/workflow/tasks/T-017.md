# T-017

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 1 |
| 描述 | skillRun toolGrep provider 补齐至 spec AC |
| 追溯 | FR-05 / AC-013 / M-2 skill-runtime |
| 依赖 | - |
| TDD Red 用例 | • grep 无匹配应返回空数组非 throw<br>• 正则非法应 E200x |
| 建议测试文件 | • packages/skill-runtime/src/tools/grep.test.ts |
| 文件所有权 | • packages/skill-runtime/src/tools/grep.ts<br>• packages/skill-runtime/src/tools/grep.test.ts |
| 验收标准 | grep 子能力 green |
| 基本验证提示 | pnpm --filter @rxwf/skill-runtime test grep |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/skill-runtime/src/tools/grep.test.ts`
- 运行命令：`pnpm --filter @rxwf/skill-runtime test grep`
- 失败摘要（预期内）：`Cannot find module './grep.js'` — grep.ts 尚未实现

### Green

- 实现文件：`packages/skill-runtime/src/tools/grep.ts`
- 运行命令：`pnpm --filter @rxwf/skill-runtime test grep`
- 通过摘要：`✓ src/tools/grep.test.ts (4 tests) 78ms` — exit 0

### Refactor

- 变更说明：无额外重构；本地 grep 遍历与 runner-agent 对齐，regex 校验严格抛 E2002（不 fallback literal）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/skill-runtime test grep` | 4 passed, exit 0 | 是 |
| linter/build | `pnpm --filter @rxwf/skill-runtime build` | tsc exit 0 | 是 |
| 冒烟（若适用） | N/A（provider 单元测试覆盖） | 跳过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/skill-runtime/src/tools/grep.ts`
- `packages/skill-runtime/src/tools/grep.test.ts`
- `docs/workflow/tasks/T-017.md`

## 实现说明

新增 `invokeGrep` / `compileGrepRegex` provider：无匹配返回 `{ matches: [] }`；非法 regex 抛 `E2002`；支持本地 Node 遍历与 `ToolInvokeClient` 委托 Runner `skill:filesystem.grep`。

## 未解决问题

- 无
