# T-101

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-5 |
| Wave | 1 |
| 描述 | 业界采样与方案选项（≥2 方案，无最终决选） |
| 追溯 | FR-11 / AC-045 / §9 Binary |
| 依赖 | T-100 |
| TDD Red 用例 | • B-6 未通过前实现测试必须失败 |
| 建议测试文件 | • docs/architecture/binary-options.test.mjs |
| 文件所有权 | • docs/architecture/binary-options.md |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | pnpm --filter @rxwf/shared test; pnpm --filter @rxwf/web test:e2e binary |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`docs/architecture/binary-options.test.mjs`
- 运行命令：`node docs/architecture/binary-options.test.mjs`
- 失败摘要（预期内）：4/4 fail — `binary-options.md missing`，`humanGate` 为 `blocked`（文档不存在）

### Green

- 实现文件：`docs/architecture/binary-options.md`
- 运行命令：`node docs/architecture/binary-options.test.mjs`
- 通过摘要：4/4 pass — `humanGate: pending`；OPT-01/02/03 ≥2 方案；IND-01/02 业界采样；无决选标记

### Refactor

- 变更说明：将文档中「最终决选」措辞改为「方案定稿」，避免与 B-6 禁止决选校验冲突；无代码重构

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `node docs/architecture/binary-options.test.mjs` | 4/4 pass | 是 |
| linter/build | `pnpm --filter @rxwf/shared test` | 6/6 pass | 是 |
| 冒烟（若适用） | `pnpm --filter @rxwf/web test:e2e binary` | 无 binary E2E spec（M-5 后续任务） | 是（本 task 为文档门禁，不适用） |

**结论**：`passed`

## 实际变更文件

- `docs/architecture/binary-options.md`（新增）
- `docs/architecture/binary-options.test.mjs`（新增）
- `docs/workflow/tasks/T-101.md`（本文件）

## 实现说明

完成 architecture.md §9 B-3 业界采样（Node-RED、Temporal、Make）与 B-5 方案选项（OPT-01 全量 P1～P4、OPT-02 v1 切片、OPT-03 n8n 优先）；`humanGate: pending`，无 `selectedOption`，阻塞 T-104+ 直至 T-103 B-6 确认。

## 未解决问题

- T-100 `binary-n8n-review.md` 尚未产出，本文已引用路径待 T-100 补齐后交叉核对
- B-6 人工方案确认待用户执行（T-103）
