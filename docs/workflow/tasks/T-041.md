# T-041

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 2 |
| 描述 | 审查并修复 nodeType `switch`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-028, AC-030 / node-runner switch |
| 依赖 | T-034 |
| TDD Red 用例 | • `switch` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E switch spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/control-flow/switch.test.ts（或新建）<br>• apps/web/e2e/nodes/switch.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/control-flow/switch.ts<br>• packages/node-runner/src/executors/control-flow/switch.test.ts<br>• apps/web/e2e/nodes/switch.spec.ts<br>• docs/test/node-audit-rows/switch.md<br>• apps/web/src/features/editor/node-params/switch.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；lite 轨验收通过 |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e switch |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/control-flow/switch.test.ts`（新增 registry 用例）、`apps/web/e2e/nodes/switch.spec.ts`（新建）
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test -- switch.test.ts`（registry 用例加入前：4 passed，无 registry 断言）
  - `pnpm --filter @rxwf/web test:e2e nodes/switch`（spec 文件不存在时：0 tests）
- 失败摘要（预期内）：M-3 所有权内缺 `e2e/nodes/switch.spec.ts` 与 `node-audit-rows/switch.md`；registry 断言缺失

### Green

- 实现文件：`switch.test.ts`（registry 断言）、`apps/web/e2e/nodes/switch.spec.ts`、`docs/test/node-audit-rows/switch.md`；执行器/面板/校验 M-2 动态 `branches[]` 已就绪，无业务代码缺陷需改
- 运行命令：
  - `pnpm --filter @rxwf/node-runner test -- switch.test.ts` → 5 passed
  - `pnpm --filter @rxwf/web test:e2e nodes/switch` → 5 passed
- 通过摘要：registry `has('switch')`；E2E 覆盖面板 + 多分支路由 + 首匹配 + 空 branches debug 失败

### Refactor

- 变更说明：E2E 使用唯一 workflow 名、`serial` 模式；空 branches 用合法定义保存后 debug 传无效 definition（保存校验与执行器分层）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test -- switch.test.ts` | 5 passed | 是 |
| E2E lite | `pnpm --filter @rxwf/web test:e2e nodes/switch` | 5 passed（23s） | 是（lite 轨） |
| 验收标准核对 | `docs/test/node-audit-rows/switch.md` 四维 ok + status ok；`SwitchBranchesPanel.test.tsx` / `validate-switch.test.ts` 已有绿 | 通过 | 是 |
| linter/build | 无 TS 变更需 build；仅测试与文档 | 跳过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/control-flow/switch.test.ts`
- `apps/web/e2e/nodes/switch.spec.ts`
- `docs/test/node-audit-rows/switch.md`
- `docs/workflow/tasks/T-041.md`

## 实现说明

M-2 已落地动态 `branches[]` 模型（`SwitchBranchesPanel`、`validateSwitchNodes`、`switchExecutor`）。本 task 补齐 M-3 审查交付：`switch` 注册断言、lite E2E spec（`e2e/nodes/switch.spec.ts`）、单行审查记录。遗留 `e2e/switch-dynamic.spec.ts`（M-2）与 owned spec 内容重叠；并行跑 `test:e2e switch` 可能因双 spec 并行抢 API  flaky，owned 验收以 `nodes/switch` 为准。

## 未解决问题

- `test:e2e switch` 同时匹配 `switch-dynamic.spec.ts` 与 `nodes/switch.spec.ts`，2 workers 下偶发 createWorkflow 失败；T-081 收口时可合并或去重 legacy spec
