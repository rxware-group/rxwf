# T-061

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `crewSequential`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner crewSequential |
| 依赖 | T-034 |
| TDD Red 用例 | • `crewSequential` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E crewSequential spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/crew-sequential.test.ts（或新建）<br>• apps/web/e2e/nodes/crewSequential.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/crew-sequential.ts<br>• packages/node-runner/src/executors/crew-sequential.test.ts<br>• apps/web/e2e/nodes/crewSequential.spec.ts<br>• docs/test/node-audit-rows/crewSequential.md<br>• apps/web/src/features/editor/node-params/crewSequential.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e crewSequential |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/crew-sequential.test.ts`（新增 registry/E1030/E2003）、`apps/web/e2e/nodes/crewSequential.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test crew-sequential.test.ts`（新增用例前仅 1 用例）；`RXWF_E2E_TRACK=lite ... playwright test crewSequential`（audit row / spec 创建前 ENOENT；面板用固定 workflow 名时 E1040 重名）
- 失败摘要（预期内）：registry `throws E2003 when not registered` 用例设计为未注册时失败；E2E audit row `ENOENT: crewSequential.md`；面板 E2E 工作流名重复 / edge 遮挡 dblclick

### Green

- 实现文件：审查确认 `crew-sequential.ts` 已注册；补充 `crew-sequential.test.ts`、`crewSequential.spec.ts`、`crewSequential.md`
- 运行命令：`pnpm --filter @rxwf/node-runner test crew-sequential.test.ts`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9967 RXWF_E2E_WEB_PORT=9407 pnpm --filter @rxwf/web exec playwright test crewSequential --retries=0`
- 通过摘要：5/5 单测；3/3 lite E2E（audit row + 面板「执行后端」/「Knowledge 注入模式」）

### Refactor

- 变更说明：E2E 使用唯一 workflow 名与 `getByTestId('rf__node-crew').dblclick({ force: true })` 规避 edge 遮挡；无执行器逻辑变更

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test crew-sequential.test.ts` | 5 passed（2026-06-20 复验） | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9967 RXWF_E2E_WEB_PORT=9407 pnpm --filter @rxwf/web exec playwright test crewSequential --retries=0` | 3 passed（audit row + 面板，2026-06-20 复验） | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus RXWF_E2E_API_PORT=9977 RXWF_E2E_WEB_PORT=9417 pnpm --filter @rxwf/web exec playwright test crewSequential --retries=0` | global-setup 失败：`docker-compose.plus.yml` web/api 无 image/build（2026-06-20 复验） | 否（本机 compose 配置） |
| 验收标准核对 | `docs/test/node-audit-rows/crewSequential.md` 四维 ok + status ok；`validate.ts` E1030/E1040；`register-plus.ts` 注册；单测 registry/handoff/E1030/E2003 | matrix 行 covered；审查行结论 ok；plus 轨 @plus 用例待 compose 修复 | 是（lite + 单测轨） |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/crew-sequential.test.ts`
- `apps/web/e2e/nodes/crewSequential.spec.ts`
- `docs/test/node-audit-rows/crewSequential.md`
- `docs/workflow/tasks/T-061.md`

## 实现说明

审查结论：面板（`node-param-schemas` + `NodeEditorParamsPane` 条件字段）、校验（`E1030`/`E1040`/警告码）、Plus 执行器（`registerPlusExecutors` + `executeCrewNode`）、错误码均已就绪。补充 registry/E1030/E2003 单测、E2E spec（@any 面板 + @plus E1030/Ollama 冒烟）与 audit row；未新增 `crewSequential.tsx`（通用面板已覆盖）。

## 未解决问题

- 本机 `deploy/docker-compose.plus.yml` 报 `service "api" has neither an image nor a build context`，plus 轨 E2E global-setup 未能启动；CI 修复 compose 后应跑通 `@plus`（E1030 + 可选 Ollama native 顺序执行）
- 帮助文档 `docs/help/zh/nodes/crewSequential.md` 由 M-6 T-141 负责
