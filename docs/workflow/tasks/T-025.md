# T-025

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 1 |
| 描述 | 工作流 ACL：WorkflowCollaboratorsPanel UI |
| 追溯 | FR-05 / AC-014 / apps/web editor |
| 依赖 | - |
| TDD Red 用例 | • 无 share 权限不显示邀请按钮<br>• 角色变更应调用 API |
| 建议测试文件 | • apps/web/src/features/editor/WorkflowCollaboratorsPanel.test.tsx |
| 文件所有权 | • apps/web/src/features/editor/WorkflowCollaboratorsPanel.tsx<br>• apps/web/src/features/editor/WorkflowCollaboratorsPanel.test.tsx |
| 验收标准 | ACL UI green |
| 基本验证提示 | pnpm --filter @rxwf/web test WorkflowCollaborators |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/web/src/features/editor/WorkflowCollaboratorsPanel.test.tsx`
- 运行命令：`pnpm --filter @rxwf/web test WorkflowCollaborators`
- 失败摘要（预期内）：组件实现已存在且满足 Red 用例；新建测试首次运行 2/2 通过（无额外实现改动）

### Green

- 实现文件：`apps/web/src/features/editor/WorkflowCollaboratorsPanel.tsx`（既有实现，`readOnly` 隐藏邀请按钮、`updateRole` → `saveCollaborators` → `api.workflows.updateCollaborators`）
- 运行命令：`pnpm --filter @rxwf/web test WorkflowCollaborators`
- 通过摘要：2 tests passed — `hides invite button when user lacks share permission (readOnly)`、`calls updateCollaborators API when collaborator role changes`

### Refactor

- 变更说明：无；测试与既有实现已对齐

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/web test WorkflowCollaborators` | 2/2 passed | 是 |
| linter/build | `ReadLints WorkflowCollaboratorsPanel.test.tsx` — 无错误；`pnpm --filter @rxwf/web typecheck` — 项目既有 TS 错误（非本任务文件） | 本任务文件无 lint/TS 问题 | 是 |
| 冒烟（若适用） | — | 不适用（单元测试覆盖 ACL UI 行为） | 是 |

**结论**：`passed`

## 实际变更文件

- `apps/web/src/features/editor/WorkflowCollaboratorsPanel.test.tsx`（新增）
- `docs/workflow/tasks/T-025.md`

## 实现说明

为既有 `WorkflowCollaboratorsPanel` 补充 TDD 单元测试：验证 `readOnly`（无 share 权限）时不渲染「添加协作者」按钮，以及通过 `Select` 变更角色时调用 `api.workflows.updateCollaborators`。

## 未解决问题

-
