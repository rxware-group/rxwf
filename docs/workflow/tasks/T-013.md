# T-013

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-1 |
| Wave | 3 |
| 描述 | playwright.config.ts 支持 RXWF_E2E_TRACK 双轨 project |
| 追溯 | FR-14 / AC-011 / §6.4 config |
| 依赖 | T-006 |
| TDD Red 用例 | • 缺少 standard/plus project 定义应失败 |
| 建议测试文件 | • apps/web/playwright.config.test.ts |
| 文件所有权 | • apps/web/playwright.config.ts<br>• apps/web/playwright.config.test.ts |
| 验收标准 | Standard/Plus/Lite 项目可区分 |
| 基本验证提示 | pnpm --filter @rxwf/web exec playwright test --list |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/web/playwright.config.test.ts`
- 运行命令：`pnpm --filter @rxwf/web exec vitest run playwright.config.test.ts`
- 失败摘要（预期内）：6 failed — `resolveE2eTrack` / `buildE2eProjects` / `assertRequiredTrackProjects` is not a function（实现尚不存在）

### Green

- 实现文件：`apps/web/playwright.config.ts`
- 运行命令：`pnpm --filter @rxwf/web exec vitest run playwright.config.test.ts`
- 通过摘要：6 passed — 含「缺少 standard/plus project 定义应失败」Red 用例；lite/standard/plus 三轨 browser project 可区分且 `metadata.track` 正确

### Refactor

- 变更说明：无额外重构；导出纯函数供单测，`defineConfig` 通过 `buildE2eProjects({ trackFilter })` 按 `RXWF_E2E_TRACK` 过滤活跃 project

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/web exec vitest run playwright.config.test.ts` | 6 pass / 0 fail | 是 |
| linter/build | vitest 通过；playwright config 可加载 | N/A typecheck（config 不在 tsconfig include） | 是 |
| 冒烟（若适用） | `pnpm --filter @rxwf/web exec playwright test --list` | 列出 `[lite-setup]`、`[lite-chromium]`、`[standard-setup]`、`[plus-setup]`；`RXWF_E2E_TRACK=standard` 仅 `[standard-setup]`；`RXWF_E2E_TRACK=plus` 仅 `[plus-setup]` | 是 |

**结论**：`passed`

## 实际变更文件

- `apps/web/playwright.config.ts`
- `apps/web/playwright.config.test.ts`

## 实现说明

导出 `E2E_TRACKS`、`resolveE2eTrack`、`assertRequiredTrackProjects`、`buildE2eProjects`；每轨 `{track}-setup` + `{track}-chromium` project 带 `metadata.track`；lite 用 `grepInvert` 排除 `@standard/@plus`，standard/plus 用 `grep` 匹配 `@standard/@plus/@any`；未设 `RXWF_E2E_TRACK` 时注册三轨全部 project，设置后仅激活对应轨。

## 未解决问题

- 无。standard/plus 轨尚无带 `@standard`/`@plus` 标签的 spec，`--list` 仅显示 setup project（符合 grep 过滤预期）。
