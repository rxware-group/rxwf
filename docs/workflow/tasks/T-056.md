# T-056

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `llmStream`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner llmStream |
| 依赖 | T-034 |
| TDD Red 用例 | • `llmStream` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E llmStream spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/llm-stream.test.ts（或新建）<br>• apps/web/e2e/nodes/llmStream.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/llm-stream.ts<br>• packages/node-runner/src/executors/llm-stream.test.ts<br>• apps/web/e2e/nodes/llmStream.spec.ts<br>• docs/test/node-audit-rows/llmStream.md<br>• apps/web/src/features/editor/node-params/llmStream.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e llmStream |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/llm-stream.test.ts`、`apps/web/e2e/nodes/llmStream.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test llm-stream.test.ts`；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e llmStream --retries=0`（audit row 创建前）
- 失败摘要（预期内）：单测 `Cannot find module './llm-stream.js'`；E2E audit row `ENOENT: docs/test/node-audit-rows/llmStream.md`

### Green

- 实现文件：`packages/node-runner/src/executors/llm-stream.ts`、`llm-stream.test.ts`、`apps/web/e2e/nodes/llmStream.spec.ts`、`docs/test/node-audit-rows/llmStream.md`
- 运行命令：`pnpm --filter @rxwf/node-runner test llm-stream.test.ts`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9925 RXWF_E2E_WEB_PORT=9365 pnpm --filter @rxwf/web test:e2e llmStream --retries=0`
- 通过摘要：5/5 单测；3/3 lite E2E（audit row + 面板 Model/Prompt + OllamaModelParamField）

### Refactor

- 变更说明：提取 `createLlmStreamExecutor` 复用 `ollama.ts` 的 `runOllamaChat`；E2E `@plus` 按 Ollama 可达性条件 skip 成功/失败路径；E2E `createWorkflow` 使用 `uniqueWorkflowName` 避免名称冲突；未新增独立 `llmStream.tsx`（通用面板已覆盖）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test llm-stream.test.ts` | 5 passed | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9925 RXWF_E2E_WEB_PORT=9365 pnpm --filter @rxwf/web test:e2e llmStream --retries=0` | 3 passed | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus RXWF_E2E_API_PORT=9924 RXWF_E2E_WEB_PORT=9364 pnpm --filter @rxwf/web test:e2e llmStream --retries=0` | global-setup 失败：`docker-compose.plus.yml` api 服务无 image/build | 否（本机环境） |
| 验收标准核对 | `docs/test/node-audit-rows/llmStream.md` 四维 ok + status ok；E2003/E3001 单测；E2E spec 含 `@plus` 执行用例 | 代码就绪；plus 需 CI Docker + 可选本机 Ollama | 部分 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/llm-stream.ts`
- `packages/node-runner/src/executors/llm-stream.test.ts`
- `apps/web/e2e/nodes/llmStream.spec.ts`
- `docs/test/node-audit-rows/llmStream.md`
- `docs/workflow/tasks/T-056.md`

## 实现说明

审查结论：面板（schema + `OllamaModelParamField` via `resolveOllamaModelField`）、校验（无额外保存错误码）、Plus 执行器（`registerPlusExecutors` 内联，逻辑已提取至 `llm-stream.ts` 复用 `runOllamaChat`）、错误码 E2003/E3001 均已就绪。输出字段为 `{ stream }` 而非 `{ response }`。补充单测、E2E spec 与 audit row。

## 未解决问题

- 本机 plus 轨 E2E global-setup 因 `deploy/docker-compose.plus.yml` 配置问题未能启动 Docker 栈；CI 修复 compose 后应跑通 `@plus`（Ollama 不可达时走 failed 路径，可达时走 success 路径）
- `register-plus.ts` 仍内联 llmStream 执行器（所有权外）；后续可改为 `createLlmStreamExecutor(deps)` 导入以消除重复
