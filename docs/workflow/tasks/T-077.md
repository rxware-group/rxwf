# T-077

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `toolGrep`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner toolGrep |
| 依赖 | T-034 |
| TDD Red 用例 | • `toolGrep` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E toolGrep spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/agent-satellite-tools.test.ts（或新建）<br>• apps/web/e2e/nodes/toolGrep.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/agent-satellite-tools.ts<br>• packages/node-runner/src/executors/agent-satellite-tools.test.ts<br>• apps/web/e2e/nodes/toolGrep.spec.ts<br>• docs/test/node-audit-rows/toolGrep.md<br>• apps/web/src/features/editor/node-params/toolGrep.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e toolGrep |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/agent-satellite-tools.test.ts`（toolGrep satellite + embedded grep）、`apps/web/e2e/nodes/toolGrep.spec.ts`
- 运行命令：`npx vitest run agent-satellite-tools.test.ts -t "greps files|fails embedded grep"`（实现前）；`playwright test toolGrep`（audit row/spec 创建前）
- 失败摘要（预期内）：embedded grep 抛 `Unknown filesystem method: grep`；E2E `ENOENT: docs/test/node-audit-rows/toolGrep.md`

### Green

- 实现文件：`agent-satellite-tools.ts`（`invokeEmbeddedRunnerTool` 补齐 `grep` + `compileGrepRegex`/`matchesGrepGlob`）、`agent-satellite-tools.test.ts`、`docs/test/node-audit-rows/toolGrep.md`、`apps/web/e2e/nodes/toolGrep.spec.ts`
- 运行命令：`npx vitest run agent-satellite-tools.test.ts -t "toolGrep|greps files|fails embedded grep"`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9941 RXWF_E2E_WEB_PORT=9381 pnpm --filter @rxwf/web exec playwright test toolGrep --retries=0`
- 通过摘要：7/7 toolGrep 相关单测；lite E2E 3/3（audit row + 面板 maxResults/内置描述）；2026-06-20 复验通过

### Refactor

- 变更说明：grep 逻辑与 `skill-run.ts` 对齐（`scanRoots` 内递归、`grepFallback: true`）；未新增 `toolGrep.tsx`（通用面板 + `BuiltinSatelliteToolDescription` 已覆盖）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `npx vitest run agent-satellite-tools.test.ts -t "toolGrep|greps files|fails embedded grep"` | 7 passed（2026-06-20 复验） | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9941 RXWF_E2E_WEB_PORT=9381 pnpm --filter @rxwf/web exec playwright test toolGrep --retries=0` | 3 passed（2026-06-20 复验） | 是 |
| E2E plus/@plus | `RXWF_E2E_TRACK=plus RXWF_E2E_API_PORT=9921 RXWF_E2E_WEB_PORT=9361 pnpm --filter @rxwf/web exec playwright test toolGrep --retries=0` | global-setup 失败：`docker-compose.plus.yml` api/web 无 image/build（2026-06-20 复验） | 否（本机 compose）；spec 已含 `@plus` E2003 + aiAgent 接线 |
| 验收标准核对 | `docs/test/node-audit-rows/toolGrep.md` 五维 ok/satellite + status ok；embedded grep 单测；E2E spec 登记 `E2E-N-toolGrep` | M-3 审查通过；plus 轨需 CI compose | 是（M-3 范围） |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/agent-satellite-tools.ts`
- `packages/node-runner/src/executors/agent-satellite-tools.test.ts`
- `docs/test/node-audit-rows/toolGrep.md`
- `apps/web/e2e/nodes/toolGrep.spec.ts`
- `docs/workflow/tasks/T-077.md`

## 实现说明

M-2 已在 `skill-run.ts` 补齐 skillRun 路径 grep；本任务补齐 **aiAgent embedded** 路径：`invokeEmbeddedRunnerTool` 支持 `skill:filesystem`/`grep`，使 `invokeAgentTool` 与 `buildAgentToolDefinitions` 端到端可用。面板（`maxResults` + 内置描述）、校验（无保存期错误码）、卫星执行语义与错误码 E2002/E1056/E1041/E2003 已审查并写入 audit row；补充单测与 E2E spec。

## 未解决问题

- 本机 plus compose `docker-compose.plus.yml` 报 api/web 无 build context，`@plus` E2003/aiAgent 用例未能本地执行（CI 应正常）
- `e2e-coverage-matrix.md` 中 `E2E-N-toolGrep` 行由编排器/T-080 合并为 covered
