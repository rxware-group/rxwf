# T-107

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-5 |
| Wave | 3 |
| 描述 | HTTP 响应 → binary 生产者 |
| 追溯 | FR-11 / AC-048 / §9 Binary |
| 依赖 | T-104, T-105 |
| TDD Red 用例 | • B-6 未通过前实现测试必须失败 |
| 建议测试文件 | • packages/node-runner/src/executors/http-binary.test.ts |
| 文件所有权 | • packages/node-runner/src/executors/http-binary.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | pnpm --filter @rxwf/shared test; pnpm --filter @rxwf/web test:e2e binary |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/http-binary.test.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/http-binary.test.ts`
- 失败摘要（预期内）：`Cannot find module './http-binary.js'`（模块未创建）

### Green

- 实现文件：`packages/node-runner/src/executors/http-binary.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test -- src/executors/http-binary.test.ts`
- 通过摘要：21 tests passed

### Refactor

- 变更说明：无额外 refactor；BIN-08 逻辑已在 `http-response.ts` 落地，本 task 在所有权文件内建立独立 producer 模块并补全测试矩阵

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test -- src/executors/http-binary.test.ts src/executors/http.test.ts src/http-response.test.ts` | 52 tests passed | 是 |
| linter/build | `pnpm --filter @rxwf/node-runner build` | 包级 build 有既有无关 TS 错误；`http-binary.ts` 无 linter/type 错误 | 是（变更范围） |
| 冒烟（若适用） | — | 不适用（producer 模块；executor 集成已由 `http.test.ts` BIN-08 覆盖） | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/http-binary.ts`（新增）
- `packages/node-runner/src/executors/http-binary.test.ts`（新增）

## 实现说明

RISK-01 核实：BIN-08 已在 `http-response.ts` + `executors/http.test.ts` 实现。本 task 在所有权文件 `http-binary.ts` 建立 HTTP 响应→binary 生产者（mode 归一化默认 `off`、content-type 自动检测、buffer 编码、Response 解析、item.binary merge），21 项单元测试覆盖 off/auto/always、Content-Disposition fileName、204、JSON 回归。

## 未解决问题

- `http-response.ts` / `http.ts` 仍使用内联逻辑，尚未 delegate 至 `http-binary.ts`（非本 task 所有权，后续 wiring 任务处理）
