# T-030

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 2 |
| 描述 | M-2 E2E：credential-types + switch 动态分支 |
| 追溯 | FR-13 / AC-018 / E2E |
| 依赖 | T-021, T-022, T-023, T-027 |
| TDD Red 用例 | • switch 多分支 E2E 未路由正确应失败 |
| 建议测试文件 | • apps/web/e2e/credential-types.spec.ts<br>• apps/web/e2e/switch-dynamic.spec.ts |
| 文件所有权 | • apps/web/e2e/credential-types.spec.ts<br>• apps/web/e2e/switch-dynamic.spec.ts |
| 验收标准 | M-2 功能 matrix 100% covered |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/web/e2e/switch-dynamic.spec.ts`、`apps/web/e2e/credential-types.spec.ts`（新建）
- 运行命令：
  - `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e switch-dynamic --project lite-chromium` → UI 用例失败（单击节点未打开 `.node-editor-modal`）；路由用例首次即通过（后端已实现）
  - `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e credential-types --project lite-chromium` → 设置页选择器/Authorization 断言路径错误
- 失败摘要（预期内）：
  - switch UI：`node-editor-modal` not found（需 dblclick + API 预建工作流）
  - switch 多分支首匹配：Red 用例 `when multiple branches match, only the first true branch receives the item` 在 spec 入库前无覆盖；实现已 green 时该断言设计为路由错误则失败
  - credential：`settings-page-shell` 不存在；`json.headers.Authorization` 为空（应读 `json.request.headers`）

### Green

- 实现文件：`apps/web/e2e/switch-dynamic.spec.ts`、`apps/web/e2e/credential-types.spec.ts`
- 运行命令：`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e credential-types switch-dynamic --project lite-chromium`
- 通过摘要：6 passed（1 auth setup + 3 switch + 1 credential + 1 credential 内含 API/UI 断言）

### Refactor

- 变更说明：抽取 switch spec 内 `createWorkflow` / `debugSwitchNode` 辅助函数；credential 测试 scoped 至设置 dialog；无业务代码改动

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e credential-types switch-dynamic --project lite-chromium` | 6 passed / 0 failed | 是（E2E-N-switch + E2E-P-011 spec 入库） |
| linter/build | 纯 Playwright spec，无 TS 编译变更 | N/A | 是 |
| 冒烟（若适用） | 同上（lite 轨，含 httpbin 外网 HTTP 凭证注入） | 6 passed | 是 |

**结论**：`passed`

## 实际变更文件

- `apps/web/e2e/credential-types.spec.ts`
- `apps/web/e2e/switch-dynamic.spec.ts`

## 实现说明

新增 M-2 E2E：`switch-dynamic.spec.ts` 覆盖 SwitchBranchesPanel 动态分支 UI、多分支逐条路由及「多条件匹配仅首分支」执行语义（`debug-node` API）；`credential-types.spec.ts` 覆盖 registry 驱动凭证表单创建、四通用类型 API、HTTP 节点 credentialId 注入（httpbin 验证 `request.headers.Authorization`）及编辑器凭证下拉引用。

## 未解决问题

- **e2e-coverage-matrix 行更新**（E2E-N-switch → `switch-dynamic.spec.ts` / covered；E2E-P-011 → `credential-types.spec.ts` / covered）不在本 task 文件所有权，需 T-032 或编排器合并。
- **standard 轨 compose**：本环境 Docker 未运行，`@standard` 轨 credential E2E 未在本机执行；spec 在 lite 轨已 green（凭证/HTTP 不依赖 Postgres）。
- **T-027 依赖**：凭证 UI 已由既有 `CredentialsPage` 实现；E2E 未阻塞于 T-027 未完成状态。
