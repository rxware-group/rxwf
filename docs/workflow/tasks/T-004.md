# T-004

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-1 |
| Wave | 1 |
| 描述 | 初始化 docs/test/e2e-coverage-matrix.md（v2.0 全功能行 + 列定义） |
| 追溯 | FR-03 / AC-007, AC-008 / §3.3 E2E 矩阵 |
| 依赖 | - |
| TDD Red 用例 | • matrix 校验脚本：行数 < 45 nodeType + 平台能力应失败<br>• 缺少 track 列应失败 |
| 建议测试文件 | • scripts/validate-e2e-matrix.test.mjs |
| 文件所有权 | • docs/test/e2e-coverage-matrix.md<br>• scripts/validate-e2e-matrix.mjs |
| 验收标准 | 矩阵含全 v2.0 功能行与必需列；初始状态可为 uncovered |
| 基本验证提示 | node scripts/validate-e2e-matrix.mjs |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`scripts/validate-e2e-matrix.mjs`（`--test` 内嵌 TDD 用例；所有权不含独立 test 文件）
- 运行命令：
  - `node scripts/validate-e2e-matrix.mjs --test` → 2 passed（校验逻辑：不完整矩阵、缺 track 列均失败）
  - `node scripts/validate-e2e-matrix.mjs` → exit 1，`ERROR: missing matrix file: docs/test/e2e-coverage-matrix.md`
- 失败摘要（预期内）：矩阵文件不存在时校验失败；内嵌用例确认不完整行集与缺 track 列可被捕获

### Green

- 实现文件：`scripts/validate-e2e-matrix.mjs`、`docs/test/e2e-coverage-matrix.md`
- 运行命令：
  - `node scripts/validate-e2e-matrix.mjs` → `OK: 65 rows (46 nodeType + 19 platform capabilities)`
  - `node scripts/validate-e2e-matrix.mjs --test` → 2 passed
- 通过摘要：矩阵含 46 可执行 nodeType（`node-type-meta.ts` 减 stickyNote）+ 19 平台能力行；7 列齐全；初始 status 均为 uncovered

### Refactor

- 变更说明：矩阵表头解析改为要求 `row_id | description` 连续列，避免与「列定义」说明表误判

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `node scripts/validate-e2e-matrix.mjs --test` | 2 passed | 是 |
| 矩阵完整性冒烟 | `node scripts/validate-e2e-matrix.mjs` | OK: 65 rows | 是 |
| linter/build | N/A（纯 Node 脚本 + Markdown，无 TS 变更） | 跳过 | 是 |

**结论**：`passed`

## 实际变更文件

- `docs/test/e2e-coverage-matrix.md`
- `scripts/validate-e2e-matrix.mjs`
- `docs/workflow/tasks/T-004.md`

## 实现说明

建立 v2.0 E2E 覆盖矩阵（46 nodeType + 19 平台能力行），列含 AC-008 要求的 description/spec_fr/node_type/e2e_spec/status 及 NFR-04 track；校验脚本从 `node-type-meta.ts` 动态读取可执行类型并支持 `--require-full` / `--nodes` 供后续 Milestone 使用。

## 未解决问题

- PRD 文案写 45 nodeType，当前 `node-type-meta.ts` 实际为 46 个可执行类型（已以源码为准）；若需对齐 PRD 计数须单独澄清是否合并/移除某 type
