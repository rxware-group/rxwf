# T-086

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-4 |
| Wave | 1 |
| 描述 | groupChat 参数 schema 与面板校验（含 UserProxy 默认 -1） |
| 追溯 | FR-10 / AC-043 / §8 Group Chat |
| 依赖 | T-085 |
| TDD Red 用例 | • userProxyTimeoutMs 默认 !== -1 应失败 |
| 建议测试文件 | • packages/workflow/src/validate-group-chat.test.ts |
| 文件所有权 | • packages/workflow/src/validate-group-chat.ts<br>• apps/web/src/features/editor/group-chat-params.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e group-chat |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/workflow/src/validate-group-chat.test.ts`
- 运行命令：`pnpm --filter @rxwf/workflow test validate-group-chat.test.ts`
- 失败摘要（预期内）：`Cannot find module './validate-group-chat.js'`（实现文件尚未创建）

### Green

- 实现文件：`packages/workflow/src/validate-group-chat.ts`、`apps/web/src/features/editor/group-chat-params.ts`
- 运行命令：`pnpm --filter @rxwf/workflow test validate-group-chat.test.ts`
- 通过摘要：4 passed — `userProxyTimeoutMs` 默认 -1、normalize 补全、E1053 非法超时、E1048 成员不足

### Refactor

- 变更说明：无额外 refactor；参数默认值与 OQ-010 / architecture §8.3 对齐

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/workflow test validate-group-chat.test.ts` | 4 passed | 是 |
| 受影响模块测试 | `pnpm --filter @rxwf/workflow test` | 98 passed (19 files) | 是 |
| linter/build | `pnpm --filter @rxwf/workflow build` | exit 0 | 是 |
| 冒烟（若适用） | E2E 未跑（本 task 仅交付 schema/校验模块，尚未接入 `validate.ts` / `node-param-schemas.ts`） | N/A | 是（AC-043 schema 层） |

**结论**：`passed`

## 实际变更文件

- `packages/workflow/src/validate-group-chat.ts`
- `packages/workflow/src/validate-group-chat.test.ts`
- `apps/web/src/features/editor/group-chat-params.ts`
- `docs/workflow/tasks/T-086.md`

## 实现说明

新增 `defaultGroupChatParameters` / `normalizeGroupChatParameters` / `validateGroupChatNodes`（含 `userProxyTimeoutMs` 默认 **-1**、E1053 参数校验、E1048/E1012/E1049 成员与 orchestrator 校验）；面板侧 `GROUP_CHAT_PARAM_FIELDS` 含 `userProxyTimeoutMs` / `userProxyTimeoutAction` 设置项及 `validateGroupChatPanelDefaults()` 守卫。

## 未解决问题

- 后续任务需将 `validateGroupChatNodes` 接入 `validate.ts`、`GROUP_CHAT_PARAM_FIELDS` 接入 `node-param-schemas.ts` / `node-port-defs.ts` 默认参数，方可端到端生效。
