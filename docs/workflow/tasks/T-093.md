# T-093

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-4 |
| Wave | 3 |
| 描述 | API orchestrationResume kind=groupChat 集成 |
| 追溯 | FR-10 / AC-037 / §8 Group Chat |
| 依赖 | T-090, T-091 |
| TDD Red 用例 | • resume 404 应失败 |
| 建议测试文件 | • apps/api/src/routes/hitl-resume.test.ts |
| 文件所有权 | • apps/api/src/routes/hitl-resume.ts<br>• apps/api/src/integration/group-chat.integration.test.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e group-chat |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/api/src/integration/group-chat.integration.test.ts`
- 运行命令：`pnpm --filter @rxwf/api test -- src/integration/group-chat.integration.test.ts`
- 失败摘要（预期内）：`Cannot find module '../routes/hitl-resume.js'`（路由模块尚未创建）

### Green

- 实现文件：`apps/api/src/routes/hitl-resume.ts`
- 运行命令：`pnpm --filter @rxwf/api test -- src/integration/group-chat.integration.test.ts`
- 通过摘要：3 passed — 不存在 execution 返回 404/E1001；UserProxy waiting 经 HTTP resume 成功并 append user 消息；空 supplement 返回 E3014

### Refactor

- 变更说明：将 group-chat-user-proxy 校验逻辑内联至 API 层（避免跨 rootDir 深导入破坏 `tsc`）；`prepareGroupChatHitlResume` 构建 `orchestrationResume { kind: 'groupChat' }` 载荷；`validate.ts` / `node-param-schemas` / `group-chat.ts` 接线超出所有权，由既有 runtime 路径承接

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/api test -- src/integration/group-chat.integration.test.ts` | 3 passed | 是 |
| 受影响模块测试 | `pnpm --filter @rxwf/api test -- src/routes/hitl-resume-group-chat.test.ts src/integration/group-chat.integration.test.ts` | 6 passed | 是 |
| linter/build | `pnpm --filter @rxwf/api exec tsc -p tsconfig.json --noEmit`（`hitl-resume.ts` 无 TS 错误；`knowledge-bases.ts` 既有无关错误） | 变更文件通过 | 是 |
| 冒烟（若适用） | E2E 属 T-094/T-095 所有权，本 task API 集成测试已覆盖 HTTP resume 链 | 跳过 | 是（AC-037 API 层） |

**结论**：`passed`

## 实际变更文件

- `apps/api/src/routes/hitl-resume.ts`
- `apps/api/src/integration/group-chat.integration.test.ts`
- `docs/workflow/tasks/T-093.md`

## 实现说明

新增 `registerHitlResumeRoutes`：POST `/api/executions/:executionId/hitl/resume` 在 API 层校验 groupChat UserProxy supplement、构建 `orchestrationResume kind=groupChat`，并修正 E1001 → 404。集成测试经 Fastify inject 验证 waiting → resume → success 全链。`executions.ts` / `bootstrap.ts` 接线及 `@rxwf/execution` 导出 group-chat-user-proxy 待后续 task。

## 未解决问题

- `registerHitlResumeRoutes` 尚未挂入 `bootstrap.ts`（`executions.ts` 仍保留旧路由）；生产入口需编排器或后续 task 切换
- `@rxwf/execution` 未导出 `group-chat-user-proxy`，API 层暂用内联校验；与 T-090 模块完全复用待 execution 包 export
- HITL sweeper 尚未接入 `runGroupChatUserProxyTimeoutSweepOnce`（T-091 模块）
