# T-088

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-4 |
| Wave | 1 |
| 描述 | groupChat orchestrator 模式（LLM JSON 调度） |
| 追溯 | FR-10 / AC-036 / §8 Group Chat |
| 依赖 | T-085 |
| TDD Red 用例 | • orchestrator 无 finish 动作应死循环检测失败 |
| 建议测试文件 | • packages/node-runner/src/executors/group-chat-orchestrator.test.ts |
| 文件所有权 | • packages/node-runner/src/executors/group-chat-orchestrator.ts<br>• packages/node-runner/src/executors/group-chat-orchestrator.test.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e group-chat |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/group-chat-orchestrator.test.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test -- group-chat-orchestrator`
- 失败摘要（预期内）：`Cannot find module './group-chat-orchestrator.js'` — 模块尚未创建

### Green

- 实现文件：`packages/node-runner/src/executors/group-chat-orchestrator.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test -- group-chat-orchestrator`
- 通过摘要：4 tests passed（含 Red 用例「orchestrator 无 finish 应 E1050 死循环检测失败」）

### Refactor

- 变更说明：无额外 refactor；移除未使用的中间变量

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test -- group-chat` | 13 passed（orchestrator 4 + group-chat 9） | 是 |
| linter/build | `pnpm --filter @rxwf/node-runner build` | 新文件无 lint 错误；package build 因既有 rag/subagent 测试 TS 错误失败（非本 task 变更） | 是（本 task 范围） |
| 冒烟（若适用） | — | E2E 留待 T-095 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/group-chat-orchestrator.ts`
- `packages/node-runner/src/executors/group-chat-orchestrator.test.ts`

## 实现说明

新增 `runGroupChatOrchestratorLoop` 封装 orchestrator 模式 LLM JSON 调度循环：`askOrchestrator` 返回 `speak`/`finish`/`request_user`；`detectOrchestratorDeadLoop` 在 maxRounds 内无 finish（且未命中关键词终止）时返回 E1050。

## 未解决问题

- `group-chat.ts` 尚未接入新模块（超出本 task 文件所有权）；后续 task 可 refactor 复用。
