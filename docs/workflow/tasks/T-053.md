# T-053

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `readWriteFile`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner readWriteFile |
| 依赖 | T-034 |
| TDD Red 用例 | • `readWriteFile` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E readWriteFile spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/read-write-file.test.ts（或新建）<br>• apps/web/e2e/nodes/readWriteFile.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/read-write-file.ts<br>• packages/node-runner/src/executors/read-write-file.test.ts<br>• apps/web/e2e/nodes/readWriteFile.spec.ts<br>• docs/test/node-audit-rows/readWriteFile.md<br>• apps/web/src/features/editor/node-params/readWriteFile.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e readWriteFile |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/read-write-file.test.ts`、`apps/web/e2e/nodes/readWriteFile.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test read-write-file.test.ts`；`pnpm --filter @rxwf/web test:e2e readWriteFile --retries=0`（audit row 创建前）
- 失败摘要（预期内）：M-3 audit row 单测 `existsSync(readWriteFileAuditRowPath)` 失败；E2E audit row 测试 `ENOENT: docs/test/node-audit-rows/readWriteFile.md`

### Green

- 实现文件：`docs/test/node-audit-rows/readWriteFile.md`、`apps/web/e2e/nodes/readWriteFile.spec.ts`；`read-write-file.test.ts` 补充 registry/audit/errorCode 断言（`read-write-file.ts` 无需改动）
- 运行命令：`pnpm --filter @rxwf/node-runner test read-write-file.test.ts`；`RXWF_E2E_TRACK=standard pnpm --filter @rxwf/web test:e2e readWriteFile --retries=0 --workers=1`
- 通过摘要：18/18 单测；standard 轨 4/4 E2E（audit row + 面板 + debug-node write/read 往返 + 空 path E2002）；lite 轨 1/1（audit row，plus 用例跳过）

### Refactor

- 变更说明：E2E describe 由 `@any` 改为 `@plus @any`，audit row describe 加 `@any`；lite 轨跳过 plus 节点执行用例（避免 createWorkflow 失败），与 `crewHierarchical` 等 plus 节点一致

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test read-write-file.test.ts` | 18 passed | 是 |
| E2E lite（audit row） | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web test:e2e readWriteFile --retries=0 --workers=1` | 1 passed（plus 用例正确跳过） | 是 |
| E2E standard（@plus @any） | `RXWF_E2E_TRACK=standard pnpm --filter @rxwf/web test:e2e readWriteFile --retries=0 --workers=1` | 4 passed（audit + 面板 + debug-node×2） | 是 |
| 验收标准核对 | `docs/test/node-audit-rows/readWriteFile.md` 四维 ok + status ok；registry E2003/E2002 有单测；E2E 面板与 debug-node 有证据 | 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/read-write-file.test.ts`
- `apps/web/e2e/nodes/readWriteFile.spec.ts`
- `docs/test/node-audit-rows/readWriteFile.md`
- `docs/workflow/tasks/T-053.md`

## 实现说明

审查结论：面板（`node-param-schemas` + `NodeEditorParamsPane` 条件字段）、Plus 执行器（`registerPlusExecutors`）、E2002 运行时校验与 binary read/write 语义均已就绪。补充 registry/audit 单测、E2E spec 与 audit row 文档；修复 E2E 轨标签（`@plus @any`）使 lite 轨不误跑 plus 节点；`read-write-file.ts` 无缺陷需修复。

## 未解决问题

- 本机 `RXWF_E2E_TRACK=plus` 因 `deploy/docker-compose.plus.yml` 缺少 api build context 无法启动 compose；`@plus @any` E2E 已在 standard 轨全绿（API 均注册 `plusDeps`），语义与 plus 轨等价
