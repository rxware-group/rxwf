# T-059

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `mcpClient`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner mcpClient |
| 依赖 | T-034 |
| TDD Red 用例 | • `mcpClient` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E mcpClient spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/mcp-client.test.ts（或新建）<br>• apps/web/e2e/nodes/mcpClient.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/mcp-client.ts<br>• packages/node-runner/src/executors/mcp-client.test.ts<br>• apps/web/e2e/nodes/mcpClient.spec.ts<br>• docs/test/node-audit-rows/mcpClient.md<br>• apps/web/src/features/editor/node-params/mcpClient.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e mcpClient |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/mcp-client.test.ts`、`apps/web/e2e/nodes/mcpClient.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test mcp-client.test.ts`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9930 RXWF_E2E_WEB_PORT=9370 pnpm --filter @rxwf/web test:e2e mcpClient --retries=0`（audit row 创建前）
- 失败摘要（预期内）：单测 `Cannot find module './mcp-client.js'`；E2E audit row `ENOENT: docs/test/node-audit-rows/mcpClient.md`

### Green

- 实现文件：`packages/node-runner/src/executors/mcp-client.ts`、`mcp-client.test.ts`、`apps/web/e2e/nodes/mcpClient.spec.ts`、`docs/test/node-audit-rows/mcpClient.md`
- 运行命令：`pnpm --filter @rxwf/node-runner test mcp-client.test.ts`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9932 RXWF_E2E_WEB_PORT=9372 pnpm --filter @rxwf/web test:e2e mcpClient --retries=0`
- 通过摘要：10/10 单测；3/3 lite E2E（audit row + 面板 MCP Server/Tool + `.mcp-client-fields`）

### Refactor

- 变更说明：自 `register-plus.ts` 提取 `createMcpClientExecutor` / `resolveMcpToolList`；改用 `callMcpTool({ serverId, toolName, args })` 替代已废弃的 `deps.mcpClient` 直连；失败路径返回 `status: failed` 而非 throw

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test mcp-client.test.ts` | 10/10 passed（2026-06-20） | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9932 RXWF_E2E_WEB_PORT=9372 pnpm --filter @rxwf/web test:e2e mcpClient --retries=0` | 3/3 passed（audit row + 面板 MCP Server/Tool + `.mcp-client-fields`） | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus RXWF_E2E_API_PORT=9928 RXWF_E2E_WEB_PORT=9368 pnpm --filter @rxwf/web test:e2e mcpClient --retries=0` | global-setup 失败：`docker-compose.plus.yml` api 无 build context（本机环境） | 否（环境阻塞；spec 含 `@plus` 缺 server 失败路径已就绪） |
| typecheck | `pnpm --filter @rxwf/node-runner exec tsc --noEmit` | 既有 `rag.test.ts` TS 错误（非本 task）；`mcp-client.ts` 无新增错误 | 是（变更范围） |
| 验收标准核对 | `docs/test/node-audit-rows/mcpClient.md`：panel/validation/executor/status=ok；error_codes E1001/E1004/E2003/E3012；e2e_spec=`nodes/mcpClient.spec.ts`；matrix 行 AUDIT-N-mcpClient 待 T-080 合并 | 审查行结论完整；plus 轨需 CI Docker | 是（matrix 合并属 T-080） |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/mcp-client.ts`
- `packages/node-runner/src/executors/mcp-client.test.ts`
- `apps/web/e2e/nodes/mcpClient.spec.ts`
- `docs/test/node-audit-rows/mcpClient.md`
- `docs/workflow/tasks/T-059.md`

## 实现说明

审查结论：面板（`McpClientFields` + `NodeEditorParamsPane`）、校验（无额外保存错误码）、Plus 执行器（已提取至 `mcp-client.ts`，经 `callMcpTool` 调用）、错误码 E2003/E1004/E1001/E3012 均已文档化。补充单测、E2E spec 与 audit row；未新增独立 `mcpClient.tsx`（专用面板组件已存在）。

## 未解决问题

- `register-plus.ts` 仍内联旧版 `deps.mcpClient` 逻辑（所有权外）；运行时应改为 `createMcpClientExecutor(deps)` 与单测模块对齐，否则 debug-node 走 E3011 throw 而非 E1004 failed
- 本机 `docker-compose.plus.yml` 无法启动 plus compose，plus 轨 E2E 未能实际执行；CI 启 Docker 后应跑通 `@plus`（缺 serverId 失败 + 可选 live MCP）
- live MCP 冒烟需 `RXWF_E2E_MCP_LIVE=1` 且本机 npx MCP filesystem server 可用
