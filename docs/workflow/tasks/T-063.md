# T-063

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 4 |
| 描述 | 审查并修复 nodeType `crewSupervisor`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-027, AC-028, AC-030 / node-runner crewSupervisor |
| 依赖 | T-034 |
| TDD Red 用例 | • `crewSupervisor` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E crewSupervisor spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/crew-supervisor.test.ts（或新建）<br>• apps/web/e2e/nodes/crewSupervisor.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/crew-supervisor.ts<br>• packages/node-runner/src/executors/crew-supervisor.test.ts<br>• apps/web/e2e/nodes/crewSupervisor.spec.ts<br>• docs/test/node-audit-rows/crewSupervisor.md<br>• apps/web/src/features/editor/node-params/crewSupervisor.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；plus 轨验收通过 |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e crewSupervisor |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/crew-supervisor.test.ts`（registry/E1032/E1035/监督循环）、`apps/web/e2e/nodes/crewSupervisor.spec.ts`
- 运行命令（历史 Red）：audit row 缺失时 E2E `ENOENT: docs/test/node-audit-rows/crewSupervisor.md`；孤立 supervisor 缺工人触发 E1032 保存校验
- 失败摘要（预期内）：上述 Red 用例已在 Green 阶段修复；当前复验无需再 Red

### Green

- 实现文件：审查确认 `crew-supervisor.ts` 经 `register-plus.ts` 注册；`crew-supervisor.test.ts`、`crewSupervisor.spec.ts`、`crewSupervisor.md` 已就绪；未新增 `crewSupervisor.tsx`（通用 schema 面板已覆盖）
- 运行命令：`pnpm --filter @rxwf/node-runner test crew-supervisor.test.ts`；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web exec playwright test crewSupervisor --retries=0`
- 通过摘要（2026-06-20 复验）：6/6 单测；3/3 lite E2E（audit row 四维 ok + 面板监督参数 + `rf__node-sup` 双击）

### Refactor

- 变更说明：E2E `@plus` 校验路径使用 validate API（E1032/E1035 为保存期错误）；面板用例复用含工人的合法 definition 并通过 testId 避免节点遮挡；本次复验无额外 refactor

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test crew-supervisor.test.ts` | 6 passed（2026-06-20） | 是 |
| E2E lite/@any | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web exec playwright test crewSupervisor --retries=0` | 3 passed（2026-06-20） | 是 |
| E2E plus | `RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web exec playwright test crewSupervisor --retries=0` | 本机端口 9878 冲突 / plus webServer 未就绪；`@plus` 用例代码就绪 | 否（本机环境；CI Docker 验收） |
| 验收标准核对 | `docs/test/node-audit-rows/crewSupervisor.md` panel/validation/executor/status=ok；单测 registry+E1032+E1035；E2E spec 含 `@plus` validate + Ollama 冒烟 | matrix 行 covered；审查行有结论 | 是（plus 轨待 CI） |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/crew-supervisor.test.ts`
- `apps/web/e2e/nodes/crewSupervisor.spec.ts`
- `docs/test/node-audit-rows/crewSupervisor.md`
- `docs/workflow/tasks/T-063.md`

## 实现说明

审查结论：面板（`node-param-schemas` + `NodeEditorParamsPane` 条件 CredentialSelect）、保存校验（`validate.ts` E1032/E1035）、Plus 执行器（`createCrewSupervisorExecutor` → `runCrewSupervisorNative`）、错误码 E1032/E1033/E1035/E2003 均已就绪。补充 registry/运行时错误单测、E2E spec 与 audit row。

## 未解决问题

- 本机 `RXWF_E2E_TRACK=plus` global-setup 因 `docker-compose.plus.yml` 无法启动而失败；CI 启 Docker 后应跑通 `@plus` validate 与 Ollama 可达时的 debug-node 冒烟
- 未新增独立 `crewSupervisor.tsx`（所有权标注「若存在」；通用 schema 面板已满足审查）
