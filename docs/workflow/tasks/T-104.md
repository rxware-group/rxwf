# T-104

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-5 |
| Wave | 3 |
| 描述 | WorkflowItem.binary 类型与 BinaryMap 扩展 |
| 追溯 | FR-11 / AC-047 / §9 Binary |
| 依赖 | T-103 |
| TDD Red 用例 | • B-6 未通过前实现测试必须失败 |
| 建议测试文件 | • packages/shared/src/workflow-item.test.ts |
| 文件所有权 | • packages/shared/src/workflow-item.ts<br>• packages/shared/src/binary-map.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | pnpm --filter @rxwf/shared test; pnpm --filter @rxwf/web test:e2e binary |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/shared/src/workflow-item.test.ts`
- 运行命令：`pnpm --filter @rxwf/shared test -- src/workflow-item.test.ts`
- 失败摘要（预期内）：`Cannot find module './binary-map.js'`（模块未创建）；B-6 gate 测试逻辑已就绪

### Green

- 实现文件：`packages/shared/src/binary-map.ts`、`packages/shared/src/workflow-item.ts`
- 运行命令：`pnpm --filter @rxwf/shared test -- src/workflow-item.test.ts`
- 通过摘要：8/8 pass — B-6 gate + BinaryMap 形状校验 + WorkflowItem.binary 类型层（AC-047）

### Refactor

- 变更说明：`isWorkflowItem` 复用 `binary-map.isBinaryMap`；`item.ts` 保留既有类型（RISK-01，待后续任务统一 re-export）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/shared test -- src/workflow-item.test.ts` | 8/8 pass | 是（AC-047 类型层） |
| 受影响模块测试 | `pnpm --filter @rxwf/shared test` | 21/21 pass | 是 |
| linter/build | `pnpm --filter @rxwf/shared build` | 既有 `binary-blob-service.test.ts` TS 错误（非本 task 所有权）；`workflow-item*` 无新增错误 | 是（本 task 范围） |
| 冒烟（若适用） | `pnpm --filter @rxwf/web test:e2e binary` | 无 binary E2E spec（T-111/T-112） | 是（本 task 为 shared 类型） |

**结论**：`passed`

## 实际变更文件

- `packages/shared/src/binary-map.ts`（新增）
- `packages/shared/src/workflow-item.ts`（新增）
- `packages/shared/src/workflow-item.test.ts`（新增）

## 实现说明

按 design §3.1 在 T-104 所有权模块中定义 `BinaryBlobRef` / `BinaryAttachment` / `BinaryMap` 及 `WorkflowItem.binary`；提供 `isBinaryAttachment`、`isBinaryMap`、`mergeBinaryMaps`、`createWorkflowItem`、`hasBinary` 运行时 helper。B-6 门禁测试读取 `binary-current-state.md` frontmatter 验证 `m5BinaryPlanGate: cleared`。

## 未解决问题

- `packages/shared/src/item.ts` 仍保留同构类型（main 既有实现）；后续 wave 应统一从 `workflow-item.ts` / `binary-map.ts` re-export 并更新 `index.ts`
