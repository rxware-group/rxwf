# T-089

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-4 |
| Wave | 1 |
| 描述 | ai-runtime runGroupChatGraph 适配层（可选路径） |
| 追溯 | FR-10 / AC-035, AC-036 / §8 Group Chat |
| 依赖 | T-085 |
| TDD Red 用例 | • graph 入口未导出应失败 |
| 建议测试文件 | • packages/ai-runtime/src/agents/group-chat-agent.test.ts |
| 文件所有权 | • packages/ai-runtime/src/agents/group-chat-agent.ts<br>• packages/ai-runtime/src/agents/group-chat-agent.test.ts |
| 验收标准 | Green + Verify passed |
| 基本验证提示 | RXWF_E2E_TRACK=plus pnpm --filter @rxwf/web test:e2e group-chat |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/ai-runtime/src/agents/group-chat-agent.test.ts`（新增 `group-chat graph entry` + E1048 用例）
- 运行命令：`pnpm --filter @rxwf/ai-runtime test group-chat-agent.test.ts`（临时移除 `runGroupChatGraph` export 后）
- 失败摘要（预期内）：6 failed — `expected undefined to be type of 'function'`（graph 入口未导出）；`(0 , runGroupChatGraph) is not a function`

### Green

- 实现文件：`packages/ai-runtime/src/agents/group-chat-agent.ts`（`runGroupChatGraph` LangGraph StateGraph 适配层：round-robin / orchestrator 调度、关键词终止、transcript markdown、E1048/E1049/E1050 错误路径）
- 运行命令：`pnpm --filter @rxwf/ai-runtime test group-chat-agent.test.ts`
- 通过摘要：6/6 passed

### Refactor

- 变更说明：无（实现已满足测试，未额外重构）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/ai-runtime test group-chat-agent.test.ts` | 6 passed | 是 |
| 受影响模块测试 | `pnpm --filter @rxwf/ai-runtime test` | 33 passed | 是 |
| linter/build | `pnpm --filter @rxwf/ai-runtime build` | tsc OK | 是 |
| 冒烟（若适用） | E2E group-chat（plus 轨，本 task 范围外） | 未执行（ai-runtime 适配层由单测 + langchain-runtime 委托用例覆盖） | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/ai-runtime/src/agents/group-chat-agent.ts`
- `packages/ai-runtime/src/agents/group-chat-agent.test.ts`
- `docs/workflow/tasks/T-089.md`

## 实现说明

在 `@rxwf/ai-runtime` 提供 `runGroupChatGraph` LangGraph 适配层：StateGraph 编排 `schedule_speaker` → `participant_turn` 循环，支持 round-robin 与 orchestrator（LLM JSON speak/finish）两种发言选择；通过 `invokeParticipant` 回调执行成员回合；`langchain-runtime.runGroupChat` 已委托此入口。

## 未解决问题

- 无
