# T-027

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 2 |
| 描述 | credential-types Web 设置页与节点引用 UI |
| 追溯 | FR-05 / AC-015 / apps/web settings |
| 依赖 | T-019, T-020 |
| TDD Red 用例 | • 节点选 credential 下拉为空应失败 |
| 建议测试文件 | • apps/web/src/features/settings/CredentialsPanel.test.tsx |
| 文件所有权 | • apps/web/src/features/settings/CredentialsPanel.tsx<br>• apps/web/src/features/settings/credential-types.ts |
| 验收标准 | 凭证 UI green |
| 基本验证提示 | pnpm --filter @rxwf/web test Credentials |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`apps/web/src/features/settings/CredentialsPanel.test.tsx`
- 运行命令：`pnpm --filter @rxwf/web test -- CredentialsPanel`
- 失败摘要（预期内）：`Failed to resolve import "./CredentialsPanel.js"` — 组件与 credential-types 模块尚不存在

### Green

- 实现文件：
  - `apps/web/src/features/settings/credential-types.ts` — 类型定义、acceptedTypes 常量、过滤/选项构建/校验辅助函数
  - `apps/web/src/features/settings/CredentialsPanel.tsx` — `CredentialsPanel`（schema 驱动设置页）+ `NodeCredentialSelect`（节点凭据引用下拉）
- 运行命令：`pnpm --filter @rxwf/web test -- CredentialsPanel`
- 通过摘要：2/2 passed（节点下拉过滤显示匹配凭据；设置页加载类型并渲染动态 secret 字段）

### Refactor

- 变更说明：无额外重构；`useCredentialCatalog` 内联于面板与节点选择组件，保持最小实现。

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/web test -- Credentials` | 2/2 passed | 是 |
| linter/build | `read_lints`（变更文件无诊断）；`pnpm --filter @rxwf/web typecheck` 失败（`agent-tool-input-sources.test.ts` 等预存 TS 错误，与本次变更无关） | 变更文件无 lint 问题 | 是 |
| 冒烟（若适用） | — | N/A（组件单测覆盖） | 是 |

**结论**：`passed`

## 实际变更文件

- `apps/web/src/features/settings/credential-types.ts`
- `apps/web/src/features/settings/CredentialsPanel.tsx`
- `apps/web/src/features/settings/CredentialsPanel.test.tsx`

## 实现说明

新增 `credential-types.ts` 共享类型与 HTTP/AI acceptedTypes 常量及辅助函数；`CredentialsPanel` 提供 schema 驱动凭据管理 UI，`NodeCredentialSelect` 按 acceptedTypes 过滤并显示 displayName 标注的凭据选项。

## 未解决问题

- `CredentialsPage` / `CredentialSelect` 尚未接入新组件（非本 task 文件所有权）；编排器或后续 task 可将其改为引用 `CredentialsPanel` / `NodeCredentialSelect`。
- `@rxwf/web` 全量 `typecheck` 因预存错误失败，非本 task 范围。
