# T-021

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 1 |
| 描述 | switch 动态分支：workflow 图校验与保存规则 |
| 追溯 | FR-05 / AC-016 / packages/workflow switch |
| 依赖 | - |
| TDD Red 用例 | • 动态分支表达式为空应校验失败<br>• 输出端口数与规则不匹配应失败 |
| 建议测试文件 | • packages/workflow/src/validate-switch.test.ts |
| 文件所有权 | • packages/workflow/src/validate-switch.ts<br>• packages/workflow/src/validate-switch.test.ts |
| 验收标准 | switch 校验 green |
| 基本验证提示 | pnpm --filter @rxwf/workflow test validate-switch |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/workflow/src/validate-switch.test.ts`
- 运行命令：`pnpm --filter @rxwf/workflow test validate-switch`
- 失败摘要（预期内）：`Cannot find module './validate-switch.js'`（实现文件尚不存在）

### Green

- 实现文件：`packages/workflow/src/validate-switch.ts`
- 运行命令：`pnpm --filter @rxwf/workflow test validate-switch`
- 通过摘要：3/3 通过（空条件拒绝、端口不匹配拒绝、合法配置通过）

### Refactor

- 变更说明：无；首版实现已足够精简

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/workflow test validate-switch` | 3/3 passed | 是 |
| linter/build | `pnpm --filter @rxwf/workflow build` + 全包 `pnpm --filter @rxwf/workflow test` | build OK，88/88 passed | 是 |
| 冒烟（若适用） | — | N/A | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/workflow/src/validate-switch.ts`
- `packages/workflow/src/validate-switch.test.ts`

## 实现说明

新增 `validateSwitchNodes`，对 `switch` 节点校验：至少一条分支、每条 `condition` 非空且为 `{{ }}` 表达式、出边 `fromOutput`/`outputIndex` 须与 `branches` 对齐；错误码统一 `E2003`。

## 未解决问题

- `validateSwitchNodes` 尚未接入 `validateWorkflowDefinition`（超出本任务文件所有权；需后续任务或编排器授权接线）
