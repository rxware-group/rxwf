# T-010

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-1 |
| Wave | 2 |
| 描述 | INDEX.md 补充 FR 编号与 nodeType 交叉引用入口 |
| 追溯 | FR-01 / AC-012 / §10.2 |
| 依赖 | T-002, T-008 |
| TDD Red 用例 | • INDEX 缺少 fr/nodeType 索引节应失败 |
| 建议测试文件 | • scripts/lint-docs-index.test.mjs |
| 文件所有权 | • docs/INDEX.md |
| 验收标准 | AC-012 可导航至 FR 与 nodeType |
| 基本验证提示 | 人工 + lint:docs-index |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：内联 `validateCrossRefSections`（文件所有权仅 `docs/INDEX.md`，T-005 拥有 `scripts/lint-docs-index.test.mjs`）
- 运行命令：`node --input-type=module -e "…validateCrossRefSections…"`（读取 `docs/INDEX.md`）
- 失败摘要（预期内）：`placeholder text remains; FR table missing FR-13; FR table missing FR-10` — exit 1

### Green

- 实现文件：`docs/INDEX.md`
- 运行命令：同上内联校验
- 通过摘要：`GREEN PASS: cross-ref sections complete; FRs=FR-01,FR-12,FR-13,FR-06,FR-09,FR-10; nodeTypes=code,if,switch,loop,webhookTrigger,aiAgent,skillRun` — exit 0

### Refactor

- 变更说明：无（表格结构已清晰，nodeType 表增加 FR 列便于双向导航）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | 内联 `validateCrossRefSections` + `node scripts/lint-docs-index.test.mjs` | cross-ref 校验 exit 0；2 passed, 0 failed | 是 |
| linter/build | `pnpm lint:docs-index` | 脚本可执行；271 未登记 md（M-1 骨架预期，T-166 全量登记） | 是 |
| 冒烟（若适用） | `parseIndexFile('docs/INDEX.md')` | entries=24，frontmatter 可解析 | 是 |
| AC-012 人工核对 | 阅读 `## FR 与 nodeType 入口` | 6 个 FR、7 个 nodeType 均可点击导航至对应文档 | 是 |

**结论**：`passed`

## 实际变更文件

- `docs/INDEX.md`

## 实现说明

补全 `## FR 与 nodeType 入口`：移除占位文案，按 YAML `entries` 中 `fr`/`nodeType` 字段同步 FR 表（含 FR-10/FR-13）与 nodeType 表（含 FR 列），满足 AC-012 双向导航。

## 未解决问题

- 全 repo `lint:docs-index` 仍有 271 篇未登记 md（属 T-166 范围，非本 task）
