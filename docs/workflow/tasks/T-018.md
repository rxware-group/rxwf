# T-018

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 1 |
| 描述 | skillRun toolWebSearch provider 补齐至 spec AC |
| 追溯 | FR-05 / AC-013 / M-2 skill-runtime |
| 依赖 | - |
| TDD Red 用例 | • 未配置 provider 应失败<br>• mock provider 返回 citations |
| 建议测试文件 | • packages/skill-runtime/src/tools/web-search.test.ts<br>• packages/node-runner/src/executors/resolve-web-search-provider-config.test.ts |
| 文件所有权 | • packages/skill-runtime/src/tools/web-search.ts<br>• packages/skill-runtime/src/tools/web-search.test.ts |
| 验收标准 | web_search 子能力 green |
| 基本验证提示 | pnpm --filter @rxwf/skill-runtime test web-search |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/skill-runtime/src/tools/web-search.test.ts`
- 运行命令：`pnpm --filter @rxwf/skill-runtime test web-search`
- 失败摘要（预期内）：`Cannot find module './web-search.js'`（实现文件尚未创建）

### Green

- 实现文件：`packages/skill-runtime/src/tools/web-search.ts`
- 运行命令：`pnpm --filter @rxwf/skill-runtime test web-search`
- 通过摘要：`src/tools/web-search.test.ts` 2 passed；`src/executor/web-search.test.ts` 4 passed

### Refactor

- 变更说明：简化 `toCitations` 参数类型，去除不必要的 conditional type

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/skill-runtime test web-search` | 6 passed (2 files) | 是 |
| linter/build | `pnpm --filter @rxwf/skill-runtime build` | exit 0 | 是 |
| 冒烟（若适用） | `pnpm --filter @rxwf/skill-runtime test` | 42 passed (17 files) | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/skill-runtime/src/tools/web-search.ts`
- `packages/skill-runtime/src/tools/web-search.test.ts`

## 实现说明

新增 `invokeWebSearch` provider：未配置 `WebSearchPort` 时抛 E1071；调用 `port.search` 后将 `results` 映射为 `citations` 并与 `summary` 一并返回，供 skillRun toolWebSearch 子能力使用。

## 未解决问题

- 与 `skill-run` 执行器集成留待 T-026；`index.ts` 导出未在本 task 所有权内
