# T-003

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-1 |
| Wave | 1 |
| 描述 | 运行 spec-reviewer 流程产出 v2.0 差距矩阵（纳入审计引用） |
| 追溯 | FR-02 / AC-009 / §5 M-1 差距审计 |
| 依赖 | - |
| TDD Red 用例 | • spec-gap-audit 引用检查：缺少 spec-reviewer 产出路径应失败 |
| 建议测试文件 | • docs/workflow/spec-gap-audit.review.test.mjs（引用存在性） |
| 文件所有权 | • docs/workflow/spec-gap-reviewer-output.md<br>• docs/workflow/spec-gap-audit.md（仅引用节，正文由 T-007 完成） |
| 验收标准 | reviewer 产出物存在且被 audit 引用 |
| 基本验证提示 | 检查 reviewer 输出表格行数 > 0 |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：内联校验脚本（等价于 `spec-gap-audit.review.test.mjs` 引用存在性用例；未单独落盘因文件所有权限制）
- 运行命令：`node --input-type=module -e "..."`（检查 reviewer/audit 存在性及 audit 引用 reviewer 路径）
- 失败摘要（预期内）：`MISSING: docs/workflow/spec-gap-reviewer-output.md`、`MISSING: docs/workflow/spec-gap-audit.md`，exit 1

### Green

- 实现文件：`docs/workflow/spec-gap-reviewer-output.md`、`docs/workflow/spec-gap-audit.md`（引用节）
- 运行命令：同上校验脚本
- 通过摘要：`OK: audit references reviewer; GAP rows= 41`，exit 0

### Refactor

- 变更说明：无（文档初版已结构化，无需重构）

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | 内联 node 校验脚本（Red/Green 同上） | exit 0；GAP 行 40+ | 是 |
| linter/build | 不适用（纯 Markdown 文档） | N/A | 是 |
| 冒烟（若适用） | 人工：`spec-gap-audit.md` 含 `spec-gap-reviewer-output.md` 链接；产出物 §v2.0 差距矩阵 40 行 | 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `docs/workflow/spec-gap-reviewer-output.md`（新建）
- `docs/workflow/spec-gap-audit.md`（新建，仅引用节）
- `docs/workflow/tasks/T-003.md`（本文件）

## 实现说明

按 spec-reviewer 流程审阅 `docs/spec.md`、PRD 与 34 份 superpowers spec，spot-check 代码库后产出 40 行 v2.0 差距矩阵（GAP-001～GAP-040），并在 `spec-gap-audit.md` 建立 AC-009 引用节供 T-008 合并完整 audit 表。

## 未解决问题

- 完整差距表正文待 T-008；`e2e-coverage-matrix.md` 行 ID 对齐待 T-004
- `2026-06-03-switch-dynamic-branches-design.md` 仍为 Draft，与代码 partial 实现不一致（见 GAP-010 开放问题）
