# T-051

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 2 |
| 描述 | 审查并修复 nodeType `executeCommand`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-028, AC-030 / node-runner executeCommand |
| 依赖 | T-034 |
| TDD Red 用例 | • `executeCommand` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E executeCommand spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/execute-command.test.ts（或新建）<br>• apps/web/e2e/nodes/executeCommand.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/execute-command.ts<br>• packages/node-runner/src/executors/execute-command.test.ts<br>• apps/web/e2e/nodes/executeCommand.spec.ts<br>• docs/test/node-audit-rows/executeCommand.md<br>• apps/web/src/features/editor/node-params/executeCommand.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；lite 轨验收通过 |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e executeCommand |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/execute-command.test.ts`、`apps/web/e2e/nodes/executeCommand.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test execute-command.test.ts`；`RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web exec playwright test executeCommand --retries=0`（audit row 创建前）
- 失败摘要（预期内）：E2E audit row 测试 `ENOENT: docs/test/node-audit-rows/executeCommand.md`；面板测试 strict mode「参数」匹配 Tab+Label（已修复选择器）

### Green

- 实现文件：`docs/test/node-audit-rows/executeCommand.md`、`apps/web/e2e/nodes/executeCommand.spec.ts`；`execute-command.test.ts` 补充 registry/errorCode 断言（执行器本身无需改动）
- 运行命令：`pnpm --filter @rxwf/node-runner test execute-command.test.ts`；`RXWF_E2E_TRACK=lite RXWF_E2E_API_PORT=9904 RXWF_E2E_WEB_PORT=9344 pnpm --filter @rxwf/web exec playwright test executeCommand --retries=0`
- 通过摘要：8/8 单测；5/5 E2E（audit row + 面板 + debug-node 成功/空 command 失败）

### Refactor

- 变更说明：E2E 面板用例改为「先 createWorkflow 再打开编辑器」（与 manualTrigger 一致）；表单标签用 `.rxwf-form-field-label` 避免 Tab 同名冲突

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test execute-command.test.ts` | 8 passed | 是 |
| E2E（lite/@any） | `RXWF_E2E_TRACK=lite pnpm --filter @rxwf/web exec playwright test executeCommand --retries=0` | 5 passed | 是 |
| 验收标准核对 | `docs/test/node-audit-rows/executeCommand.md` 四维 ok + status ok；executor 注册与 E2002 错误码有单测/E2E 证据 | 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/execute-command.test.ts`
- `apps/web/e2e/nodes/executeCommand.spec.ts`
- `docs/test/node-audit-rows/executeCommand.md`
- `docs/workflow/tasks/T-051.md`

## 实现说明

审查结论：面板（`ExecuteCommandFields`）、运行时校验、builtin 执行器注册与 E2002 失败路径均已就绪，无需修改 `execute-command.ts`。补充 registry 单测、E2E spec 与 audit row 文档。

## 未解决问题

- standard 轨 E2E 需 Docker compose（本机未启 Docker 时跳过）；lite/@any 已全绿
