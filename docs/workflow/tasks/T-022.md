# T-022

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-2 |
| Wave | 1 |
| 描述 | switch 动态分支：执行器路由逻辑 |
| 追溯 | FR-05 / AC-016 / node-runner switch |
| 依赖 | - |
| TDD Red 用例 | • 多分支匹配应仅走第一 true<br>• 无匹配应丢弃 item |
| 建议测试文件 | • packages/node-runner/src/executors/control-flow/switch.test.ts |
| 文件所有权 | • packages/node-runner/src/executors/control-flow/switch.ts<br>• packages/node-runner/src/executors/control-flow/switch.test.ts |
| 验收标准 | switch 执行 green |
| 基本验证提示 | pnpm --filter @rxwf/node-runner test switch |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/control-flow/switch.test.ts`（新增「多分支匹配仅走第一 true」用例）
- 运行命令：`pnpm --filter @rxwf/node-runner test switch`（临时移除 `break` 以触发预期失败）
- 失败摘要（预期内）：2 failed — `when multiple branches match` 断言 branch[1]/[2] 应为 `[]` 却收到 item；`routes items to the first matching branch in order` 因 catch-all `{{ true }}` 分支重复接收 item 失败

### Green

- 实现文件：`packages/node-runner/src/executors/control-flow/switch.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test switch`
- 通过摘要：4 passed — 按 `branches` 顺序求值，首条 true 分支 `push` 后 `break`；无匹配不 push（丢弃 item）；`branches.length === 0` 返回 `E2003`

### Refactor

- 变更说明：无额外重构；复用 `resolveIfConditionTemplate` + `evaluateCondition` 与 IF 节点一致

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test switch` | 4 passed | 是 |
| linter/build | `pnpm --filter @rxwf/node-runner build` | exit 0 | 是 |
| 冒烟（若适用） | — | N/A | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/control-flow/switch.ts`
- `packages/node-runner/src/executors/control-flow/switch.test.ts`

## 实现说明

`switchExecutor` 对每个 input item 按 `branches` 数组顺序求值 `{{ }}` 条件，命中首条 true 分支后 `break`；全部 false 则丢弃 item 并返回 `success`；空分支列表返回 `failed`/`E2003`。

## 未解决问题

-
