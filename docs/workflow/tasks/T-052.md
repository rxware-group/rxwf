# T-052

> 任务详情文件。开发阶段由被指派的 developer 独占写入；编排器负责合并到 `tasks.md` 索引。

## 元数据（dev-leader 填写，开发阶段只读）

| 字段 | 值 |
|------|-----|
| Milestone | M-3 |
| Wave | 2 |
| 描述 | 审查并修复 nodeType `executeWorkflow`：面板/校验/执行器/错误码 + E2E |
| 追溯 | FR-09, FR-18 / AC-024, AC-028, AC-030 / node-runner executeWorkflow |
| 依赖 | T-034 |
| TDD Red 用例 | • `executeWorkflow` 执行器未注册应失败<br>• 面板渲染快照/校验测试失败直至修复<br>• E2E executeWorkflow spec 应失败直至节点可执行 |
| 建议测试文件 | • packages/node-runner/src/executors/subworkflow.test.ts（或新建）<br>• apps/web/e2e/nodes/executeWorkflow.spec.ts |
| 文件所有权 | • packages/node-runner/src/executors/subworkflow.ts<br>• packages/node-runner/src/executors/subworkflow.test.ts<br>• apps/web/e2e/nodes/executeWorkflow.spec.ts<br>• docs/test/node-audit-rows/executeWorkflow.md<br>• apps/web/src/features/editor/node-params/executeWorkflow.tsx（若存在） |
| 验收标准 | matrix 行 covered；审查行有结论；lite 轨验收通过 |
| 基本验证提示 | pnpm --filter @rxwf/web test:e2e executeWorkflow |

## 状态

`done`

## TDD 证据（developer 填写）

### Red

- 测试文件：`packages/node-runner/src/executors/subworkflow.test.ts`、`apps/web/e2e/nodes/executeWorkflow.spec.ts`
- 运行命令：`pnpm --filter @rxwf/node-runner test subworkflow.test.ts`（补全 registry/executionId fallback 用例后）；`pnpm --filter @rxwf/web test:e2e executeWorkflow`（spec/audit 文件创建前）
- 失败摘要（预期内）：
  - 新增 `uses executionId as parentExecutionId fallback` 用例在修复前失败（仍报 `requires parentExecutionId`）
  - E2E `debug-node runs published child` 失败：`executeWorkflow requires parentExecutionId`（debug-node 未传 `parentExecutionId`）
  - audit row / spec 文件缺失时 E2E 无法运行

### Green

- 实现文件：`subworkflow.ts`（`parentExecutionId ?? executionId` 回退）、`subworkflow.test.ts`、`executeWorkflow.spec.ts`、`executeWorkflow.md`
- 运行命令：`pnpm --filter @rxwf/node-runner build`；`pnpm --filter @rxwf/node-runner test subworkflow.test.ts`；`RXWF_E2E_API_PORT=19878 RXWF_E2E_WEB_PORT=19323 pnpm --filter @rxwf/web test:e2e executeWorkflow --project lite-chromium`
- 通过摘要：10/10 单元测试通过；5/5 E2E 通过（含 audit row、面板、子流 debug-node、缺 workflowId 失败路径）

### Refactor

- 变更说明：E2E 统一使用 `page.request` 避免 `request` fixture 401；子工作流 child 使用 `acceptAll` inputMode 避免 E1056

## Task 基本验证（developer 填写，Verify 步骤）

| 验证项 | 命令 | 结果 | 满足验收标准 |
|--------|------|------|--------------|
| 任务范围测试 | `pnpm --filter @rxwf/node-runner test subworkflow.test.ts` | 12 passed / 0 failed | 是 |
| 构建 | `pnpm --filter @rxwf/node-runner build` | exit 0 | 是 |
| 冒烟（E2E） | `RXWF_E2E_API_PORT=19878 RXWF_E2E_WEB_PORT=19323 pnpm --filter @rxwf/web test:e2e executeWorkflow --project lite-chromium` | 5 passed / 0 failed | 是（@any 在 lite 轨验证；standard 轨同 grep） |
| 验收标准核对 | audit row `status=ok`；E2E 覆盖面板+执行；registry/错误码有单元证据 | 通过 | 是 |

**结论**：`passed`

## 实际变更文件

- `packages/node-runner/src/executors/subworkflow.ts`
- `packages/node-runner/src/executors/subworkflow.test.ts`
- `apps/web/e2e/nodes/executeWorkflow.spec.ts`
- `docs/test/node-audit-rows/executeWorkflow.md`
- `docs/workflow/tasks/T-052.md`

## 实现说明

审查 `executeWorkflow` 全链路：面板（workflowId + SubworkflowInputMappingFields）、校验（E1003/E1053/W1015）、执行器（条件注册 + runChild 管道）、错误码（E2003/E2008/E1054 等）。修复 debug-node 路径：`parentExecutionId` 缺失时回退 `executionId`，使 partial/debug 子流可执行。新增 audit row 与 E2E spec（AUDIT-N-executeWorkflow / E2E-N-executeWorkflow）。

## 未解决问题

- `e2e-coverage-matrix.md` 中 E2E-N-executeWorkflow 行仍 `uncovered`（归 T-081 批量更新，非本 task 所有权）
- `node-audit-matrix.md` 合并待 T-080；本 task 已写入 `node-audit-rows/executeWorkflow.md`
- E2E 变更 `node-runner` 后须 `pnpm --filter @rxwf/node-runner build`，API dev 不 watch 工作区包源码
